'use strict';

var Functor = require('./functor'),
    Pledge  = require('./pledge');

var Cell = function(tuple) ***REMOVED***
  this._ext     = tuple[0];
  this._session = tuple[1];

  this._functors = ***REMOVED***
    incoming: new Functor(this._session, 'processIncomingMessage'),
    outgoing: new Functor(this._session, 'processOutgoingMessage')
  ***REMOVED***;
***REMOVED***;

Cell.prototype.pending = function(direction) ***REMOVED***
  var functor = this._functors[direction];
  if (!functor._stopped) functor.pending += 1;
***REMOVED***;

Cell.prototype.incoming = function(error, message, callback, context) ***REMOVED***
  this._exec('incoming', error, message, callback, context);
***REMOVED***;

Cell.prototype.outgoing = function(error, message, callback, context) ***REMOVED***
  this._exec('outgoing', error, message, callback, context);
***REMOVED***;

Cell.prototype.close = function() ***REMOVED***
  this._closed = this._closed || new Pledge();
  this._doClose();
  return this._closed;
***REMOVED***;

Cell.prototype._exec = function(direction, error, message, callback, context) ***REMOVED***
  this._functors[direction].call(error, message, function(err, msg) ***REMOVED***
    if (err) err.message = this._ext.name + ': ' + err.message;
    callback.call(context, err, msg);
    this._doClose();
  ***REMOVED***, this);
***REMOVED***;

Cell.prototype._doClose = function() ***REMOVED***
  var fin  = this._functors.incoming,
      fout = this._functors.outgoing;

  if (!this._closed || fin.pending + fout.pending !== 0) return;
  if (this._session) this._session.close();
  this._session = null;
  this._closed.done();
***REMOVED***;

module.exports = Cell;
