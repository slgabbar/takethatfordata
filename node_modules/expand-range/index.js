/*!
 * expand-range <https://github.com/jonschlinkert/expand-range>
 *
 * Copyright (c) 2014-2015, Jon Schlinkert.
 * Licensed under the MIT license.
 */

'use strict';

var fill = require('fill-range');

module.exports = function expandRange(str, options, fn) ***REMOVED***
  if (typeof str !== 'string') ***REMOVED***
    throw new TypeError('expand-range expects a string.');
  ***REMOVED***

  if (typeof options === 'function') ***REMOVED***
    fn = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  if (typeof options === 'boolean') ***REMOVED***
    options = ***REMOVED******REMOVED***;
    options.makeRe = true;
  ***REMOVED***

  // create arguments to pass to fill-range
  var opts = options || ***REMOVED******REMOVED***;
  var args = str.split('..');
  var len = args.length;
  if (len > 3) ***REMOVED*** return str; ***REMOVED***

  // if only one argument, it can't expand so return it
  if (len === 1) ***REMOVED*** return args; ***REMOVED***

  // if `true`, tell fill-range to regexify the string
  if (typeof fn === 'boolean' && fn === true) ***REMOVED***
    opts.makeRe = true;
  ***REMOVED***

  args.push(opts);
  return fill.apply(null, args.concat(fn));
***REMOVED***;
