/*!
 * get-value <https://github.com/jonschlinkert/get-value>
 *
 * Copyright (c) 2014-2015, Jon Schlinkert.
 * Licensed under the MIT License.
 */

module.exports = function(obj, prop, a, b, c) ***REMOVED***
  if (!isObject(obj) || !prop) ***REMOVED***
    return obj;
  ***REMOVED***

  prop = toString(prop);

  // allowing for multiple properties to be passed as
  // a string or array, but much faster (3-4x) than doing
  // `[].slice.call(arguments)`
  if (a) prop += '.' + toString(a);
  if (b) prop += '.' + toString(b);
  if (c) prop += '.' + toString(c);

  if (prop in obj) ***REMOVED***
    return obj[prop];
  ***REMOVED***

  var segs = prop.split('.');
  var len = segs.length;
  var i = -1;

  while (obj && (++i < len)) ***REMOVED***
    var key = segs[i];
    while (key[key.length - 1] === '\\') ***REMOVED***
      key = key.slice(0, -1) + '.' + segs[++i];
    ***REMOVED***
    obj = obj[key];
  ***REMOVED***
  return obj;
***REMOVED***;

function isObject(val) ***REMOVED***
  return val !== null && (typeof val === 'object' || typeof val === 'function');
***REMOVED***

function toString(val) ***REMOVED***
  if (!val) return '';
  if (Array.isArray(val)) ***REMOVED***
    return val.join('.');
  ***REMOVED***
  return val;
***REMOVED***
