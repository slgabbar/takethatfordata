var once = require('once')
var eos = require('end-of-stream')
var fs = require('fs') // we only need fs to get the ReadStream and WriteStream prototypes

var noop = function () ***REMOVED******REMOVED***
var ancient = /^v?\.0/.test(process.version)

var isFn = function (fn) ***REMOVED***
  return typeof fn === 'function'
***REMOVED***

var isFS = function (stream) ***REMOVED***
  if (!ancient) return false // newer node version do not need to care about fs is a special way
  if (!fs) return false // browser
  return (stream instanceof (fs.ReadStream || noop) || stream instanceof (fs.WriteStream || noop)) && isFn(stream.close)
***REMOVED***

var isRequest = function (stream) ***REMOVED***
  return stream.setHeader && isFn(stream.abort)
***REMOVED***

var destroyer = function (stream, reading, writing, callback) ***REMOVED***
  callback = once(callback)

  var closed = false
  stream.on('close', function () ***REMOVED***
    closed = true
  ***REMOVED***)

  eos(stream, ***REMOVED***readable: reading, writable: writing***REMOVED***, function (err) ***REMOVED***
    if (err) return callback(err)
    closed = true
    callback()
  ***REMOVED***)

  var destroyed = false
  return function (err) ***REMOVED***
    if (closed) return
    if (destroyed) return
    destroyed = true

    if (isFS(stream)) return stream.close(noop) // use close for fs streams to avoid fd leaks
    if (isRequest(stream)) return stream.abort() // request.destroy just do .end - .abort is what we want

    if (isFn(stream.destroy)) return stream.destroy()

    callback(err || new Error('stream was destroyed'))
  ***REMOVED***
***REMOVED***

var call = function (fn) ***REMOVED***
  fn()
***REMOVED***

var pipe = function (from, to) ***REMOVED***
  return from.pipe(to)
***REMOVED***

var pump = function () ***REMOVED***
  var streams = Array.prototype.slice.call(arguments)
  var callback = isFn(streams[streams.length - 1] || noop) && streams.pop() || noop

  if (Array.isArray(streams[0])) streams = streams[0]
  if (streams.length < 2) throw new Error('pump requires two streams per minimum')

  var error
  var destroys = streams.map(function (stream, i) ***REMOVED***
    var reading = i < streams.length - 1
    var writing = i > 0
    return destroyer(stream, reading, writing, function (err) ***REMOVED***
      if (!error) error = err
      if (err) destroys.forEach(call)
      if (reading) return
      destroys.forEach(call)
      callback(error)
    ***REMOVED***)
  ***REMOVED***)

  streams.reduce(pipe)
***REMOVED***

module.exports = pump
