/*!
 * arr-diff <https://github.com/jonschlinkert/arr-diff>
 *
 * Copyright (c) 2014-2017, Jon Schlinkert.
 * Released under the MIT License.
 */

'use strict';

module.exports = function diff(arr/*, arrays*/) ***REMOVED***
  var len = arguments.length;
  var idx = 0;
  while (++idx < len) ***REMOVED***
    arr = diffArray(arr, arguments[idx]);
  ***REMOVED***
  return arr;
***REMOVED***;

function diffArray(one, two) ***REMOVED***
  if (!Array.isArray(two)) ***REMOVED***
    return one.slice();
  ***REMOVED***

  var tlen = two.length
  var olen = one.length;
  var idx = -1;
  var arr = [];

  while (++idx < olen) ***REMOVED***
    var ele = one[idx];

    var hasEle = false;
    for (var i = 0; i < tlen; i++) ***REMOVED***
      var val = two[i];

      if (ele === val) ***REMOVED***
        hasEle = true;
        break;
      ***REMOVED***
    ***REMOVED***

    if (hasEle === false) ***REMOVED***
      arr.push(ele);
    ***REMOVED***
  ***REMOVED***
  return arr;
***REMOVED***
