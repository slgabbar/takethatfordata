/**
 * Javascript implementation of a basic Public Key Infrastructure, including
 * support for RSA public and private keys.
 *
 * @author Dave Longley
 *
 * Copyright (c) 2010-2013 Digital Bazaar, Inc.
 */
var forge = require('./forge');
require('./asn1');
require('./oids');
require('./pbe');
require('./pem');
require('./pbkdf2');
require('./pkcs12');
require('./pss');
require('./rsa');
require('./util');
require('./x509');

// shortcut for asn.1 API
var asn1 = forge.asn1;

/* Public Key Infrastructure (PKI) implementation. */
var pki = module.exports = forge.pki = forge.pki || ***REMOVED******REMOVED***;

/**
 * NOTE: THIS METHOD IS DEPRECATED. Use pem.decode() instead.
 *
 * Converts PEM-formatted data to DER.
 *
 * @param pem the PEM-formatted data.
 *
 * @return the DER-formatted data.
 */
pki.pemToDer = function(pem) ***REMOVED***
  var msg = forge.pem.decode(pem)[0];
  if(msg.procType && msg.procType.type === 'ENCRYPTED') ***REMOVED***
    throw new Error('Could not convert PEM to DER; PEM is encrypted.');
  ***REMOVED***
  return forge.util.createBuffer(msg.body);
***REMOVED***;

/**
 * Converts an RSA private key from PEM format.
 *
 * @param pem the PEM-formatted private key.
 *
 * @return the private key.
 */
pki.privateKeyFromPem = function(pem) ***REMOVED***
  var msg = forge.pem.decode(pem)[0];

  if(msg.type !== 'PRIVATE KEY' && msg.type !== 'RSA PRIVATE KEY') ***REMOVED***
    var error = new Error('Could not convert private key from PEM; PEM ' +
      'header type is not "PRIVATE KEY" or "RSA PRIVATE KEY".');
    error.headerType = msg.type;
    throw error;
  ***REMOVED***
  if(msg.procType && msg.procType.type === 'ENCRYPTED') ***REMOVED***
    throw new Error('Could not convert private key from PEM; PEM is encrypted.');
  ***REMOVED***

  // convert DER to ASN.1 object
  var obj = asn1.fromDer(msg.body);

  return pki.privateKeyFromAsn1(obj);
***REMOVED***;

/**
 * Converts an RSA private key to PEM format.
 *
 * @param key the private key.
 * @param maxline the maximum characters per line, defaults to 64.
 *
 * @return the PEM-formatted private key.
 */
pki.privateKeyToPem = function(key, maxline) ***REMOVED***
  // convert to ASN.1, then DER, then PEM-encode
  var msg = ***REMOVED***
    type: 'RSA PRIVATE KEY',
    body: asn1.toDer(pki.privateKeyToAsn1(key)).getBytes()
  ***REMOVED***;
  return forge.pem.encode(msg, ***REMOVED***maxline: maxline***REMOVED***);
***REMOVED***;

/**
 * Converts a PrivateKeyInfo to PEM format.
 *
 * @param pki the PrivateKeyInfo.
 * @param maxline the maximum characters per line, defaults to 64.
 *
 * @return the PEM-formatted private key.
 */
pki.privateKeyInfoToPem = function(pki, maxline) ***REMOVED***
  // convert to DER, then PEM-encode
  var msg = ***REMOVED***
    type: 'PRIVATE KEY',
    body: asn1.toDer(pki).getBytes()
  ***REMOVED***;
  return forge.pem.encode(msg, ***REMOVED***maxline: maxline***REMOVED***);
***REMOVED***;
