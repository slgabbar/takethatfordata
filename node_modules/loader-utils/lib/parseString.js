"use strict";

function parseString(str) ***REMOVED***
	try ***REMOVED***
		if(str[0] === "\"") return JSON.parse(str);
		if(str[0] === "'" && str.substr(str.length - 1) === "'") ***REMOVED***
			return parseString(
				str
					.replace(/\\.|"/g, x => x === "\"" ? "\\\"" : x)
					.replace(/^'|'$/g, "\"")
			);
		***REMOVED***
		return JSON.parse("\"" + str + "\"");
	***REMOVED*** catch(e) ***REMOVED***
		return str;
	***REMOVED***
***REMOVED***

module.exports = parseString;
