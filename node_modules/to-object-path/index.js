/*!
 * to-object-path <https://github.com/jonschlinkert/to-object-path>
 *
 * Copyright (c) 2015, Jon Schlinkert.
 * Licensed under the MIT License.
 */

'use strict';

var typeOf = require('kind-of');

module.exports = function toPath(args) ***REMOVED***
  if (typeOf(args) !== 'arguments') ***REMOVED***
    args = arguments;
  ***REMOVED***
  return filter(args).join('.');
***REMOVED***;

function filter(arr) ***REMOVED***
  var len = arr.length;
  var idx = -1;
  var res = [];

  while (++idx < len) ***REMOVED***
    var ele = arr[idx];
    if (typeOf(ele) === 'arguments' || Array.isArray(ele)) ***REMOVED***
      res.push.apply(res, filter(ele));
    ***REMOVED*** else if (typeof ele === 'string') ***REMOVED***
      res.push(ele);
    ***REMOVED***
  ***REMOVED***
  return res;
***REMOVED***
