'use strict';

var defineProperty = require('core-js/library/fn/object/define-property');
var forEach = require('core-js/library/fn/array/for-each');
var keys = require('core-js/library/fn/object/keys');

module.exports = function defineProperties (obj, map) ***REMOVED***
    forEach(keys(map), function (name) ***REMOVED***
        defineProperty(obj, name, ***REMOVED***
            configurable: true,
            enumerable: false,
            value: map[name],
            writable: true
        ***REMOVED***);
    ***REMOVED***);
***REMOVED***;
