module.exports = function xor (a, b) ***REMOVED***
  var length = Math.min(a.length, b.length)
  var buffer = new Buffer(length)

  for (var i = 0; i < length; ++i) ***REMOVED***
    buffer[i] = a[i] ^ b[i]
  ***REMOVED***

  return buffer
***REMOVED***
