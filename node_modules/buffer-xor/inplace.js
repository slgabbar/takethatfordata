module.exports = function xorInplace (a, b) ***REMOVED***
  var length = Math.min(a.length, b.length)

  for (var i = 0; i < length; ++i) ***REMOVED***
    a[i] = a[i] ^ b[i]
  ***REMOVED***

  return a.slice(0, length)
***REMOVED***
