#!/usr/bin/env node

var gp12 = require('../');
var argv = process.argv;
var p12Path = argv[2];

if (!p12Path) ***REMOVED***
  console.error('Please specify a *.p12 file to convert.');
  process.exit(1);
***REMOVED***

gp12(p12Path, function(err, pem) ***REMOVED***
  if (err) ***REMOVED***
    console.log(err);
    process.exit(1);
  ***REMOVED*** else ***REMOVED***
    console.log(pem);
  ***REMOVED***
***REMOVED***);
