// @@match logic
require('./_fix-re-wks')('match', 1, function (defined, MATCH, $match) ***REMOVED***
  // 21.1.3.11 String.prototype.match(regexp)
  return [function match(regexp) ***REMOVED***
    'use strict';
    var O = defined(this);
    var fn = regexp == undefined ? undefined : regexp[MATCH];
    return fn !== undefined ? fn.call(regexp, O) : new RegExp(regexp)[MATCH](String(O));
  ***REMOVED***, $match];
***REMOVED***);
