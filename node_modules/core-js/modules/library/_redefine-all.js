var hide = require('./_hide');
module.exports = function (target, src, safe) ***REMOVED***
  for (var key in src) ***REMOVED***
    if (safe && target[key]) target[key] = src[key];
    else hide(target, key, src[key]);
  ***REMOVED*** return target;
***REMOVED***;
