'use strict';
var strong = require('./_collection-strong');
var validate = require('./_validate-collection');
var SET = 'Set';

// 23.2 Set Objects
module.exports = require('./_collection')(SET, function (get) ***REMOVED***
  return function Set() ***REMOVED*** return get(this, arguments.length > 0 ? arguments[0] : undefined); ***REMOVED***;
***REMOVED***, ***REMOVED***
  // 23.2.3.1 Set.prototype.add(value)
  add: function add(value) ***REMOVED***
    return strong.def(validate(this, SET), value = value === 0 ? 0 : value, value);
  ***REMOVED***
***REMOVED***, strong);
