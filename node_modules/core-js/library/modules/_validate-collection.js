var isObject = require('./_is-object');
module.exports = function (it, TYPE) ***REMOVED***
  if (!isObject(it) || it._t !== TYPE) throw TypeError('Incompatible receiver, ' + TYPE + ' required!');
  return it;
***REMOVED***;
