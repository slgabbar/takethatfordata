module.exports = function (regExp, replace) ***REMOVED***
  var replacer = replace === Object(replace) ? function (part) ***REMOVED***
    return replace[part];
  ***REMOVED*** : replace;
  return function (it) ***REMOVED***
    return String(it).replace(regExp, replacer);
  ***REMOVED***;
***REMOVED***;
