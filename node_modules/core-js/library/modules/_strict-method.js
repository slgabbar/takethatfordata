'use strict';
var fails = require('./_fails');

module.exports = function (method, arg) ***REMOVED***
  return !!method && fails(function () ***REMOVED***
    // eslint-disable-next-line no-useless-call
    arg ? method.call(null, function () ***REMOVED*** /* empty */ ***REMOVED***, 1) : method.call(null);
  ***REMOVED***);
***REMOVED***;
