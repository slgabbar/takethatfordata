// most Object methods by ES6 should accept primitives
var $export = require('./_export');
var core = require('./_core');
var fails = require('./_fails');
module.exports = function (KEY, exec) ***REMOVED***
  var fn = (core.Object || ***REMOVED******REMOVED***)[KEY] || Object[KEY];
  var exp = ***REMOVED******REMOVED***;
  exp[KEY] = exec(fn);
  $export($export.S + $export.F * fails(function () ***REMOVED*** fn(1); ***REMOVED***), 'Object', exp);
***REMOVED***;
