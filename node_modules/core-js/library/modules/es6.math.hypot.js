// 20.2.2.17 Math.hypot([value1[, value2[, … ]]])
var $export = require('./_export');
var abs = Math.abs;

$export($export.S, 'Math', ***REMOVED***
  hypot: function hypot(value1, value2) ***REMOVED*** // eslint-disable-line no-unused-vars
    var sum = 0;
    var i = 0;
    var aLen = arguments.length;
    var larg = 0;
    var arg, div;
    while (i < aLen) ***REMOVED***
      arg = abs(arguments[i++]);
      if (larg < arg) ***REMOVED***
        div = larg / arg;
        sum = sum * div * div + 1;
        larg = arg;
      ***REMOVED*** else if (arg > 0) ***REMOVED***
        div = arg / larg;
        sum += div * div;
      ***REMOVED*** else sum += arg;
    ***REMOVED***
    return larg === Infinity ? Infinity : larg * Math.sqrt(sum);
  ***REMOVED***
***REMOVED***);
