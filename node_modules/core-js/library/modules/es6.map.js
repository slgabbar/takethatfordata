'use strict';
var strong = require('./_collection-strong');
var validate = require('./_validate-collection');
var MAP = 'Map';

// 23.1 Map Objects
module.exports = require('./_collection')(MAP, function (get) ***REMOVED***
  return function Map() ***REMOVED*** return get(this, arguments.length > 0 ? arguments[0] : undefined); ***REMOVED***;
***REMOVED***, ***REMOVED***
  // 23.1.3.6 Map.prototype.get(key)
  get: function get(key) ***REMOVED***
    var entry = strong.getEntry(validate(this, MAP), key);
    return entry && entry.v;
  ***REMOVED***,
  // 23.1.3.9 Map.prototype.set(key, value)
  set: function set(key, value) ***REMOVED***
    return strong.def(validate(this, MAP), key === 0 ? 0 : key, value);
  ***REMOVED***
***REMOVED***, strong, true);
