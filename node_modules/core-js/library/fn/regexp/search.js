require('../../modules/es6.regexp.search');
var SEARCH = require('../../modules/_wks')('search');
module.exports = function (it, str) ***REMOVED***
  return RegExp.prototype[SEARCH].call(it, str);
***REMOVED***;
