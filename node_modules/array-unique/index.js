/*!
 * array-unique <https://github.com/jonschlinkert/array-unique>
 *
 * Copyright (c) 2014-2015, Jon Schlinkert.
 * Licensed under the MIT License.
 */

'use strict';

module.exports = function unique(arr) ***REMOVED***
  if (!Array.isArray(arr)) ***REMOVED***
    throw new TypeError('array-unique expects an array.');
  ***REMOVED***

  var len = arr.length;
  var i = -1;

  while (i++ < len) ***REMOVED***
    var j = i + 1;

    for (; j < arr.length; ++j) ***REMOVED***
      if (arr[i] === arr[j]) ***REMOVED***
        arr.splice(j--, 1);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
  return arr;
***REMOVED***;

module.exports.immutable = function uniqueImmutable(arr) ***REMOVED***
  if (!Array.isArray(arr)) ***REMOVED***
    throw new TypeError('array-unique expects an array.');
  ***REMOVED***

  var arrLen = arr.length;
  var newArr = new Array(arrLen);

  for (var i = 0; i < arrLen; i++) ***REMOVED***
    newArr[i] = arr[i];
  ***REMOVED***

  return module.exports(newArr);
***REMOVED***;
