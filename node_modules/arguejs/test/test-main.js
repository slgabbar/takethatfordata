var tests = Object.keys(window.__karma__.files).filter(function (file) ***REMOVED***
      return /Spec\.js$/.test(file);
***REMOVED***);

requirejs.config(***REMOVED***
    // Karma serves files from '/base'
    baseUrl: '/base',

    paths: ***REMOVED***
      'chai': 'lib/chai'
    ***REMOVED***,

    shim: ***REMOVED***
        'underscore': ***REMOVED***
            exports: '_'
        ***REMOVED***
    ***REMOVED***,

    // ask Require.js to load these files (all our tests)
    deps: tests,

    // start test run, once Require.js is done
    callback: window.__karma__.start
***REMOVED***);

