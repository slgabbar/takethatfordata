// take an un-split argv string and tokenize it.
module.exports = function (argString) ***REMOVED***
  if (Array.isArray(argString)) return argString

  var i = 0
  var c = null
  var opening = null
  var args = []

  for (var ii = 0; ii < argString.length; ii++) ***REMOVED***
    c = argString.charAt(ii)

    // split on spaces unless we're in quotes.
    if (c === ' ' && !opening) ***REMOVED***
      i++
      continue
    ***REMOVED***

    // don't split the string if we're in matching
    // opening or closing single and double quotes.
    if (c === opening) ***REMOVED***
      opening = null
      continue
    ***REMOVED*** else if ((c === "'" || c === '"') && !opening) ***REMOVED***
      opening = c
      continue
    ***REMOVED***

    if (!args[i]) args[i] = ''
    args[i] += c
  ***REMOVED***

  return args
***REMOVED***
