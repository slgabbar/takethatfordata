/*!
 * assign-symbols <https://github.com/jonschlinkert/assign-symbols>
 *
 * Copyright (c) 2015, Jon Schlinkert.
 * Licensed under the MIT License.
 */

'use strict';

module.exports = function(receiver, objects) ***REMOVED***
  if (receiver === null || typeof receiver === 'undefined') ***REMOVED***
    throw new TypeError('expected first argument to be an object.');
  ***REMOVED***

  if (typeof objects === 'undefined' || typeof Symbol === 'undefined') ***REMOVED***
    return receiver;
  ***REMOVED***

  if (typeof Object.getOwnPropertySymbols !== 'function') ***REMOVED***
    return receiver;
  ***REMOVED***

  var isEnumerable = Object.prototype.propertyIsEnumerable;
  var target = Object(receiver);
  var len = arguments.length, i = 0;

  while (++i < len) ***REMOVED***
    var provider = Object(arguments[i]);
    var names = Object.getOwnPropertySymbols(provider);

    for (var j = 0; j < names.length; j++) ***REMOVED***
      var key = names[j];

      if (isEnumerable.call(provider, key)) ***REMOVED***
        target[key] = provider[key];
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
  return target;
***REMOVED***;
