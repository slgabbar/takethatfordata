'use strict';

var isStream = module.exports = function (stream) ***REMOVED***
	return stream !== null && typeof stream === 'object' && typeof stream.pipe === 'function';
***REMOVED***;

isStream.writable = function (stream) ***REMOVED***
	return isStream(stream) && stream.writable !== false && typeof stream._write === 'function' && typeof stream._writableState === 'object';
***REMOVED***;

isStream.readable = function (stream) ***REMOVED***
	return isStream(stream) && stream.readable !== false && typeof stream._read === 'function' && typeof stream._readableState === 'object';
***REMOVED***;

isStream.duplex = function (stream) ***REMOVED***
	return isStream.writable(stream) && isStream.readable(stream);
***REMOVED***;

isStream.transform = function (stream) ***REMOVED***
	return isStream.duplex(stream) && typeof stream._transform === 'function' && typeof stream._transformState === 'object';
***REMOVED***;
