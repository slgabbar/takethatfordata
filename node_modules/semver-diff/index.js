'use strict';
var semver = require('semver');

module.exports = function (a, b) ***REMOVED***
	if (semver.gt(a, b)) ***REMOVED***
		return null;
	***REMOVED***

	a = semver.parse(a);
	b = semver.parse(b);

	for (var key in a) ***REMOVED***
		if (key === 'major' || key === 'minor' || key === 'patch') ***REMOVED***
			if (a[key] !== b[key]) ***REMOVED***
				return key;
			***REMOVED***
		***REMOVED***

		if (key === 'prerelease' || key === 'build') ***REMOVED***
			if (JSON.stringify(a[key]) !== JSON.stringify(b[key])) ***REMOVED***
				return key;
			***REMOVED***
		***REMOVED***
	***REMOVED***

	return null;
***REMOVED***;
