'use strict';

// See: https://github.com/IndigoUnited/node-cross-spawn/pull/34#issuecomment-221623455
function hasEmptyArgumentBug() ***REMOVED***
    var nodeVer;

    if (process.platform !== 'win32') ***REMOVED***
        return false;
    ***REMOVED***

    nodeVer = process.version.substr(1).split('.').map(function (num) ***REMOVED***
        return parseInt(num, 10);
    ***REMOVED***);

    return (nodeVer[0] === 0 && nodeVer[1] < 12);
***REMOVED***

module.exports = hasEmptyArgumentBug();
