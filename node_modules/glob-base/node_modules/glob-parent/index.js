'use strict';

var path = require('path');
var isglob = require('is-glob');

module.exports = function globParent(str) ***REMOVED***
	str += 'a'; // preserves full path in case of trailing path separator
	do ***REMOVED***str = path.dirname(str)***REMOVED*** while (isglob(str));
	return str;
***REMOVED***;
