var constants = exports;

// Helper
constants._reverse = function reverse(map) ***REMOVED***
  var res = ***REMOVED******REMOVED***;

  Object.keys(map).forEach(function(key) ***REMOVED***
    // Convert key to integer if it is stringified
    if ((key | 0) == key)
      key = key | 0;

    var value = map[key];
    res[value] = key;
  ***REMOVED***);

  return res;
***REMOVED***;

constants.der = require('./der');
