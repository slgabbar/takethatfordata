/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/
function ParsePlugin(source, target) ***REMOVED***
	this.source = source;
	this.target = target;
***REMOVED***
module.exports = ParsePlugin;

ParsePlugin.prototype.apply = function(resolver) ***REMOVED***
	var target = this.target;
	resolver.plugin(this.source, function(request, callback) ***REMOVED***
		var parsed = resolver.parse(request.request);
		var obj = Object.assign(***REMOVED******REMOVED***, request, parsed);
		if(request.query && !parsed.query) ***REMOVED***
			obj.query = request.query;
		***REMOVED***
		if(parsed && callback.log) ***REMOVED***
			if(parsed.module)
				callback.log("Parsed request is a module");
			if(parsed.directory)
				callback.log("Parsed request is a directory");
		***REMOVED***
		resolver.doResolve(target, obj, null, callback);
	***REMOVED***);
***REMOVED***;
