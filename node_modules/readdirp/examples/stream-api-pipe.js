var readdirp =  require('..')
  , path = require('path')
  , through = require('through2')

// print out all JavaScript files along with their size
readdirp(***REMOVED*** root: path.join(__dirname), fileFilter: '*.js' ***REMOVED***)
  .on('warn', function (err) ***REMOVED*** console.error('non-fatal error', err); ***REMOVED***)
  .on('error', function (err) ***REMOVED*** console.error('fatal error', err); ***REMOVED***)
  .pipe(through.obj(function (entry, _, cb) ***REMOVED*** 
    this.push(***REMOVED*** path: entry.path, size: entry.stat.size ***REMOVED***);
    cb();
  ***REMOVED***))
  .pipe(through.obj(
    function (res, _, cb) ***REMOVED*** 
      this.push(JSON.stringify(res) + '\n');
      cb();
    ***REMOVED***)
  )
  .pipe(process.stdout);
