"use strict";

var eq   = require("./eq")
  , some = require("./some");

module.exports = function (obj, searchValue) ***REMOVED***
	var result;
	return some(obj, function (value, name) ***REMOVED***
		if (eq(value, searchValue)) ***REMOVED***
			result = name;
			return true;
		***REMOVED***
		return false;
	***REMOVED***)
		? result
		: null;
***REMOVED***;
