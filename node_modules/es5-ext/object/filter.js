"use strict";

var callable = require("./valid-callable")
  , forEach  = require("./for-each")
  , call     = Function.prototype.call;

module.exports = function (obj, cb /*, thisArg*/) ***REMOVED***
	var result = ***REMOVED******REMOVED***, thisArg = arguments[2];
	callable(cb);
	forEach(obj, function (value, key, targetObj, index) ***REMOVED***
		if (call.call(cb, thisArg, value, key, targetObj, index)) result[key] = targetObj[key];
	***REMOVED***);
	return result;
***REMOVED***;
