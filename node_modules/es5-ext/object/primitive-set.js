"use strict";

var forEach = Array.prototype.forEach, create = Object.create;

// eslint-disable-next-line no-unused-vars
module.exports = function (arg /*, …args*/) ***REMOVED***
	var set = create(null);
	forEach.call(arguments, function (name) ***REMOVED***
		set[name] = true;
	***REMOVED***);
	return set;
***REMOVED***;
