"use strict";

var isPlainObject = require("./is-plain-object")
  , forEach       = require("./for-each")

  , process;

process = function self(value, key) ***REMOVED***
	if (isPlainObject(value)) forEach(value, self, this);
	else this[key] = value;
***REMOVED***;

module.exports = function (obj) ***REMOVED***
	var flattened = ***REMOVED******REMOVED***;
	forEach(obj, process, flattened);
	return flattened;
***REMOVED***;
