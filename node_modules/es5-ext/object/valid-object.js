"use strict";

var isObject = require("./is-object");

module.exports = function (value) ***REMOVED***
	if (!isObject(value)) throw new TypeError(value + " is not an Object");
	return value;
***REMOVED***;
