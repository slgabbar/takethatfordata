"use strict";

var keys  = require("../keys")
  , value = require("../valid-value")
  , max   = Math.max;

module.exports = function (dest, src /*, …srcn*/) ***REMOVED***
	var error, i, length = max(arguments.length, 2), assign;
	dest = Object(value(dest));
	assign = function (key) ***REMOVED***
		try ***REMOVED***
			dest[key] = src[key];
		***REMOVED*** catch (e) ***REMOVED***
			if (!error) error = e;
		***REMOVED***
	***REMOVED***;
	for (i = 1; i < length; ++i) ***REMOVED***
		src = arguments[i];
		keys(src).forEach(assign);
	***REMOVED***
	if (error !== undefined) throw error;
	return dest;
***REMOVED***;
