"use strict";

var value    = require("../../object/valid-value")
  , aFrom    = require("../from")
  , toArray  = require("../to-array")
  , contains = require("./contains")
  , byLength = require("./_compare-by-length")
  , filter   = Array.prototype.filter
  , push     = Array.prototype.push;

module.exports = function (/* …lists*/) ***REMOVED***
	var lists, seen, result;
	if (!arguments.length) return aFrom(this);
	push.apply(lists = [this], arguments);
	lists.forEach(value);
	seen = [];
	result = [];
	lists.sort(byLength).forEach(function (list) ***REMOVED***
		result = result
			.filter(function (item) ***REMOVED***
				return !contains.call(list, item);
			***REMOVED***)
			.concat(
				filter.call(list, function (item) ***REMOVED***
					return !contains.call(seen, item);
				***REMOVED***)
			);
		push.apply(seen, toArray(list));
	***REMOVED***);
	return result;
***REMOVED***;
