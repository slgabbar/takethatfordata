"use strict";

var callable    = require("../../../object/valid-callable")
  , ensureValue = require("../../../object/valid-value")
  , some        = Array.prototype.some
  , apply       = Function.prototype.apply;

module.exports = function (predicate /*, thisArg*/) ***REMOVED***
	var k, self;
	self = Object(ensureValue(this));
	callable(predicate);

	return some.call(
		self,
		function (value, index) ***REMOVED***
			if (apply.call(predicate, this, arguments)) ***REMOVED***
				k = index;
				return true;
			***REMOVED***
			return false;
		***REMOVED***,
		arguments[1]
	)
		? k
		: -1;
***REMOVED***;
