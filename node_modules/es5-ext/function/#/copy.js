"use strict";

var mixin         = require("../../object/mixin")
  , validFunction = require("../valid-function")
  , re            = /^\s*function\s*([\0-')-\uffff]+)*\s*\(([\0-(*-\uffff]*)\)\s*\***REMOVED***/;

module.exports = function () ***REMOVED***
	var match = String(validFunction(this)).match(re), fn;

	// eslint-disable-next-line no-new-func
	fn = new Function(
		"fn",
		"return function " +
			match[1].trim() +
			"(" +
			match[2] +
			") ***REMOVED*** return fn.apply(this, arguments); ***REMOVED***;"
	)(this);
	try ***REMOVED***
		mixin(fn, this);
	***REMOVED*** catch (ignore) ***REMOVED******REMOVED***
	return fn;
***REMOVED***;
