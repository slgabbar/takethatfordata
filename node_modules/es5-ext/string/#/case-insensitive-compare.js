"use strict";

var toLowerCase = String.prototype.toLowerCase;

module.exports = function (other) ***REMOVED***
	return toLowerCase.call(this).localeCompare(toLowerCase.call(String(other)));
***REMOVED***;
