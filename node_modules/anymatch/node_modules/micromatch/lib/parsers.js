'use strict';

var extglob = require('extglob');
var nanomatch = require('nanomatch');
var regexNot = require('regex-not');
var toRegex = require('to-regex');
var not;

/**
 * Characters to use in negation regex (we want to "not" match
 * characters that are matched by other parsers)
 */

var TEXT = '([!@*?+]?\\(|\\)|\\[:?(?=.*?:?\\])|:?\\]|[*+?!^$.\\\\/])+';
var createNotRegex = function(opts) ***REMOVED***
  return not || (not = textRegex(TEXT));
***REMOVED***;

/**
 * Parsers
 */

module.exports = function(snapdragon) ***REMOVED***
  var parsers = snapdragon.parser.parsers;

  // register nanomatch parsers
  snapdragon.use(nanomatch.parsers);

  // get references to some specific nanomatch parsers before they
  // are overridden by the extglob and/or parsers
  var escape = parsers.escape;
  var slash = parsers.slash;
  var qmark = parsers.qmark;
  var plus = parsers.plus;
  var star = parsers.star;
  var dot = parsers.dot;

  // register extglob parsers
  snapdragon.use(extglob.parsers);

  // custom micromatch parsers
  snapdragon.parser
    .use(function() ***REMOVED***
      // override "notRegex" created in nanomatch parser
      this.notRegex = /^\!+(?!\()/;
    ***REMOVED***)
    // reset the referenced parsers
    .capture('escape', escape)
    .capture('slash', slash)
    .capture('qmark', qmark)
    .capture('star', star)
    .capture('plus', plus)
    .capture('dot', dot)

    /**
     * Override `text` parser
     */

    .capture('text', function() ***REMOVED***
      if (this.isInside('bracket')) return;
      var pos = this.position();
      var m = this.match(createNotRegex(this.options));
      if (!m || !m[0]) return;

      // escape regex boundary characters and simple brackets
      var val = m[0].replace(/([[\]^$])/g, '\\$1');

      return pos(***REMOVED***
        type: 'text',
        val: val
      ***REMOVED***);
    ***REMOVED***);
***REMOVED***;

/**
 * Create text regex
 */

function textRegex(pattern) ***REMOVED***
  var notStr = regexNot.create(pattern, ***REMOVED***contains: true, strictClose: false***REMOVED***);
  var prefix = '(?:[\\^]|\\\\|';
  return toRegex(prefix + notStr + ')', ***REMOVED***strictClose: false***REMOVED***);
***REMOVED***
