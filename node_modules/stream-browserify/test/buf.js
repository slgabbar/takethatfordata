var path = require('path');
var test = require('tape');

var Writable = require('../').Writable;
var inherits = require('inherits');

inherits(TestWritable, Writable);

function TestWritable(opt) ***REMOVED***
    if (!(this instanceof TestWritable))
        return new TestWritable(opt);
    Writable.call(this, opt);
    this._written = [];
***REMOVED***

TestWritable.prototype._write = function(chunk, encoding, cb) ***REMOVED***
    this._written.push(chunk);
    cb();
***REMOVED***;

var buf = Buffer([ 88 ]);

test('.writable writing ArrayBuffer', function(t) ***REMOVED***
    var writable = new TestWritable();
    
    writable.write(buf);
    writable.end();
    
    t.equal(writable._written.length, 1);
    t.equal(writable._written[0].toString(), 'X')
    t.end()
***REMOVED***);
