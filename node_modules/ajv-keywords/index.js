'use strict';

var KEYWORDS = require('./keywords');

module.exports = defineKeywords;


/**
 * Defines one or several keywords in ajv instance
 * @param  ***REMOVED***Ajv***REMOVED*** ajv validator instance
 * @param  ***REMOVED***String|Array<String>|undefined***REMOVED*** keyword keyword(s) to define
 * @return ***REMOVED***Ajv***REMOVED*** ajv instance (for chaining)
 */
function defineKeywords(ajv, keyword) ***REMOVED***
  if (Array.isArray(keyword)) ***REMOVED***
    for (var i=0; i<keyword.length; i++)
      get(keyword[i])(ajv);
    return ajv;
  ***REMOVED***
  if (keyword) ***REMOVED***
    get(keyword)(ajv);
    return ajv;
  ***REMOVED***
  for (keyword in KEYWORDS) get(keyword)(ajv);
  return ajv;
***REMOVED***


defineKeywords.get = get;

function get(keyword) ***REMOVED***
  var defFunc = KEYWORDS[keyword];
  if (!defFunc) throw new Error('Unknown keyword ' + keyword);
  return defFunc;
***REMOVED***
