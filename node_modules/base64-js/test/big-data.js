var test = require('tape')
var b64 = require('../')

test('convert big data to base64', function (t) ***REMOVED***
  var b64str, arr, i, length
  var big = new Uint8Array(64 * 1024 * 1024)
  for (i = 0, length = big.length; i < length; ++i) ***REMOVED***
    big[i] = i % 256
  ***REMOVED***
  b64str = b64.fromByteArray(big)
  arr = b64.toByteArray(b64str)
  t.ok(equal(arr, big))
  t.end()
***REMOVED***)

function equal (a, b) ***REMOVED***
  var i
  var length = a.length
  if (length !== b.length) return false
  for (i = 0; i < length; ++i) ***REMOVED***
    if (a[i] !== b[i]) return false
  ***REMOVED***
  return true
***REMOVED***
