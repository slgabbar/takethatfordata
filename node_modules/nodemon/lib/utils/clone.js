module.exports = clone;

// via http://stackoverflow.com/a/728694/22617
function clone(obj) ***REMOVED***
  // Handle the 3 simple types, and null or undefined
  if (null === obj || 'object' !== typeof obj) ***REMOVED***
    return obj;
  ***REMOVED***

  var copy;

  // Handle Date
  if (obj instanceof Date) ***REMOVED***
    copy = new Date();
    copy.setTime(obj.getTime());
    return copy;
  ***REMOVED***

  // Handle Array
  if (obj instanceof Array) ***REMOVED***
    copy = [];
    for (var i = 0, len = obj.length; i < len; i++) ***REMOVED***
      copy[i] = clone(obj[i]);
    ***REMOVED***
    return copy;
  ***REMOVED***

  // Handle Object
  if (obj instanceof Object) ***REMOVED***
    copy = ***REMOVED******REMOVED***;
    for (var attr in obj) ***REMOVED***
      if (obj.hasOwnProperty && obj.hasOwnProperty(attr)) ***REMOVED***
        copy[attr] = clone(obj[attr]);
      ***REMOVED***
    ***REMOVED***
    return copy;
  ***REMOVED***

  throw new Error('Unable to copy obj! Its type isn\'t supported.');
***REMOVED***