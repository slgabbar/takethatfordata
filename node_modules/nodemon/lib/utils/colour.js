/**
 * Encodes a string in a colour: red, yellow or green
 * @param  ***REMOVED***String***REMOVED*** c   colour to highlight in
 * @param  ***REMOVED***String***REMOVED*** str the string to encode
 * @return ***REMOVED***String***REMOVED***     coloured string for terminal printing
 */
function colour(c, str) ***REMOVED***
  return (colour[c] || colour.black) + str + colour.black;
***REMOVED***

function strip(str) ***REMOVED***
  re.lastIndex = 0; // reset position
  return str.replace(re, '');
***REMOVED***

colour.red = '\x1B[31m';
colour.yellow = '\x1B[33m';
colour.green = '\x1B[32m';
colour.black = '\x1B[39m';

var reStr = Object.keys(colour).map(function (key) ***REMOVED***
  return colour[key];
***REMOVED***).join('|');

var re = new RegExp(('(' + reStr + ')').replace(/\[/g, '\\['), 'g');

colour.strip = strip;


module.exports = colour;