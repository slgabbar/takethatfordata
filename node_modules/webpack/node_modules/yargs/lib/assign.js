// lazy Object.assign logic that only works for merging
// two objects; eventually we should replace this with Object.assign.
module.exports = function assign (defaults, configuration) ***REMOVED***
  var o = ***REMOVED******REMOVED***
  configuration = configuration || ***REMOVED******REMOVED***

  Object.keys(defaults).forEach(function (k) ***REMOVED***
    o[k] = defaults[k]
  ***REMOVED***)
  Object.keys(configuration).forEach(function (k) ***REMOVED***
    o[k] = configuration[k]
  ***REMOVED***)

  return o
***REMOVED***
