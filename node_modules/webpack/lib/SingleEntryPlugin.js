/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/
"use strict";
const SingleEntryDependency = require("./dependencies/SingleEntryDependency");

class SingleEntryPlugin ***REMOVED***
	constructor(context, entry, name) ***REMOVED***
		this.context = context;
		this.entry = entry;
		this.name = name;
	***REMOVED***

	apply(compiler) ***REMOVED***
		compiler.plugin("compilation", (compilation, params) => ***REMOVED***
			const normalModuleFactory = params.normalModuleFactory;

			compilation.dependencyFactories.set(SingleEntryDependency, normalModuleFactory);
		***REMOVED***);

		compiler.plugin("make", (compilation, callback) => ***REMOVED***
			const dep = SingleEntryPlugin.createDependency(this.entry, this.name);
			compilation.addEntry(this.context, dep, this.name, callback);
		***REMOVED***);
	***REMOVED***

	static createDependency(entry, name) ***REMOVED***
		const dep = new SingleEntryDependency(entry);
		dep.loc = name;
		return dep;
	***REMOVED***
***REMOVED***

module.exports = SingleEntryPlugin;
