'use strict';
module.exports = function (str) ***REMOVED***
	var isExtendedLengthPath = /^\\\\\?\\/.test(str);
	var hasNonAscii = /[^\x00-\x80]+/.test(str);

	if (isExtendedLengthPath || hasNonAscii) ***REMOVED***
		return str;
	***REMOVED***

	return str.replace(/\\/g, '/');
***REMOVED***;
