var tape = require("tape");

var fetch = require("..");

tape.test("fetch", function(test) ***REMOVED***

    if (typeof Promise !== "undefined") ***REMOVED***
        var promise = fetch("NOTFOUND");
        promise.catch(function() ***REMOVED******REMOVED***);
        test.ok(promise instanceof Promise, "should return a promise if callback has been omitted");
    ***REMOVED***

    // TODO - some way to test this properly?
    
    test.end();
***REMOVED***);
