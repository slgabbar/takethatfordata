/*
 *
 * Copyright 2015 gRPC authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 */

#include "src/core/lib/security/credentials/credentials.h"

#include <stdio.h>
#include <string.h>

#include "src/core/lib/channel/channel_args.h"
#include "src/core/lib/http/httpcli.h"
#include "src/core/lib/http/parser.h"
#include "src/core/lib/iomgr/executor.h"
#include "src/core/lib/json/json.h"
#include "src/core/lib/support/string.h"
#include "src/core/lib/surface/api_trace.h"

#include <grpc/support/alloc.h>
#include <grpc/support/log.h>
#include <grpc/support/string_util.h>
#include <grpc/support/sync.h>
#include <grpc/support/time.h>

/* -- Common. -- */

grpc_credentials_metadata_request *grpc_credentials_metadata_request_create(
    grpc_call_credentials *creds) ***REMOVED***
  grpc_credentials_metadata_request *r =
      gpr_zalloc(sizeof(grpc_credentials_metadata_request));
  r->creds = grpc_call_credentials_ref(creds);
  return r;
***REMOVED***

void grpc_credentials_metadata_request_destroy(
    grpc_exec_ctx *exec_ctx, grpc_credentials_metadata_request *r) ***REMOVED***
  grpc_call_credentials_unref(exec_ctx, r->creds);
  grpc_http_response_destroy(&r->response);
  gpr_free(r);
***REMOVED***

grpc_channel_credentials *grpc_channel_credentials_ref(
    grpc_channel_credentials *creds) ***REMOVED***
  if (creds == NULL) return NULL;
  gpr_ref(&creds->refcount);
  return creds;
***REMOVED***

void grpc_channel_credentials_unref(grpc_exec_ctx *exec_ctx,
                                    grpc_channel_credentials *creds) ***REMOVED***
  if (creds == NULL) return;
  if (gpr_unref(&creds->refcount)) ***REMOVED***
    if (creds->vtable->destruct != NULL) ***REMOVED***
      creds->vtable->destruct(exec_ctx, creds);
    ***REMOVED***
    gpr_free(creds);
  ***REMOVED***
***REMOVED***

void grpc_channel_credentials_release(grpc_channel_credentials *creds) ***REMOVED***
  GRPC_API_TRACE("grpc_channel_credentials_release(creds=%p)", 1, (creds));
  grpc_exec_ctx exec_ctx = GRPC_EXEC_CTX_INIT;
  grpc_channel_credentials_unref(&exec_ctx, creds);
  grpc_exec_ctx_finish(&exec_ctx);
***REMOVED***

grpc_call_credentials *grpc_call_credentials_ref(grpc_call_credentials *creds) ***REMOVED***
  if (creds == NULL) return NULL;
  gpr_ref(&creds->refcount);
  return creds;
***REMOVED***

void grpc_call_credentials_unref(grpc_exec_ctx *exec_ctx,
                                 grpc_call_credentials *creds) ***REMOVED***
  if (creds == NULL) return;
  if (gpr_unref(&creds->refcount)) ***REMOVED***
    if (creds->vtable->destruct != NULL) ***REMOVED***
      creds->vtable->destruct(exec_ctx, creds);
    ***REMOVED***
    gpr_free(creds);
  ***REMOVED***
***REMOVED***

void grpc_call_credentials_release(grpc_call_credentials *creds) ***REMOVED***
  GRPC_API_TRACE("grpc_call_credentials_release(creds=%p)", 1, (creds));
  grpc_exec_ctx exec_ctx = GRPC_EXEC_CTX_INIT;
  grpc_call_credentials_unref(&exec_ctx, creds);
  grpc_exec_ctx_finish(&exec_ctx);
***REMOVED***

bool grpc_call_credentials_get_request_metadata(
    grpc_exec_ctx *exec_ctx, grpc_call_credentials *creds,
    grpc_polling_entity *pollent, grpc_auth_metadata_context context,
    grpc_credentials_mdelem_array *md_array, grpc_closure *on_request_metadata,
    grpc_error **error) ***REMOVED***
  if (creds == NULL || creds->vtable->get_request_metadata == NULL) ***REMOVED***
    return true;
  ***REMOVED***
  return creds->vtable->get_request_metadata(
      exec_ctx, creds, pollent, context, md_array, on_request_metadata, error);
***REMOVED***

void grpc_call_credentials_cancel_get_request_metadata(
    grpc_exec_ctx *exec_ctx, grpc_call_credentials *creds,
    grpc_credentials_mdelem_array *md_array, grpc_error *error) ***REMOVED***
  if (creds == NULL || creds->vtable->cancel_get_request_metadata == NULL) ***REMOVED***
    return;
  ***REMOVED***
  creds->vtable->cancel_get_request_metadata(exec_ctx, creds, md_array, error);
***REMOVED***

grpc_security_status grpc_channel_credentials_create_security_connector(
    grpc_exec_ctx *exec_ctx, grpc_channel_credentials *channel_creds,
    const char *target, const grpc_channel_args *args,
    grpc_channel_security_connector **sc, grpc_channel_args **new_args) ***REMOVED***
  *new_args = NULL;
  if (channel_creds == NULL) ***REMOVED***
    return GRPC_SECURITY_ERROR;
  ***REMOVED***
  GPR_ASSERT(channel_creds->vtable->create_security_connector != NULL);
  return channel_creds->vtable->create_security_connector(
      exec_ctx, channel_creds, NULL, target, args, sc, new_args);
***REMOVED***

grpc_channel_credentials *
grpc_channel_credentials_duplicate_without_call_credentials(
    grpc_channel_credentials *channel_creds) ***REMOVED***
  if (channel_creds != NULL && channel_creds->vtable != NULL &&
      channel_creds->vtable->duplicate_without_call_credentials != NULL) ***REMOVED***
    return channel_creds->vtable->duplicate_without_call_credentials(
        channel_creds);
  ***REMOVED*** else ***REMOVED***
    return grpc_channel_credentials_ref(channel_creds);
  ***REMOVED***
***REMOVED***

static void credentials_pointer_arg_destroy(grpc_exec_ctx *exec_ctx, void *p) ***REMOVED***
  grpc_channel_credentials_unref(exec_ctx, p);
***REMOVED***

static void *credentials_pointer_arg_copy(void *p) ***REMOVED***
  return grpc_channel_credentials_ref(p);
***REMOVED***

static int credentials_pointer_cmp(void *a, void *b) ***REMOVED*** return GPR_ICMP(a, b); ***REMOVED***

static const grpc_arg_pointer_vtable credentials_pointer_vtable = ***REMOVED***
    credentials_pointer_arg_copy, credentials_pointer_arg_destroy,
    credentials_pointer_cmp***REMOVED***;

grpc_arg grpc_channel_credentials_to_arg(
    grpc_channel_credentials *credentials) ***REMOVED***
  return grpc_channel_arg_pointer_create(
      GRPC_ARG_CHANNEL_CREDENTIALS, credentials, &credentials_pointer_vtable);
***REMOVED***

grpc_channel_credentials *grpc_channel_credentials_from_arg(
    const grpc_arg *arg) ***REMOVED***
  if (strcmp(arg->key, GRPC_ARG_CHANNEL_CREDENTIALS)) return NULL;
  if (arg->type != GRPC_ARG_POINTER) ***REMOVED***
    gpr_log(GPR_ERROR, "Invalid type %d for arg %s", arg->type,
            GRPC_ARG_CHANNEL_CREDENTIALS);
    return NULL;
  ***REMOVED***
  return arg->value.pointer.p;
***REMOVED***

grpc_channel_credentials *grpc_channel_credentials_find_in_args(
    const grpc_channel_args *args) ***REMOVED***
  size_t i;
  if (args == NULL) return NULL;
  for (i = 0; i < args->num_args; i++) ***REMOVED***
    grpc_channel_credentials *credentials =
        grpc_channel_credentials_from_arg(&args->args[i]);
    if (credentials != NULL) return credentials;
  ***REMOVED***
  return NULL;
***REMOVED***

grpc_server_credentials *grpc_server_credentials_ref(
    grpc_server_credentials *creds) ***REMOVED***
  if (creds == NULL) return NULL;
  gpr_ref(&creds->refcount);
  return creds;
***REMOVED***

void grpc_server_credentials_unref(grpc_exec_ctx *exec_ctx,
                                   grpc_server_credentials *creds) ***REMOVED***
  if (creds == NULL) return;
  if (gpr_unref(&creds->refcount)) ***REMOVED***
    if (creds->vtable->destruct != NULL) ***REMOVED***
      creds->vtable->destruct(exec_ctx, creds);
    ***REMOVED***
    if (creds->processor.destroy != NULL && creds->processor.state != NULL) ***REMOVED***
      creds->processor.destroy(creds->processor.state);
    ***REMOVED***
    gpr_free(creds);
  ***REMOVED***
***REMOVED***

void grpc_server_credentials_release(grpc_server_credentials *creds) ***REMOVED***
  GRPC_API_TRACE("grpc_server_credentials_release(creds=%p)", 1, (creds));
  grpc_exec_ctx exec_ctx = GRPC_EXEC_CTX_INIT;
  grpc_server_credentials_unref(&exec_ctx, creds);
  grpc_exec_ctx_finish(&exec_ctx);
***REMOVED***

grpc_security_status grpc_server_credentials_create_security_connector(
    grpc_exec_ctx *exec_ctx, grpc_server_credentials *creds,
    grpc_server_security_connector **sc) ***REMOVED***
  if (creds == NULL || creds->vtable->create_security_connector == NULL) ***REMOVED***
    gpr_log(GPR_ERROR, "Server credentials cannot create security context.");
    return GRPC_SECURITY_ERROR;
  ***REMOVED***
  return creds->vtable->create_security_connector(exec_ctx, creds, sc);
***REMOVED***

void grpc_server_credentials_set_auth_metadata_processor(
    grpc_server_credentials *creds, grpc_auth_metadata_processor processor) ***REMOVED***
  GRPC_API_TRACE(
      "grpc_server_credentials_set_auth_metadata_processor("
      "creds=%p, "
      "processor=grpc_auth_metadata_processor ***REMOVED*** process: %p, state: %p ***REMOVED***)",
      3, (creds, (void *)(intptr_t)processor.process, processor.state));
  if (creds == NULL) return;
  if (creds->processor.destroy != NULL && creds->processor.state != NULL) ***REMOVED***
    creds->processor.destroy(creds->processor.state);
  ***REMOVED***
  creds->processor = processor;
***REMOVED***

static void server_credentials_pointer_arg_destroy(grpc_exec_ctx *exec_ctx,
                                                   void *p) ***REMOVED***
  grpc_server_credentials_unref(exec_ctx, p);
***REMOVED***

static void *server_credentials_pointer_arg_copy(void *p) ***REMOVED***
  return grpc_server_credentials_ref(p);
***REMOVED***

static int server_credentials_pointer_cmp(void *a, void *b) ***REMOVED***
  return GPR_ICMP(a, b);
***REMOVED***

static const grpc_arg_pointer_vtable cred_ptr_vtable = ***REMOVED***
    server_credentials_pointer_arg_copy, server_credentials_pointer_arg_destroy,
    server_credentials_pointer_cmp***REMOVED***;

grpc_arg grpc_server_credentials_to_arg(grpc_server_credentials *p) ***REMOVED***
  return grpc_channel_arg_pointer_create(GRPC_SERVER_CREDENTIALS_ARG, p,
                                         &cred_ptr_vtable);
***REMOVED***

grpc_server_credentials *grpc_server_credentials_from_arg(const grpc_arg *arg) ***REMOVED***
  if (strcmp(arg->key, GRPC_SERVER_CREDENTIALS_ARG) != 0) return NULL;
  if (arg->type != GRPC_ARG_POINTER) ***REMOVED***
    gpr_log(GPR_ERROR, "Invalid type %d for arg %s", arg->type,
            GRPC_SERVER_CREDENTIALS_ARG);
    return NULL;
  ***REMOVED***
  return arg->value.pointer.p;
***REMOVED***

grpc_server_credentials *grpc_find_server_credentials_in_args(
    const grpc_channel_args *args) ***REMOVED***
  size_t i;
  if (args == NULL) return NULL;
  for (i = 0; i < args->num_args; i++) ***REMOVED***
    grpc_server_credentials *p =
        grpc_server_credentials_from_arg(&args->args[i]);
    if (p != NULL) return p;
  ***REMOVED***
  return NULL;
***REMOVED***
