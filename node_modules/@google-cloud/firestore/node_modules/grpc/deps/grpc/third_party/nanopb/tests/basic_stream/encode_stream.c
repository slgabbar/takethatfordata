/* Same as test_encode1.c, except writes directly to stdout.
 */

#include <stdio.h>
#include <pb_encode.h>
#include "person.pb.h"
#include "test_helpers.h"

/* This binds the pb_ostream_t into the stdout stream */
bool streamcallback(pb_ostream_t *stream, const uint8_t *buf, size_t count)
***REMOVED***
    FILE *file = (FILE*) stream->state;
    return fwrite(buf, 1, count, file) == count;
***REMOVED***

int main()
***REMOVED***
    /* Initialize the structure with constants */
    Person person = ***REMOVED***"Test Person 99", 99, true, "test@person.com",
        3, ***REMOVED******REMOVED***"555-12345678", true, Person_PhoneType_MOBILE***REMOVED***,
            ***REMOVED***"99-2342", false, 0***REMOVED***,
            ***REMOVED***"1234-5678", true, Person_PhoneType_WORK***REMOVED***,
        ***REMOVED******REMOVED***;
    
    /* Prepare the stream, output goes directly to stdout */
    pb_ostream_t stream = ***REMOVED***&streamcallback, NULL, SIZE_MAX, 0***REMOVED***;
    stream.state = stdout;
    SET_BINARY_MODE(stdout);
    
    /* Now encode it and check if we succeeded. */
    if (pb_encode(&stream, Person_fields, &person))
    ***REMOVED***
        return 0; /* Success */
    ***REMOVED***
    else
    ***REMOVED***
        fprintf(stderr, "Encoding failed: %s\n", PB_GET_ERROR(&stream));
        return 1; /* Failure */
    ***REMOVED***
***REMOVED***
