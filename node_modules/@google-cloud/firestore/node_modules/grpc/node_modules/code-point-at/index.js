/* eslint-disable babel/new-cap, xo/throw-new-error */
'use strict';
module.exports = function (str, pos) ***REMOVED***
	if (str === null || str === undefined) ***REMOVED***
		throw TypeError();
	***REMOVED***

	str = String(str);

	var size = str.length;
	var i = pos ? Number(pos) : 0;

	if (Number.isNaN(i)) ***REMOVED***
		i = 0;
	***REMOVED***

	if (i < 0 || i >= size) ***REMOVED***
		return undefined;
	***REMOVED***

	var first = str.charCodeAt(i);

	if (first >= 0xD800 && first <= 0xDBFF && size > i + 1) ***REMOVED***
		var second = str.charCodeAt(i + 1);

		if (second >= 0xDC00 && second <= 0xDFFF) ***REMOVED***
			return ((first - 0xD800) * 0x400) + second - 0xDC00 + 0x10000;
		***REMOVED***
	***REMOVED***

	return first;
***REMOVED***;
