var parse = require('../');
var test = require('tape');

test('stops parsing on the first non-option when stopEarly is set', function (t) ***REMOVED***
    var argv = parse(['--aaa', 'bbb', 'ccc', '--ddd'], ***REMOVED***
        stopEarly: true
    ***REMOVED***);

    t.deepEqual(argv, ***REMOVED***
        aaa: 'bbb',
        _: ['ccc', '--ddd']
    ***REMOVED***);

    t.end();
***REMOVED***);
