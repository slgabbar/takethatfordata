var tar = require("../tar.js")
  , fstream = require("fstream")
  , fs = require("fs")

var dirDest = fs.createWriteStream('dir.tar')


function onError(err) ***REMOVED***
  console.error('An error occurred:', err)
***REMOVED***

function onEnd() ***REMOVED***
  console.log('Packed!')
***REMOVED***

var packer = tar.Pack(***REMOVED*** noProprietary: true ***REMOVED***)
  .on('error', onError)
  .on('end', onEnd);

// This must be a "directory"
fstream.Reader(***REMOVED*** path: __dirname, type: "Directory" ***REMOVED***)
  .on('error', onError)
  .pipe(packer)
  .pipe(dirDest)
