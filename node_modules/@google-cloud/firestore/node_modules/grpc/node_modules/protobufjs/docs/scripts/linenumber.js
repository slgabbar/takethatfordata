/*global document */
(function() ***REMOVED***
    var source = document.getElementsByClassName('prettyprint source linenums');
    var i = 0;
    var lineNumber = 0;
    var lineId;
    var lines;
    var totalLines;
    var anchorHash;

    if (source && source[0]) ***REMOVED***
        anchorHash = document.location.hash.substring(1);
        lines = source[0].getElementsByTagName('li');
        totalLines = lines.length;

        for (; i < totalLines; i++) ***REMOVED***
            lineNumber++;
            lineId = 'line' + lineNumber;
            lines[i].id = lineId;
            if (lineId === anchorHash) ***REMOVED***
                lines[i].className += ' selected';
            ***REMOVED***
        ***REMOVED***
    ***REMOVED***
***REMOVED***)();
