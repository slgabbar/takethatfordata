"use strict";

module.exports = exports = unpublish;

exports.usage = 'Lists all published binaries (requires aws-sdk)';

var fs = require('fs');
var log = require('npmlog');
var versioning = require('./util/versioning.js');
var s3_setup = require('./util/s3_setup.js');
var config = require('rc')("node_pre_gyp",***REMOVED***acl:"public-read"***REMOVED***);

function unpublish(gyp, argv, callback) ***REMOVED***
    var AWS = require("aws-sdk");
    var package_json = JSON.parse(fs.readFileSync('./package.json'));
    var opts = versioning.evaluate(package_json, gyp.opts);
    s3_setup.detect(opts.hosted_path,config);
    AWS.config.update(config);
    var s3 =  new AWS.S3();
    var s3_opts = ***REMOVED***  Bucket: config.bucket,
                     Prefix: config.prefix
                  ***REMOVED***;
    s3.listObjects(s3_opts, function(err, meta)***REMOVED***
        if (err && err.code == 'NotFound') ***REMOVED***
            return callback(new Error('['+package_json.name+'] Not found: https://' + s3_opts.Bucket + '.s3.amazonaws.com/'+config.prefix));
        ***REMOVED*** else if(err) ***REMOVED***
            return callback(err);
        ***REMOVED*** else ***REMOVED***
            log.verbose(JSON.stringify(meta,null,1));
            if (meta && meta.Contents) ***REMOVED***
                meta.Contents.forEach(function(obj) ***REMOVED***
                    console.log(obj.Key);
                ***REMOVED***);
            ***REMOVED*** else ***REMOVED***
                console.error('['+package_json.name+'] No objects found at https://' + s3_opts.Bucket + '.s3.amazonaws.com/'+config.prefix );
            ***REMOVED***
            return callback();
        ***REMOVED***
    ***REMOVED***);
***REMOVED***
