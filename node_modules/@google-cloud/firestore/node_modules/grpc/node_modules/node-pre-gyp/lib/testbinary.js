"use strict";

module.exports = exports = testbinary;

exports.usage = 'Tests that the binary.node can be required';

var fs = require('fs');
var path = require('path');
var log = require('npmlog');
var cp = require('child_process');
var versioning = require('./util/versioning.js');
var path = require('path');

function testbinary(gyp, argv, callback) ***REMOVED***
    var args = [];
    var options = ***REMOVED******REMOVED***;
    var shell_cmd = process.execPath;
    var package_json = JSON.parse(fs.readFileSync('./package.json'));
    var opts = versioning.evaluate(package_json, gyp.opts);
    // skip validation for runtimes we don't explicitly support (like electron)
    if (opts.runtime &&
        opts.runtime !== 'node-webkit' &&
        opts.runtime !== 'node') ***REMOVED***
        return callback();
    ***REMOVED***
    var nw = (opts.runtime && opts.runtime === 'node-webkit');
    // ensure on windows that / are used for require path
    var binary_module = opts.module.replace(/\\/g, '/');
    if ((process.arch != opts.target_arch) ||
        (process.platform != opts.target_platform)) ***REMOVED***
        var msg = "skipping validation since host platform/arch (";
        msg += process.platform+'/'+process.arch+")";
        msg += " does not match target (";
        msg += opts.target_platform+'/'+opts.target_arch+")";
        log.info('validate', msg);
        return callback();
    ***REMOVED***
    if (nw) ***REMOVED***
        options.timeout = 5000;
        if (process.platform === 'darwin') ***REMOVED***
            shell_cmd = 'node-webkit';
        ***REMOVED*** else if (process.platform === 'win32') ***REMOVED***
            shell_cmd = 'nw.exe';
        ***REMOVED*** else ***REMOVED***
            shell_cmd = 'nw';
        ***REMOVED***
        var modulePath = path.resolve(binary_module);
        var appDir = path.join(__dirname, 'util', 'nw-pre-gyp');
        args.push(appDir);
        args.push(modulePath);
        log.info("validate","Running test command: '" + shell_cmd + ' ' + args.join(' ') + "'");
        cp.execFile(shell_cmd, args, options, function(err, stdout, stderr) ***REMOVED***
            // check for normal timeout for node-webkit
            if (err) ***REMOVED***
                if (err.killed === true && err.signal && err.signal.indexOf('SIG') > -1) ***REMOVED***
                    return callback();
                ***REMOVED***
                var stderrLog = stderr.toString();
                log.info('stderr', stderrLog);
                if( /^\s*Xlib:\s*extension\s*"RANDR"\s*missing\s*on\s*display\s*":\d+\.\d+"\.\s*$/.test(stderrLog) )***REMOVED***
                    log.info('RANDR', 'stderr contains only RANDR error, ignored');
                    return callback();
                ***REMOVED***
                return callback(err);
            ***REMOVED***
            return callback();
        ***REMOVED***);
        return;
    ***REMOVED***
    args.push('--eval');
    args.push("require('" + binary_module.replace(/'/g, '\'') +"')");
    log.info("validate","Running test command: '" + shell_cmd + ' ' + args.join(' ') + "'");
    cp.execFile(shell_cmd, args, options, function(err, stdout, stderr) ***REMOVED***
        if (err) ***REMOVED***
            return callback(err, ***REMOVED*** stdout:stdout, stderr:stderr***REMOVED***);
        ***REMOVED***
        return callback();
    ***REMOVED***);
***REMOVED***
