#!/usr/bin/env node

var rimraf = require('./')

var help = false
var dashdash = false
var noglob = false
var args = process.argv.slice(2).filter(function(arg) ***REMOVED***
  if (dashdash)
    return !!arg
  else if (arg === '--')
    dashdash = true
  else if (arg === '--no-glob' || arg === '-G')
    noglob = true
  else if (arg === '--glob' || arg === '-g')
    noglob = false
  else if (arg.match(/^(-+|\/)(h(elp)?|\?)$/))
    help = true
  else
    return !!arg
***REMOVED***)

if (help || args.length === 0) ***REMOVED***
  // If they didn't ask for help, then this is not a "success"
  var log = help ? console.log : console.error
  log('Usage: rimraf <path> [<path> ...]')
  log('')
  log('  Deletes all files and folders at "path" recursively.')
  log('')
  log('Options:')
  log('')
  log('  -h, --help     Display this usage info')
  log('  -G, --no-glob  Do not expand glob patterns in arguments')
  log('  -g, --glob     Expand glob patterns in arguments (default)')
  process.exit(help ? 0 : 1)
***REMOVED*** else
  go(0)

function go (n) ***REMOVED***
  if (n >= args.length)
    return
  var options = ***REMOVED******REMOVED***
  if (noglob)
    options = ***REMOVED*** glob: false ***REMOVED***
  rimraf(args[n], options, function (er) ***REMOVED***
    if (er)
      throw er
    go(n+1)
  ***REMOVED***)
***REMOVED***
