'use strict';

var ruleModules = require('./_rules')
  , toHash = require('./util').toHash;

module.exports = function rules() ***REMOVED***
  var RULES = [
    ***REMOVED*** type: 'number',
      rules: [ 'maximum', 'minimum', 'multipleOf'] ***REMOVED***,
    ***REMOVED*** type: 'string',
      rules: [ 'maxLength', 'minLength', 'pattern', 'format' ] ***REMOVED***,
    ***REMOVED*** type: 'array',
      rules: [ 'maxItems', 'minItems', 'uniqueItems', 'items' ] ***REMOVED***,
    ***REMOVED*** type: 'object',
      rules: [ 'maxProperties', 'minProperties', 'required', 'dependencies', 'properties' ] ***REMOVED***,
    ***REMOVED*** rules: [ '$ref', 'enum', 'not', 'anyOf', 'oneOf', 'allOf' ] ***REMOVED***
  ];

  var ALL = [ 'type', 'additionalProperties', 'patternProperties' ];
  var KEYWORDS = [ 'additionalItems', '$schema', 'id', 'title', 'description', 'default' ];
  var TYPES = [ 'number', 'integer', 'string', 'array', 'object', 'boolean', 'null' ];
  RULES.all = toHash(ALL);

  RULES.forEach(function (group) ***REMOVED***
    group.rules = group.rules.map(function (keyword) ***REMOVED***
      ALL.push(keyword);
      var rule = RULES.all[keyword] = ***REMOVED***
        keyword: keyword,
        code: ruleModules[keyword]
      ***REMOVED***;
      return rule;
    ***REMOVED***);
  ***REMOVED***);

  RULES.keywords = toHash(ALL.concat(KEYWORDS));
  RULES.types = toHash(TYPES);
  RULES.custom = ***REMOVED******REMOVED***;

  return RULES;
***REMOVED***;
