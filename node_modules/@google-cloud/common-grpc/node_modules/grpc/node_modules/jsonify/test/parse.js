var test = require('tap').test;
var json = require('../');
var garbage = require('garbage');

test('parse', function (t) ***REMOVED***
    for (var i = 0; i < 50; i++) ***REMOVED***
        var s = JSON.stringify(garbage(50));
        
        t.deepEqual(
            json.parse(s),
            JSON.parse(s)
        );
    ***REMOVED***
    
    t.end();
***REMOVED***);
