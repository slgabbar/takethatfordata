'use strict'
var validate = require('aproba')
var renderTemplate = require('./render-template.js')
var wideTruncate = require('./wide-truncate')
var stringWidth = require('string-width')

module.exports = function (theme, width, completed) ***REMOVED***
  validate('ONN', [theme, width, completed])
  if (completed < 0) completed = 0
  if (completed > 1) completed = 1
  if (width <= 0) return ''
  var sofar = Math.round(width * completed)
  var rest = width - sofar
  var template = [
    ***REMOVED***type: 'complete', value: repeat(theme.complete, sofar), length: sofar***REMOVED***,
    ***REMOVED***type: 'remaining', value: repeat(theme.remaining, rest), length: rest***REMOVED***
  ]
  return renderTemplate(width, template, theme)
***REMOVED***

// lodash's way of repeating
function repeat (string, width) ***REMOVED***
  var result = ''
  var n = width
  do ***REMOVED***
    if (n % 2) ***REMOVED***
      result += string
    ***REMOVED***
    n = Math.floor(n / 2)
    /*eslint no-self-assign: 0*/
    string += string
  ***REMOVED*** while (n && stringWidth(result) < width)

  return wideTruncate(result, width)
***REMOVED***
