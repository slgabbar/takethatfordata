/*
 *
 * Copyright 2015 gRPC authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 */

#include "src/core/lib/debug/trace.h"

#include <string.h>

#include <grpc/support/alloc.h>
#include <grpc/support/log.h>
#include "src/core/lib/support/env.h"

int grpc_tracer_set_enabled(const char *name, int enabled);

typedef struct tracer ***REMOVED***
  grpc_tracer_flag *flag;
  struct tracer *next;
***REMOVED*** tracer;
static tracer *tracers;

#ifdef GRPC_THREADSAFE_TRACER
#define TRACER_SET(flag, on) gpr_atm_no_barrier_store(&(flag).value, (on))
#else
#define TRACER_SET(flag, on) (flag).value = (on)
#endif

void grpc_register_tracer(grpc_tracer_flag *flag) ***REMOVED***
  tracer *t = (tracer *)gpr_malloc(sizeof(*t));
  t->flag = flag;
  t->next = tracers;
  TRACER_SET(*flag, false);
  tracers = t;
***REMOVED***

static void add(const char *beg, const char *end, char ***ss, size_t *ns) ***REMOVED***
  size_t n = *ns;
  size_t np = n + 1;
  char *s;
  size_t len;
  GPR_ASSERT(end >= beg);
  len = (size_t)(end - beg);
  s = (char *)gpr_malloc(len + 1);
  memcpy(s, beg, len);
  s[len] = 0;
  *ss = (char **)gpr_realloc(*ss, sizeof(char **) * np);
  (*ss)[n] = s;
  *ns = np;
***REMOVED***

static void split(const char *s, char ***ss, size_t *ns) ***REMOVED***
  const char *c = strchr(s, ',');
  if (c == NULL) ***REMOVED***
    add(s, s + strlen(s), ss, ns);
  ***REMOVED*** else ***REMOVED***
    add(s, c, ss, ns);
    split(c + 1, ss, ns);
  ***REMOVED***
***REMOVED***

static void parse(const char *s) ***REMOVED***
  char **strings = NULL;
  size_t nstrings = 0;
  size_t i;
  split(s, &strings, &nstrings);

  for (i = 0; i < nstrings; i++) ***REMOVED***
    if (strings[i][0] == '-') ***REMOVED***
      grpc_tracer_set_enabled(strings[i] + 1, 0);
    ***REMOVED*** else ***REMOVED***
      grpc_tracer_set_enabled(strings[i], 1);
    ***REMOVED***
  ***REMOVED***

  for (i = 0; i < nstrings; i++) ***REMOVED***
    gpr_free(strings[i]);
  ***REMOVED***
  gpr_free(strings);
***REMOVED***

static void list_tracers() ***REMOVED***
  gpr_log(GPR_DEBUG, "available tracers:");
  tracer *t;
  for (t = tracers; t; t = t->next) ***REMOVED***
    gpr_log(GPR_DEBUG, "\t%s", t->flag->name);
  ***REMOVED***
***REMOVED***

void grpc_tracer_init(const char *env_var) ***REMOVED***
  char *e = gpr_getenv(env_var);
  if (e != NULL) ***REMOVED***
    parse(e);
    gpr_free(e);
  ***REMOVED***
***REMOVED***

void grpc_tracer_shutdown(void) ***REMOVED***
  while (tracers) ***REMOVED***
    tracer *t = tracers;
    tracers = t->next;
    gpr_free(t);
  ***REMOVED***
***REMOVED***

int grpc_tracer_set_enabled(const char *name, int enabled) ***REMOVED***
  tracer *t;
  if (0 == strcmp(name, "all")) ***REMOVED***
    for (t = tracers; t; t = t->next) ***REMOVED***
      TRACER_SET(*t->flag, enabled);
    ***REMOVED***
  ***REMOVED*** else if (0 == strcmp(name, "list_tracers")) ***REMOVED***
    list_tracers();
  ***REMOVED*** else if (0 == strcmp(name, "refcount")) ***REMOVED***
    for (t = tracers; t; t = t->next) ***REMOVED***
      if (strstr(t->flag->name, "refcount") != NULL) ***REMOVED***
        TRACER_SET(*t->flag, enabled);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    int found = 0;
    for (t = tracers; t; t = t->next) ***REMOVED***
      if (0 == strcmp(name, t->flag->name)) ***REMOVED***
        TRACER_SET(*t->flag, enabled);
        found = 1;
      ***REMOVED***
    ***REMOVED***
    if (!found) ***REMOVED***
      gpr_log(GPR_ERROR, "Unknown trace var: '%s'", name);
      return 0; /* early return */
    ***REMOVED***
  ***REMOVED***
  return 1;
***REMOVED***
