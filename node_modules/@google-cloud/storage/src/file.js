/*!
 * Copyright 2014 Google Inc. All Rights Reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

'use strict';

var Buffer = require('safe-buffer').Buffer;
var common = require('@google-cloud/common');
var concat = require('concat-stream');
var createErrorClass = require('create-error-class');
var crypto = require('crypto');
var duplexify = require('duplexify');
var extend = require('extend');
var format = require('string-format-obj');
var fs = require('fs');
var hashStreamValidation = require('hash-stream-validation');
var is = require('is');
var once = require('once');
var pumpify = require('pumpify');
var resumableUpload = require('gcs-resumable-upload');
var streamEvents = require('stream-events');
var through = require('through2');
var util = require('util');
var zlib = require('zlib');

var Acl = require('./acl.js');

/**
 * Custom error type for errors related to getting signed errors and policies.
 *
 * @private
 *
 * @param ***REMOVED***string***REMOVED*** message - Custom error message.
 * @returns ***REMOVED***Error***REMOVED***
 */
var SigningError = createErrorClass('SigningError', function(message) ***REMOVED***
  this.message = message;
***REMOVED***);

/**
 * @const ***REMOVED***string***REMOVED***
 * @private
 */
var STORAGE_DOWNLOAD_BASE_URL = 'https://storage.googleapis.com';

/**
 * @const ***REMOVED***string***REMOVED***
 * @private
 */
var STORAGE_UPLOAD_BASE_URL = 'https://www.googleapis.com/upload/storage/v1/b';

/**
 * @const ***REMOVED***RegExp***REMOVED***
 * @private
 */
var GS_URL_REGEXP = /^gs:\/\/([a-z0-9_.-]+)\/(.+)$/;

/**
 * A File object is created from your ***REMOVED***@link Bucket***REMOVED*** object using
 * ***REMOVED***@link Bucket#file***REMOVED***.
 *
 * @class
 * @param ***REMOVED***Bucket***REMOVED*** bucket The Bucket instance this file is
 *     attached to.
 * @param ***REMOVED***string***REMOVED*** name The name of the remote file.
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***string***REMOVED*** [options.encryptionKey] A custom encryption key.
 * @param ***REMOVED***number***REMOVED*** [options.generation] Generation to scope the file to.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for all requests made from File object.
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 */
function File(bucket, name, options) ***REMOVED***
  options = options || ***REMOVED******REMOVED***;

  this.bucket = bucket;
  this.storage = bucket.parent;
  this.userProject = options.userProject || bucket.userProject;

  Object.defineProperty(this, 'name', ***REMOVED***
    enumerable: true,
    value: name.replace(/^\/+/, ''), // Remove leading slashes.
  ***REMOVED***);

  var generation = parseInt(options.generation, 10);

  if (!isNaN(generation)) ***REMOVED***
    this.generation = generation;
    this.requestQueryObject = ***REMOVED***
      generation: this.generation,
    ***REMOVED***;
  ***REMOVED***

  common.ServiceObject.call(this, ***REMOVED***
    parent: bucket,
    baseUrl: '/o',
    id: encodeURIComponent(this.name),
  ***REMOVED***);

  if (options.encryptionKey) ***REMOVED***
    this.setEncryptionKey(options.encryptionKey);
  ***REMOVED***

  /**
   * Cloud Storage uses access control lists (ACLs) to manage object and
   * bucket access. ACLs are the mechanism you use to share objects with other
   * users and allow other users to access your buckets and objects.
   *
   * An ACL consists of one or more entries, where each entry grants permissions
   * to an entity. Permissions define the actions that can be performed against
   * an object or bucket (for example, `READ` or `WRITE`); the entity defines
   * who the permission applies to (for example, a specific user or group of
   * users).
   *
   * The `acl` object on a File instance provides methods to get you a list of
   * the ACLs defined on your bucket, as well as set, update, and delete them.
   *
   * @see [About Access Control lists]***REMOVED***@link http://goo.gl/6qBBPO***REMOVED***
   *
   * @name File#acl
   * @mixes Acl
   *
   * @example
   * var storage = require('@google-cloud/storage')();
   * var myBucket = storage.bucket('my-bucket');
   *
   * var file = myBucket.file('my-file');
   * //-
   * // Make a file publicly readable.
   * //-
   * var options = ***REMOVED***
   *   entity: 'allUsers',
   *   role: storage.acl.READER_ROLE
   * ***REMOVED***;
   *
   * file.acl.add(options, function(err, aclObject) ***REMOVED******REMOVED***);
   *
   * //-
   * // If the callback is omitted, we'll return a Promise.
   * //-
   * file.acl.add(options).then(function(data) ***REMOVED***
   *   var aclObject = data[0];
   *   var apiResponse = data[1];
   * ***REMOVED***);
   */
  this.acl = new Acl(***REMOVED***
    request: this.request.bind(this),
    pathPrefix: '/acl',
  ***REMOVED***);
***REMOVED***

util.inherits(File, common.ServiceObject);

/**
 * @typedef ***REMOVED***array***REMOVED*** CopyResponse
 * @property ***REMOVED***File***REMOVED*** 0 The copied ***REMOVED***@link File***REMOVED***.
 * @property ***REMOVED***object***REMOVED*** 1 The full API response.
 */
/**
 * @callback CopyCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***File***REMOVED*** copiedFile The copied ***REMOVED***@link File***REMOVED***.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Copy this file to another file. By default, this will copy the file to the
 * same bucket, but you can choose to copy it to another Bucket by providing
 * a Bucket or File object or a URL starting with "gs://".
 *
 * @see [Objects: copy API Documentation]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/objects/copy***REMOVED***
 *
 * @throws ***REMOVED***Error***REMOVED*** If the destination file is not provided.
 *
 * @param ***REMOVED***string|Bucket|File***REMOVED*** destination Destination file.
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options. See an
 *     [Object resource](https://cloud.google.com/storage/docs/json_api/v1/objects#resource).
 * @param ***REMOVED***string***REMOVED*** [options.token] A previously-returned `rewriteToken` from an
 *     unfinished rewrite request.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***CopyCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<CopyResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 *
 * //-
 * // You can pass in a variety of types for the destination.
 * //
 * // For all of the below examples, assume we are working with the following
 * // Bucket and File objects.
 * //-
 * var bucket = storage.bucket('my-bucket');
 * var file = bucket.file('my-image.png');
 *
 * //-
 * // If you pass in a string for the destination, the file is copied to its
 * // current bucket, under the new name provided.
 * //-
 * file.copy('my-image-copy.png', function(err, copiedFile, apiResponse) ***REMOVED***
 *   // `my-bucket` now contains:
 *   // - "my-image.png"
 *   // - "my-image-copy.png"
 *
 *   // `copiedFile` is an instance of a File object that refers to your new
 *   // file.
 * ***REMOVED***);
 *
 * //-
 * // If you pass in a string starting with "gs://" for the destination, the
 * // file is copied to the other bucket and under the new name provided.
 * //-
 * var newLocation = 'gs://another-bucket/my-image-copy.png';
 * file.copy(newLocation, function(err, copiedFile, apiResponse) ***REMOVED***
 *   // `my-bucket` still contains:
 *   // - "my-image.png"
 *   //
 *   // `another-bucket` now contains:
 *   // - "my-image-copy.png"
 *
 *   // `copiedFile` is an instance of a File object that refers to your new
 *   // file.
 * ***REMOVED***);
 *
 * //-
 * // If you pass in a Bucket object, the file will be copied to that bucket
 * // using the same name.
 * //-
 * var anotherBucket = storage.bucket('another-bucket');
 * file.copy(anotherBucket, function(err, copiedFile, apiResponse) ***REMOVED***
 *   // `my-bucket` still contains:
 *   // - "my-image.png"
 *   //
 *   // `another-bucket` now contains:
 *   // - "my-image.png"
 *
 *   // `copiedFile` is an instance of a File object that refers to your new
 *   // file.
 * ***REMOVED***);
 *
 * //-
 * // If you pass in a File object, you have complete control over the new
 * // bucket and filename.
 * //-
 * var anotherFile = anotherBucket.file('my-awesome-image.png');
 * file.copy(anotherFile, function(err, copiedFile, apiResponse) ***REMOVED***
 *   // `my-bucket` still contains:
 *   // - "my-image.png"
 *   //
 *   // `another-bucket` now contains:
 *   // - "my-awesome-image.png"
 *
 *   // Note:
 *   // The `copiedFile` parameter is equal to `anotherFile`.
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.copy(newLocation).then(function(data) ***REMOVED***
 *   var newFile = data[0];
 *   var apiResponse = data[1];
 * ***REMOVED***);
 *
 * @example <caption>include:samples/files.js</caption>
 * region_tag:storage_copy_file
 * Another example:
 */
File.prototype.copy = function(destination, options, callback) ***REMOVED***
  var self = this;

  var noDestinationError = new Error('Destination file should have a name.');

  if (!destination) ***REMOVED***
    throw noDestinationError;
  ***REMOVED***

  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  options = extend(true, ***REMOVED******REMOVED***, options);
  callback = callback || common.util.noop;

  var destBucket;
  var destName;
  var newFile;

  if (is.string(destination)) ***REMOVED***
    var parsedDestination = GS_URL_REGEXP.exec(destination);
    if (parsedDestination !== null && parsedDestination.length === 3) ***REMOVED***
      destBucket = this.storage.bucket(parsedDestination[1]);
      destName = parsedDestination[2];
    ***REMOVED*** else ***REMOVED***
      destBucket = this.bucket;
      destName = destination;
    ***REMOVED***
  ***REMOVED*** else if (
    destination.constructor &&
    destination.constructor.name === 'Bucket'
  ) ***REMOVED***
    destBucket = destination;
    destName = this.name;
  ***REMOVED*** else if (destination instanceof File) ***REMOVED***
    destBucket = destination.bucket;
    destName = destination.name;
    newFile = destination;
  ***REMOVED*** else ***REMOVED***
    throw noDestinationError;
  ***REMOVED***

  var query = ***REMOVED******REMOVED***;
  if (is.defined(this.generation)) ***REMOVED***
    query.sourceGeneration = this.generation;
  ***REMOVED***
  if (is.defined(options.token)) ***REMOVED***
    query.rewriteToken = options.token;
  ***REMOVED***
  if (is.defined(options.userProject)) ***REMOVED***
    query.userProject = options.userProject;
    delete options.userProject;
  ***REMOVED***

  newFile = newFile || destBucket.file(destName);

  var headers = ***REMOVED******REMOVED***;
  if (is.defined(newFile.encryptionKey)) ***REMOVED***
    headers['x-goog-copy-source-encryption-algorithm'] = 'AES256';
    headers['x-goog-copy-source-encryption-key'] = this.encryptionKeyBase64;
    headers[
      'x-goog-copy-source-encryption-key-sha256'
    ] = this.encryptionKeyHash;

    this.setEncryptionKey(newFile.encryptionKey);
  ***REMOVED***

  this.request(
    ***REMOVED***
      method: 'POST',
      uri: format('/rewriteTo/b/***REMOVED***bucketName***REMOVED***/o/***REMOVED***fileName***REMOVED***', ***REMOVED***
        bucketName: destBucket.name,
        fileName: encodeURIComponent(destName),
      ***REMOVED***),
      qs: query,
      json: options,
      headers: headers,
    ***REMOVED***,
    function(err, resp) ***REMOVED***
      if (err) ***REMOVED***
        callback(err, null, resp);
        return;
      ***REMOVED***

      if (resp.rewriteToken) ***REMOVED***
        var options = ***REMOVED***
          token: resp.rewriteToken,
        ***REMOVED***;

        if (query.userProject) ***REMOVED***
          options.userProject = query.userProject;
        ***REMOVED***

        self.copy(newFile, options, callback);
        return;
      ***REMOVED***

      callback(null, newFile, resp);
    ***REMOVED***
  );
***REMOVED***;

/**
 * Create a readable stream to read the contents of the remote file. It can be
 * piped to a writable stream or listened to for 'data' events to read a file's
 * contents.
 *
 * In the unlikely event there is a mismatch between what you downloaded and the
 * version in your Bucket, your error handler will receive an error with code
 * "CONTENT_DOWNLOAD_MISMATCH". If you receive this error, the best recourse is
 * to try downloading the file again.
 *
 * For faster crc32c computation, you must manually install
 * [`fast-crc32c`](http://www.gitnpm.com/fast-crc32c):
 *
 *     $ npm install --save fast-crc32c
 *
 * NOTE: Readable streams will emit the `end` event when the file is fully
 * downloaded.
 *
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***string|boolean***REMOVED*** [options.validation] Possible values: `"md5"`,
 *     `"crc32c"`, or `false`. By default, data integrity is validated with a
 *     CRC32c checksum. You may use MD5 if preferred, but that hash is not
 *     supported for composite objects. An error will be raised if MD5 is
 *     specified but is not available. You may also choose to skip validation
 *     completely, however this is **not recommended**.
 * @param ***REMOVED***number***REMOVED*** [options.start] A byte offset to begin the file's download
 *     from. Default is 0. NOTE: Byte ranges are inclusive; that is,
 *     `options.start = 0` and `options.end = 999` represent the first 1000
 *     bytes in a file or object. NOTE: when specifying a byte range, data
 *     integrity is not available.
 * @param ***REMOVED***number***REMOVED*** [options.end] A byte offset to stop reading the file at.
 *     NOTE: Byte ranges are inclusive; that is, `options.start = 0` and
 *     `options.end = 999` represent the first 1000 bytes in a file or object.
 *     NOTE: when specifying a byte range, data integrity is not available.
 * @returns ***REMOVED***ReadableStream***REMOVED***
 *
 * @example
 * //-
 * // <h4>Downloading a File</h4>
 * //
 * // The example below demonstrates how we can reference a remote file, then
 * // pipe its contents to a local file. This is effectively creating a local
 * // backup of your remote data.
 * //-
 * var storage = require('@google-cloud/storage')();
 * var bucket = storage.bucket('my-bucket');
 *
 * var fs = require('fs');
 * var remoteFile = bucket.file('image.png');
 * var localFilename = '/Users/stephen/Photos/image.png';
 *
 * remoteFile.createReadStream()
 *   .on('error', function(err) ***REMOVED******REMOVED***)
 *   .on('response', function(response) ***REMOVED***
 *     // Server connected and responded with the specified status and headers.
 *    ***REMOVED***)
 *   .on('end', function() ***REMOVED***
 *     // The file is fully downloaded.
 *   ***REMOVED***)
 *   .pipe(fs.createWriteStream(localFilename));
 *
 * //-
 * // To limit the downloaded data to only a byte range, pass an options object.
 * //-
 * var logFile = myBucket.file('access_log');
 * logFile.createReadStream(***REMOVED***
 *     start: 10000,
 *     end: 20000
 *   ***REMOVED***)
 *   .on('error', function(err) ***REMOVED******REMOVED***)
 *   .pipe(fs.createWriteStream('/Users/stephen/logfile.txt'));
 *
 * //-
 * // To read a tail byte range, specify only `options.end` as a negative
 * // number.
 * //-
 * var logFile = myBucket.file('access_log');
 * logFile.createReadStream(***REMOVED***
 *     end: -100
 *   ***REMOVED***)
 *   .on('error', function(err) ***REMOVED******REMOVED***)
 *   .pipe(fs.createWriteStream('/Users/stephen/logfile.txt'));
 */
File.prototype.createReadStream = function(options) ***REMOVED***
  options = options || ***REMOVED******REMOVED***;

  var self = this;
  var rangeRequest = is.number(options.start) || is.number(options.end);
  var tailRequest = options.end < 0;

  var validateStream; // Created later, if necessary.
  var throughStream = streamEvents(through());

  var crc32c = true;
  var md5 = false;

  var refreshedMetadata = false;

  if (is.string(options.validation)) ***REMOVED***
    options.validation = options.validation.toLowerCase();
    crc32c = options.validation === 'crc32c';
    md5 = options.validation === 'md5';
  ***REMOVED*** else if (options.validation === false) ***REMOVED***
    crc32c = false;
  ***REMOVED***

  if (rangeRequest) ***REMOVED***
    if (is.string(options.validation) || options.validation === true) ***REMOVED***
      throw new Error('Cannot use validation with file ranges (start/end).');
    ***REMOVED***
    // Range requests can't receive data integrity checks.
    crc32c = false;
    md5 = false;
  ***REMOVED***

  // Authenticate the request, then pipe the remote API request to the stream
  // returned to the user.
  function makeRequest() ***REMOVED***
    var reqOpts = ***REMOVED***
      forever: false,
      uri: '',
      headers: ***REMOVED***
        'Accept-Encoding': 'gzip',
      ***REMOVED***,
      qs: ***REMOVED***
        alt: 'media',
      ***REMOVED***,
    ***REMOVED***;

    if (self.generation) ***REMOVED***
      reqOpts.qs.generation = self.generation;
    ***REMOVED***

    if (options.userProject) ***REMOVED***
      reqOpts.qs.userProject = options.userProject;
    ***REMOVED***

    if (rangeRequest) ***REMOVED***
      var start = is.number(options.start) ? options.start : '0';
      var end = is.number(options.end) ? options.end : '';

      reqOpts.headers.Range = `bytes=$***REMOVED***tailRequest ? end : `$***REMOVED***start***REMOVED***-$***REMOVED***end***REMOVED***`***REMOVED***`;
    ***REMOVED***

    self
      .requestStream(reqOpts)
      .on('error', err => ***REMOVED***
        throughStream.destroy(err);
      ***REMOVED***)
      .on('response', res => ***REMOVED***
        throughStream.emit('response', res);
        common.util.handleResp(null, res, null, onResponse);
      ***REMOVED***)
      .resume();

    // We listen to the response event from the request stream so that we can...
    //
    //   1) Intercept any data from going to the user if an error occurred.
    //   2) Calculate the hashes from the http.IncomingMessage response stream,
    //      which will return the bytes from the source without decompressing
    //      gzip'd content. We then send it through decompressed, if applicable,
    //      to the user.
    function onResponse(err, body, rawResponseStream) ***REMOVED***
      if (err) ***REMOVED***
        // Get error message from the body.
        rawResponseStream.pipe(
          concat(function(body) ***REMOVED***
            err.message = body.toString();
            throughStream.destroy(err);
          ***REMOVED***)
        );

        return;
      ***REMOVED***

      var headers = rawResponseStream.toJSON().headers;
      var isCompressed = headers['content-encoding'] === 'gzip';

      var shouldRunValidation = !rangeRequest && (crc32c || md5);

      var throughStreams = [];

      if (shouldRunValidation) ***REMOVED***
        validateStream = hashStreamValidation(***REMOVED***crc32c, md5***REMOVED***);
        throughStreams.push(validateStream);
      ***REMOVED***

      if (isCompressed) ***REMOVED***
        throughStreams.push(zlib.createGunzip());
      ***REMOVED***

      if (throughStreams.length === 1) ***REMOVED***
        rawResponseStream = rawResponseStream.pipe(throughStreams[0]);
      ***REMOVED*** else if (throughStreams.length > 1) ***REMOVED***
        rawResponseStream = rawResponseStream.pipe(pumpify.obj(throughStreams));
      ***REMOVED***

      rawResponseStream.on('end', onComplete).pipe(throughStream, ***REMOVED***end: false***REMOVED***);
    ***REMOVED***

    // This is hooked to the `complete` event from the request stream. This is
    // our chance to validate the data and let the user know if anything went
    // wrong.
    function onComplete(err) ***REMOVED***
      if (err) ***REMOVED***
        throughStream.destroy(err);
        return;
      ***REMOVED***

      if (rangeRequest) ***REMOVED***
        throughStream.end();
        return;
      ***REMOVED***

      if (!refreshedMetadata) ***REMOVED***
        refreshedMetadata = true;
        self.getMetadata(***REMOVED***userProject: options.userProject***REMOVED***, onComplete);
        return;
      ***REMOVED***

      var hashes = ***REMOVED***
        crc32c: self.metadata.crc32c,
        md5: self.metadata.md5Hash,
      ***REMOVED***;

      // If we're doing validation, assume the worst-- a data integrity
      // mismatch. If not, these tests won't be performed, and we can assume the
      // best.
      var failed = crc32c || md5;

      if (crc32c && hashes.crc32c) ***REMOVED***
        // We must remove the first four bytes from the returned checksum.
        // http://stackoverflow.com/questions/25096737/
        //   base64-encoding-of-crc32c-long-value
        failed = !validateStream.test('crc32c', hashes.crc32c.substr(4));
      ***REMOVED***

      if (md5 && hashes.md5) ***REMOVED***
        failed = !validateStream.test('md5', hashes.md5);
      ***REMOVED***

      if (md5 && !hashes.md5) ***REMOVED***
        var hashError = new Error(
          [
            'MD5 verification was specified, but is not available for the',
            'requested object. MD5 is not available for composite objects.',
          ].join(' ')
        );
        hashError.code = 'MD5_NOT_AVAILABLE';

        throughStream.destroy(hashError);
      ***REMOVED*** else if (failed) ***REMOVED***
        var mismatchError = new Error(
          [
            'The downloaded data did not match the data from the server.',
            'To be sure the content is the same, you should download the',
            'file again.',
          ].join(' ')
        );
        mismatchError.code = 'CONTENT_DOWNLOAD_MISMATCH';

        throughStream.destroy(mismatchError);
      ***REMOVED*** else ***REMOVED***
        throughStream.end();
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  throughStream.on('reading', makeRequest);

  return throughStream;
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** CreateResumableUploadResponse
 * @property ***REMOVED***string***REMOVED*** 0 The resumable upload's unique session URI.
 */
/**
 * @callback CreateResumableUploadCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***string***REMOVED*** uri The resumable upload's unique session URI.
 */
/**
 * Create a unique resumable upload session URI. This is the first step when
 * performing a resumable upload.
 *
 * See the [Resumable upload guide](https://cloud.google.com/storage/docs/json_api/v1/how-tos/resumable-upload)
 * for more on how the entire process works.
 *
 * <h4>Note</h4>
 *
 * If you are just looking to perform a resumable upload without worrying about
 * any of the details, see ***REMOVED***@link File#createWriteStream***REMOVED***. Resumable uploads
 * are performed by default.
 *
 * @see [Resumable upload guide]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/how-tos/resumable-upload***REMOVED***
 *
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***object***REMOVED*** [options.metadata] Metadata to set on the file.
 * @param ***REMOVED***string***REMOVED*** [options.origin] Origin header to set for the upload.
 * @param ***REMOVED***string***REMOVED*** [options.predefinedAcl] Apply a predefined set of access
 *     controls to this object.
 *
 *     Acceptable values are:
 *     - **`authenticatedRead`** - Object owner gets `OWNER` access, and
 *       `allAuthenticatedUsers` get `READER` access.
 *
 *     - **`bucketOwnerFullControl`** - Object owner gets `OWNER` access, and
 *       project team owners get `OWNER` access.
 *
 *     - **`bucketOwnerRead`** - Object owner gets `OWNER` access, and project
 *       team owners get `READER` access.
 *
 *     - **`private`** - Object owner gets `OWNER` access.
 *
 *     - **`projectPrivate`** - Object owner gets `OWNER` access, and project
 *       team members get access according to their roles.
 *
 *     - **`publicRead`** - Object owner gets `OWNER` access, and `allUsers` get
 *       `READER` access.
 * @param ***REMOVED***boolean***REMOVED*** [options.private] Make the uploaded file private. (Alias for
 *     `options.predefinedAcl = 'private'`)
 * @param ***REMOVED***boolean***REMOVED*** [options.public] Make the uploaded file public. (Alias for
 *     `options.predefinedAcl = 'publicRead'`)
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***CreateResumableUploadCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<CreateResumableUploadResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 * file.createResumableUpload(function(err, uri) ***REMOVED***
 *   if (!err) ***REMOVED***
 *     // `uri` can be used to PUT data to.
 *   ***REMOVED***
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.createResumableUpload().then(function(data) ***REMOVED***
 *   var uri = data[0];
 * ***REMOVED***);
 */
File.prototype.createResumableUpload = function(options, callback) ***REMOVED***
  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  resumableUpload.createURI(
    ***REMOVED***
      authClient: this.bucket.storage.authClient,
      bucket: this.bucket.name,
      file: this.name,
      generation: this.generation,
      metadata: options.metadata,
      origin: options.origin,
      predefinedAcl: options.predefinedAcl,
      private: options.private,
      public: options.public,
      userProject: options.userProject,
    ***REMOVED***,
    callback
  );
***REMOVED***;

/**
 * Create a writable stream to overwrite the contents of the file in your
 * bucket.
 *
 * A File object can also be used to create files for the first time.
 *
 * Resumable uploads are automatically enabled and must be shut off explicitly
 * by setting `options.resumable` to `false`.
 *
 * <p class="notice">
 *   There is some overhead when using a resumable upload that can cause
 *   noticeable performance degradation while uploading a series of small files.
 *   When uploading files less than 10MB, it is recommended that the resumable
 *   feature is disabled.
 * </p>
 *
 * For faster crc32c computation, you must manually install
 * [`fast-crc32c`](http://www.gitnpm.com/fast-crc32c):
 *
 *     $ npm install --save fast-crc32c
 *
 * NOTE: Writable streams will emit the `finish` event when the file is fully
 * uploaded.
 *
 * @see [Upload Options (Simple or Resumable)]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/how-tos/upload***REMOVED***
 * @see [Objects: insert API Documentation]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/objects/insert***REMOVED***
 *
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***boolean***REMOVED*** [options.gzip] Automatically gzip the file. This will set
 *     `options.metadata.contentEncoding` to `gzip`.
 * @param ***REMOVED***object***REMOVED*** [options.metadata] See the examples below or
 *     [Objects: insert request body](https://cloud.google.com/storage/docs/json_api/v1/objects/insert#request_properties_JSON)
 *     for more details.
 * @param ***REMOVED***string***REMOVED*** [options.offset] The starting byte of the upload stream, for
 *     resuming an interrupted upload. Defaults to 0.
 * @param ***REMOVED***string***REMOVED*** [options.predefinedAcl] Apply a predefined set of access
 *     controls to this object.
 *
 *     Acceptable values are:
 *     - **`authenticatedRead`** - Object owner gets `OWNER` access, and
 *       `allAuthenticatedUsers` get `READER` access.
 *
 *     - **`bucketOwnerFullControl`** - Object owner gets `OWNER` access, and
 *       project team owners get `OWNER` access.
 *
 *     - **`bucketOwnerRead`** - Object owner gets `OWNER` access, and project
 *       team owners get `READER` access.
 *
 *     - **`private`** - Object owner gets `OWNER` access.
 *
 *     - **`projectPrivate`** - Object owner gets `OWNER` access, and project
 *       team members get access according to their roles.
 *
 *     - **`publicRead`** - Object owner gets `OWNER` access, and `allUsers` get
 *       `READER` access.
 * @param ***REMOVED***boolean***REMOVED*** [options.private] Make the uploaded file private. (Alias for
 *     `options.predefinedAcl = 'private'`)
 * @param ***REMOVED***boolean***REMOVED*** [options.public] Make the uploaded file public. (Alias for
 *     `options.predefinedAcl = 'publicRead'`)
 * @param ***REMOVED***boolean***REMOVED*** [options.resumable] Force a resumable upload. NOTE: When
 *     working with streams, the file format and size is unknown until it's
 *     completely consumed. Because of this, it's best for you to be explicit
 *     for what makes sense given your input.
 * @param ***REMOVED***string***REMOVED*** [options.uri] The URI for an already-created resumable
 *     upload. See ***REMOVED***@link File#createResumableUpload***REMOVED***.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***string|boolean***REMOVED*** [options.validation] Possible values: `"md5"`,
 *     `"crc32c"`, or `false`. By default, data integrity is validated with a
 *     CRC32c checksum. You may use MD5 if preferred, but that hash is not
 *     supported for composite objects. An error will be raised if MD5 is
 *     specified but is not available. You may also choose to skip validation
 *     completely, however this is **not recommended**.
 * @returns ***REMOVED***WritableStream***REMOVED***
 *
 * @example
 * var fs = require('fs');
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * //-
 * // <h4>Uploading a File</h4>
 * //
 * // Now, consider a case where we want to upload a file to your bucket. You
 * // have the option of using ***REMOVED***@link Bucket#upload***REMOVED***, but that is just
 * // a convenience method which will do the following.
 * //-
 * fs.createReadStream('/Users/stephen/Photos/birthday-at-the-zoo/panda.jpg')
 *   .pipe(file.createWriteStream())
 *   .on('error', function(err) ***REMOVED******REMOVED***)
 *   .on('finish', function() ***REMOVED***
 *     // The file upload is complete.
 *   ***REMOVED***);
 *
 * //-
 * // <h4>Uploading a File with gzip compression</h4>
 * //-
 * fs.createReadStream('/Users/stephen/site/index.html')
 *   .pipe(file.createWriteStream(***REMOVED*** gzip: true ***REMOVED***))
 *   .on('error', function(err) ***REMOVED******REMOVED***)
 *   .on('finish', function() ***REMOVED***
 *     // The file upload is complete.
 *   ***REMOVED***);
 *
 * //-
 * // Downloading the file with `createReadStream` will automatically decode the
 * // file.
 * //-
 *
 * //-
 * // <h4>Uploading a File with Metadata</h4>
 * //
 * // One last case you may run into is when you want to upload a file to your
 * // bucket and set its metadata at the same time. Like above, you can use
 * // ***REMOVED***@link Bucket#upload***REMOVED*** to do this, which is just a wrapper around
 * // the following.
 * //-
 * fs.createReadStream('/Users/stephen/Photos/birthday-at-the-zoo/panda.jpg')
 *   .pipe(file.createWriteStream(***REMOVED***
 *     metadata: ***REMOVED***
 *       contentType: 'image/jpeg',
 *       metadata: ***REMOVED***
 *         custom: 'metadata'
 *       ***REMOVED***
 *     ***REMOVED***
 *   ***REMOVED***))
 *   .on('error', function(err) ***REMOVED******REMOVED***)
 *   .on('finish', function() ***REMOVED***
 *     // The file upload is complete.
 *   ***REMOVED***);
 */
File.prototype.createWriteStream = function(options) ***REMOVED***
  options = options || ***REMOVED******REMOVED***;

  var self = this;

  options = extend(***REMOVED***metadata: ***REMOVED******REMOVED******REMOVED***, options);

  var gzip = options.gzip;

  if (gzip) ***REMOVED***
    options.metadata.contentEncoding = 'gzip';
  ***REMOVED***

  var crc32c = true;
  var md5 = false;

  if (is.string(options.validation)) ***REMOVED***
    options.validation = options.validation.toLowerCase();
    crc32c = options.validation === 'crc32c';
    md5 = options.validation === 'md5';
  ***REMOVED*** else if (options.validation === false) ***REMOVED***
    crc32c = false;
  ***REMOVED***

  // Collect data as it comes in to store in a hash. This is compared to the
  // checksum value on the returned metadata from the API.
  var validateStream = hashStreamValidation(***REMOVED***
    crc32c: crc32c,
    md5: md5,
  ***REMOVED***);

  var fileWriteStream = duplexify();

  var stream = streamEvents(
    pumpify([
      gzip ? zlib.createGzip() : through(),
      validateStream,
      fileWriteStream,
    ])
  );

  // Wait until we've received data to determine what upload technique to use.
  stream.on('writing', function() ***REMOVED***
    if (options.resumable === false) ***REMOVED***
      self.startSimpleUpload_(fileWriteStream, options);
    ***REMOVED*** else ***REMOVED***
      self.startResumableUpload_(fileWriteStream, options);
    ***REMOVED***
  ***REMOVED***);

  fileWriteStream.on('response', stream.emit.bind(stream, 'response'));

  // This is to preserve the `finish` event. We wait until the request stream
  // emits "complete", as that is when we do validation of the data. After that
  // is successful, we can allow the stream to naturally finish.
  //
  // Reference for tracking when we can use a non-hack solution:
  // https://github.com/nodejs/node/pull/2314
  fileWriteStream.on('prefinish', function() ***REMOVED***
    stream.cork();
  ***REMOVED***);

  // Compare our hashed version vs the completed upload's version.
  fileWriteStream.on('complete', function() ***REMOVED***
    var metadata = self.metadata;

    // If we're doing validation, assume the worst-- a data integrity mismatch.
    // If not, these tests won't be performed, and we can assume the best.
    var failed = crc32c || md5;

    if (crc32c && metadata.crc32c) ***REMOVED***
      // We must remove the first four bytes from the returned checksum.
      // http://stackoverflow.com/questions/25096737/
      //   base64-encoding-of-crc32c-long-value
      failed = !validateStream.test('crc32c', metadata.crc32c.substr(4));
    ***REMOVED***

    if (md5 && metadata.md5Hash) ***REMOVED***
      failed = !validateStream.test('md5', metadata.md5Hash);
    ***REMOVED***

    if (failed) ***REMOVED***
      self.delete(function(err) ***REMOVED***
        var code;
        var message;

        if (err) ***REMOVED***
          code = 'FILE_NO_UPLOAD_DELETE';
          message = [
            'The uploaded data did not match the data from the server. As a',
            'precaution, we attempted to delete the file, but it was not',
            'successful. To be sure the content is the same, you should try',
            'removing the file manually, then uploading the file again.',
            '\n\nThe delete attempt failed with this message:',
            '\n\n  ' + err.message,
          ].join(' ');
        ***REMOVED*** else if (md5 && !metadata.md5Hash) ***REMOVED***
          code = 'MD5_NOT_AVAILABLE';
          message = [
            'MD5 verification was specified, but is not available for the',
            'requested object. MD5 is not available for composite objects.',
          ].join(' ');
        ***REMOVED*** else ***REMOVED***
          code = 'FILE_NO_UPLOAD';
          message = [
            'The uploaded data did not match the data from the server. As a',
            'precaution, the file has been deleted. To be sure the content',
            'is the same, you should try uploading the file again.',
          ].join(' ');
        ***REMOVED***

        var error = new Error(message);
        error.code = code;
        error.errors = [err];

        fileWriteStream.destroy(error);
      ***REMOVED***);

      return;
    ***REMOVED***

    stream.uncork();
  ***REMOVED***);

  return stream;
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** DeleteFileResponse
 * @property ***REMOVED***object***REMOVED*** 0 The full API response.
 */
/**
 * @callback DeleteFileCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Delete the file.
 *
 * @see [Objects: delete API Documentation]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/objects/delete***REMOVED***
 *
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***DeleteFileCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<DeleteFileResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 * file.delete(function(err, apiResponse) ***REMOVED******REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.delete().then(function(data) ***REMOVED***
 *   var apiResponse = data[0];
 * ***REMOVED***);
 *
 * @example <caption>include:samples/files.js</caption>
 * region_tag:storage_delete_file
 * Another example:
 */
File.prototype.delete = function(options, callback) ***REMOVED***
  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  options = extend(***REMOVED******REMOVED***, this.requestQueryObject, options);

  this.parent.delete.call(this, options, callback);
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** DownloadResponse
 * @property ***REMOVED***object***REMOVED*** [0] The contents of a File.
 */
/**
 * @callback DownloadCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***buffer***REMOVED*** [contents] The contents of a File.
 */
/**
 * Convenience method to download a file into memory or to a local destination.
 *
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options. The arguments match those
 *     passed to ***REMOVED***@link File#createReadStream***REMOVED***.
 * @param ***REMOVED***string***REMOVED*** [options.destination] Local file path to write the file's
 *     contents to.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***DownloadCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<DownloadResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * //-
 * // Download a file into memory. The contents will be available as the second
 * // argument in the demonstration below, `contents`.
 * //-
 * file.download(function(err, contents) ***REMOVED******REMOVED***);
 *
 * //-
 * // Download a file to a local destination.
 * //-
 * file.download(***REMOVED***
 *   destination: '/Users/me/Desktop/file-backup.txt'
 * ***REMOVED***, function(err) ***REMOVED******REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.download().then(function(data) ***REMOVED***
 *   var contents = data[0];
 * ***REMOVED***);
 *
 * @example <caption>include:samples/files.js</caption>
 * region_tag:storage_download_file
 * Another example:
 *
 * @example <caption>include:samples/encryption.js</caption>
 * region_tag:storage_download_encrypted_file
 * Example of downloading an encrypted file:
 *
 * @example <caption>include:samples/requesterPays.js</caption>
 * region_tag:storage_download_file_requester_pays
 * Example of downloading a file where the requester pays:
 */
File.prototype.download = function(options, callback) ***REMOVED***
  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  callback = once(callback);

  var destination = options.destination;
  delete options.destination;

  var fileStream = this.createReadStream(options);

  if (destination) ***REMOVED***
    fileStream
      .on('error', callback)
      .pipe(fs.createWriteStream(destination))
      .on('error', callback)
      .on('finish', callback);
  ***REMOVED*** else ***REMOVED***
    fileStream.on('error', callback).pipe(concat(callback.bind(null, null)));
  ***REMOVED***
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** FileExistsResponse
 * @property ***REMOVED***boolean***REMOVED*** 0 Whether the ***REMOVED***@link File***REMOVED*** exists.
 */
/**
 * @callback FileExistsCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***boolean***REMOVED*** exists Whether the ***REMOVED***@link File***REMOVED*** exists.
 */
/**
 * Check if the file exists.
 *
 * @param ***REMOVED***options***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***FileExistsCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<FileExistsResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * file.exists(function(err, exists) ***REMOVED******REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.exists().then(function(data) ***REMOVED***
 *   var exists = data[0];
 * ***REMOVED***);
 */
File.prototype.exists = function(options, callback) ***REMOVED***
  this.parent.exists.call(this, options, callback);
***REMOVED***;

/**
 * The Storage API allows you to use a custom key for server-side encryption.
 *
 * @see [Customer-supplied Encryption Keys]***REMOVED***@link https://cloud.google.com/storage/docs/encryption#customer-supplied***REMOVED***
 *
 * @param ***REMOVED***string|buffer***REMOVED*** encryptionKey An AES-256 encryption key.
 * @returns ***REMOVED***File***REMOVED***
 *
 * @example
 * var crypto = require('crypto');
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var encryptionKey = crypto.randomBytes(32);
 *
 * var fileWithCustomEncryption = myBucket.file('my-file');
 * fileWithCustomEncryption.setEncryptionKey(encryptionKey);
 *
 * var fileWithoutCustomEncryption = myBucket.file('my-file');
 *
 * fileWithCustomEncryption.save('data', function(err) ***REMOVED***
 *   // Try to download with the File object that hasn't had
 *   // `setEncryptionKey()` called:
 *   fileWithoutCustomEncryption.download(function(err) ***REMOVED***
 *     // We will receive an error:
 *     //   err.message === 'Bad Request'
 *
 *     // Try again with the File object we called `setEncryptionKey()` on:
 *     fileWithCustomEncryption.download(function(err, contents) ***REMOVED***
 *       // contents.toString() === 'data'
 *     ***REMOVED***);
 *   ***REMOVED***);
 * ***REMOVED***);
 *
 * @example <caption>include:samples/encryption.js</caption>
 * region_tag:storage_upload_encrypted_file
 * Example of uploading an encrypted file:
 *
 * @example <caption>include:samples/encryption.js</caption>
 * region_tag:storage_download_encrypted_file
 * Example of downloading an encrypted file:
 */
File.prototype.setEncryptionKey = function(encryptionKey) ***REMOVED***
  var self = this;

  this.encryptionKey = encryptionKey;
  this.encryptionKeyBase64 = Buffer.from(encryptionKey).toString('base64');

  this.encryptionKeyHash = crypto
    .createHash('sha256')
    .update(this.encryptionKeyBase64, 'base64')
    .digest('base64');

  this.interceptors.push(***REMOVED***
    request: function(reqOpts) ***REMOVED***
      reqOpts.headers = reqOpts.headers || ***REMOVED******REMOVED***;
      reqOpts.headers['x-goog-encryption-algorithm'] = 'AES256';
      reqOpts.headers['x-goog-encryption-key'] = self.encryptionKeyBase64;
      reqOpts.headers['x-goog-encryption-key-sha256'] = self.encryptionKeyHash;
      return reqOpts;
    ***REMOVED***,
  ***REMOVED***);

  return this;
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** GetFileResponse
 * @property ***REMOVED***File***REMOVED*** 0 The ***REMOVED***@link File***REMOVED***.
 * @property ***REMOVED***object***REMOVED*** 1 The full API response.
 */
/**
 * @callback GetFileCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***File***REMOVED*** file The ***REMOVED***@link File***REMOVED***.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Get a file object and its metadata if it exists.
 *
 * @param ***REMOVED***options***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***GetFileCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<GetFileResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * file.get(function(err, file, apiResponse) ***REMOVED***
 *   // file.metadata` has been populated.
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.get().then(function(data) ***REMOVED***
 *   var file = data[0];
 *   var apiResponse = data[1];
 * ***REMOVED***);
 */
File.prototype.get = function(options, callback) ***REMOVED***
  this.parent.get.call(this, options, callback);
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** GetFileMetadataResponse
 * @property ***REMOVED***object***REMOVED*** 0 The ***REMOVED***@link File***REMOVED*** metadata.
 * @property ***REMOVED***object***REMOVED*** 1 The full API response.
 */
/**
 * @callback GetFileMetadataCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***object***REMOVED*** metadata The ***REMOVED***@link File***REMOVED*** metadata.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Get the file's metadata.
 *
 * @see [Objects: get API Documentation]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/objects/get***REMOVED***
 *
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***GetFileMetadataCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<GetFileMetadataResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * file.getMetadata(function(err, metadata, apiResponse) ***REMOVED******REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.getMetadata().then(function(data) ***REMOVED***
 *   var metadata = data[0];
 *   var apiResponse = data[1];
 * ***REMOVED***);
 *
 * @example <caption>include:samples/files.js</caption>
 * region_tag:storage_get_metadata
 * Another example:
 */
File.prototype.getMetadata = function(options, callback) ***REMOVED***
  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  options = extend(***REMOVED******REMOVED***, this.requestQueryObject, options);

  this.parent.getMetadata.call(this, options, callback);
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** GetSignedPolicyResponse
 * @property ***REMOVED***object***REMOVED*** 0 The document policy.
 */
/**
 * @callback GetSignedPolicyCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***object***REMOVED*** policy The document policy.
 */
/**
 * Get a signed policy document to allow a user to upload data with a POST
 * request.
 *
 * @see [Policy Document Reference]***REMOVED***@link https://cloud.google.com/storage/docs/xml-api/post-object#policydocument***REMOVED***
 *
 * @throws ***REMOVED***Error***REMOVED*** If an expiration timestamp from the past is given.
 * @throws ***REMOVED***Error***REMOVED*** If options.equals has an array with less or more than two
 *     members.
 * @throws ***REMOVED***Error***REMOVED*** If options.startsWith has an array with less or more than two
 *     members.
 *
 * @param ***REMOVED***object***REMOVED*** options Configuration options.
 * @param ***REMOVED***array|array[]***REMOVED*** [options.equals] Array of request parameters and
 *     their expected value (e.g. [['$<field>', '<value>']]). Values are
 *     translated into equality constraints in the conditions field of the
 *     policy document (e.g. ['eq', '$<field>', '<value>']). If only one
 *     equality condition is to be specified, options.equals can be a one-
 *     dimensional array (e.g. ['$<field>', '<value>']).
 * @param ***REMOVED*******REMOVED*** options.expires - A timestamp when this policy will expire. Any
 *     value given is passed to `new Date()`.
 * @param ***REMOVED***array|array[]***REMOVED*** [options.startsWith] Array of request parameters and
 *     their expected prefixes (e.g. [['$<field>', '<value>']). Values are
 *     translated into starts-with constraints in the conditions field of the
 *     policy document (e.g. ['starts-with', '$<field>', '<value>']). If only
 *     one prefix condition is to be specified, options.startsWith can be a one-
 *     dimensional array (e.g. ['$<field>', '<value>']).
 * @param ***REMOVED***string***REMOVED*** [options.acl] ACL for the object from possibly predefined
 *     ACLs.
 * @param ***REMOVED***string***REMOVED*** [options.successRedirect] The URL to which the user client
 *     is redirected if the upload is successful.
 * @param ***REMOVED***string***REMOVED*** [options.successStatus] - The status of the Google Storage
 *     response if the upload is successful (must be string).
 * @param ***REMOVED***object***REMOVED*** [options.contentLengthRange]
 * @param ***REMOVED***number***REMOVED*** [options.contentLengthRange.min] Minimum value for the
 *     request's content length.
 * @param ***REMOVED***number***REMOVED*** [options.contentLengthRange.max] Maximum value for the
 *     request's content length.
 * @param ***REMOVED***GetSignedPolicyCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<GetSignedPolicyResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 * var options = ***REMOVED***
 *   equals: ['$Content-Type', 'image/jpeg'],
 *   expires: '10-25-2022',
 *   contentLengthRange: ***REMOVED***
 *     min: 0,
 *     max: 1024
 *   ***REMOVED***
 * ***REMOVED***;
 *
 * file.getSignedPolicy(options, function(err, policy) ***REMOVED***
 *   // policy.string: the policy document in plain text.
 *   // policy.base64: the policy document in base64.
 *   // policy.signature: the policy signature in base64.
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.getSignedPolicy(options).then(function(data) ***REMOVED***
 *   var policy = data[0];
 * ***REMOVED***);
 */
File.prototype.getSignedPolicy = function(options, callback) ***REMOVED***
  var expires = new Date(options.expires);

  if (expires < Date.now()) ***REMOVED***
    throw new Error('An expiration date cannot be in the past.');
  ***REMOVED***

  options = extend(***REMOVED******REMOVED***, options);

  var conditions = [
    ['eq', '$key', this.name],
    ***REMOVED***
      bucket: this.bucket.name,
    ***REMOVED***,
  ];

  if (is.array(options.equals)) ***REMOVED***
    if (!is.array(options.equals[0])) ***REMOVED***
      options.equals = [options.equals];
    ***REMOVED***
    options.equals.forEach(function(condition) ***REMOVED***
      if (!is.array(condition) || condition.length !== 2) ***REMOVED***
        throw new Error('Equals condition must be an array of 2 elements.');
      ***REMOVED***
      conditions.push(['eq', condition[0], condition[1]]);
    ***REMOVED***);
  ***REMOVED***

  if (is.array(options.startsWith)) ***REMOVED***
    if (!is.array(options.startsWith[0])) ***REMOVED***
      options.startsWith = [options.startsWith];
    ***REMOVED***
    options.startsWith.forEach(function(condition) ***REMOVED***
      if (!is.array(condition) || condition.length !== 2) ***REMOVED***
        throw new Error('StartsWith condition must be an array of 2 elements.');
      ***REMOVED***
      conditions.push(['starts-with', condition[0], condition[1]]);
    ***REMOVED***);
  ***REMOVED***

  if (options.acl) ***REMOVED***
    conditions.push(***REMOVED***
      acl: options.acl,
    ***REMOVED***);
  ***REMOVED***

  if (options.successRedirect) ***REMOVED***
    conditions.push(***REMOVED***
      success_action_redirect: options.successRedirect,
    ***REMOVED***);
  ***REMOVED***

  if (options.successStatus) ***REMOVED***
    conditions.push(***REMOVED***
      success_action_status: options.successStatus,
    ***REMOVED***);
  ***REMOVED***

  if (options.contentLengthRange) ***REMOVED***
    var min = options.contentLengthRange.min;
    var max = options.contentLengthRange.max;
    if (!is.number(min) || !is.number(max)) ***REMOVED***
      throw new Error('ContentLengthRange must have numeric min & max fields.');
    ***REMOVED***
    conditions.push(['content-length-range', min, max]);
  ***REMOVED***

  var policy = ***REMOVED***
    expiration: expires.toISOString(),
    conditions: conditions,
  ***REMOVED***;

  var policyString = JSON.stringify(policy);
  var policyBase64 = Buffer.from(policyString).toString('base64');

  this.storage.authClient.sign(policyBase64, function(err, signature) ***REMOVED***
    if (err) ***REMOVED***
      callback(new SigningError(err.message));
      return;
    ***REMOVED***

    callback(null, ***REMOVED***
      string: policyString,
      base64: policyBase64,
      signature: signature,
    ***REMOVED***);
  ***REMOVED***);
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** GetSignedUrlResponse
 * @property ***REMOVED***object***REMOVED*** 0 The signed URL.
 */
/**
 * @callback GetSignedUrlCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***object***REMOVED*** url The signed URL.
 */
/**
 * Get a signed URL to allow limited time access to the file.
 *
 * @see [Signed URLs Reference]***REMOVED***@link https://cloud.google.com/storage/docs/access-control/signed-urls***REMOVED***
 *
 * @throws ***REMOVED***Error***REMOVED*** if an expiration timestamp from the past is given.
 *
 * @param ***REMOVED***object***REMOVED*** config Configuration object.
 * @param ***REMOVED***string***REMOVED*** config.action "read" (HTTP: GET), "write" (HTTP: PUT), or
 *     "delete" (HTTP: DELETE).
 * @param ***REMOVED***string***REMOVED*** [config.cname] The cname for this bucket, i.e.,
 *     "https://cdn.example.com".
 * @param ***REMOVED***string***REMOVED*** [config.contentMd5] The MD5 digest value in base64. If you
 *     provide this, the client must provide this HTTP header with this same
 *     value in its request.
 * @param ***REMOVED***string***REMOVED*** [config.contentType] If you provide this value, the client
 *     must provide this HTTP header set to the same value.
 * @param ***REMOVED*******REMOVED*** config.expires A timestamp when this link will expire. Any value
 *     given is passed to `new Date()`.
 * @param ***REMOVED***object***REMOVED*** [config.extensionHeaders] If these headers are used, the
 *     server will check to make sure that the client provides matching values.
 *     See [Canonical extension headers](https://cloud.google.com/storage/docs/access-control/signed-urls#about-canonical-extension-headers)
 *     for the requirements of this feature, most notably:
 *       - The header name must be prefixed with `x-goog-`
 *       - The header name must be all lowercase
 * @param ***REMOVED***string***REMOVED*** [config.promptSaveAs] The filename to prompt the user to
 *     save the file as when the signed url is accessed. This is ignored if
 *     `config.responseDisposition` is set.
 * @param ***REMOVED***string***REMOVED*** [config.responseDisposition] The
 *     [response-content-disposition parameter](http://goo.gl/yMWxQV) of the
 *     signed url.
 * @param ***REMOVED***string***REMOVED*** [config.responseType] The response-content-type parameter
 *     of the signed url.
 * @param ***REMOVED***GetSignedUrlCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<GetSignedUrlResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * //-
 * // Generate a URL that allows temporary access to download your file.
 * //-
 * var request = require('request');
 *
 * var config = ***REMOVED***
 *   action: 'read',
 *   expires: '03-17-2025'
 * ***REMOVED***;
 *
 * file.getSignedUrl(config, function(err, url) ***REMOVED***
 *   if (err) ***REMOVED***
 *     console.error(err);
 *     return;
 *   ***REMOVED***
 *
 *   // The file is now available to read from this URL.
 *   request(url, function(err, resp) ***REMOVED***
 *     // resp.statusCode = 200
 *   ***REMOVED***);
 * ***REMOVED***);
 *
 * //-
 * // Generate a URL to allow write permissions. This means anyone with this URL
 * // can send a POST request with new data that will overwrite the file.
 * //-
 * file.getSignedUrl(***REMOVED***
 *   action: 'write',
 *   expires: '03-17-2025'
 * ***REMOVED***, function(err, url) ***REMOVED***
 *   if (err) ***REMOVED***
 *     console.error(err);
 *     return;
 *   ***REMOVED***
 *
 *   // The file is now available to be written to.
 *   var writeStream = request.put(url);
 *   writeStream.end('New data');
 *
 *   writeStream.on('complete', function(resp) ***REMOVED***
 *     // Confirm the new content was saved.
 *     file.download(function(err, fileContents) ***REMOVED***
 *       console.log('Contents:', fileContents.toString());
 *       // Contents: New data
 *     ***REMOVED***);
 *   ***REMOVED***);
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.getSignedUrl(config).then(function(data) ***REMOVED***
 *   var url = data[0];
 * ***REMOVED***);
 *
 * @example <caption>include:samples/files.js</caption>
 * region_tag:storage_generate_signed_url
 * Another example:
 */
File.prototype.getSignedUrl = function(config, callback) ***REMOVED***
  var self = this;
  var expires = new Date(config.expires);
  var expiresInSeconds = Math.round(expires / 1000); // The API expects seconds.

  if (expires < Date.now()) ***REMOVED***
    throw new Error('An expiration date cannot be in the past.');
  ***REMOVED***

  config = extend(***REMOVED******REMOVED***, config);

  config.action = ***REMOVED***
    read: 'GET',
    write: 'PUT',
    delete: 'DELETE',
  ***REMOVED***[config.action];

  var name = encodeURIComponent(this.name);
  var host = config.cname || STORAGE_DOWNLOAD_BASE_URL + '/' + self.bucket.name;
  config.resource = '/' + this.bucket.name + '/' + name;

  var extensionHeadersString = '';

  if (config.extensionHeaders) ***REMOVED***
    for (var headerName in config.extensionHeaders) ***REMOVED***
      extensionHeadersString += format('***REMOVED***name***REMOVED***:***REMOVED***value***REMOVED***\n', ***REMOVED***
        name: headerName,
        value: config.extensionHeaders[headerName],
      ***REMOVED***);
    ***REMOVED***
  ***REMOVED***

  var blobToSign = [
    config.action,
    config.contentMd5 || '',
    config.contentType || '',
    expiresInSeconds,
    extensionHeadersString + config.resource,
  ].join('\n');

  var authClient = this.storage.authClient;

  authClient.sign(blobToSign, function(err, signature) ***REMOVED***
    if (err) ***REMOVED***
      callback(new SigningError(err.message));
      return;
    ***REMOVED***

    var responseContentType;
    if (is.string(config.responseType)) ***REMOVED***
      responseContentType =
        '&response-content-type=' + encodeURIComponent(config.responseType);
    ***REMOVED***

    var responseContentDisposition;
    if (is.string(config.promptSaveAs)) ***REMOVED***
      responseContentDisposition =
        '&response-content-disposition=attachment; filename="' +
        encodeURIComponent(config.promptSaveAs) +
        '"';
    ***REMOVED***
    if (is.string(config.responseDisposition)) ***REMOVED***
      responseContentDisposition =
        '&response-content-disposition=' +
        encodeURIComponent(config.responseDisposition);
    ***REMOVED***

    var signedUrl = format('***REMOVED***host***REMOVED***/***REMOVED***name***REMOVED******REMOVED***id***REMOVED******REMOVED***exp***REMOVED******REMOVED***sig***REMOVED******REMOVED***type***REMOVED******REMOVED***disp***REMOVED******REMOVED***gen***REMOVED***', ***REMOVED***
      host: host.replace(/[/]*$/, ''), // Remove trailing slashes.
      name: name,
      id: '?GoogleAccessId=' + authClient.credentials.client_email,
      exp: '&Expires=' + expiresInSeconds,
      sig: '&Signature=' + encodeURIComponent(signature),
      type: responseContentType || '',
      disp: responseContentDisposition || '',
      gen: self.generation ? '&generation=' + self.generation : '',
    ***REMOVED***);

    callback(null, signedUrl);
  ***REMOVED***);
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** MakeFilePrivateResponse
 * @property ***REMOVED***object***REMOVED*** 0 The full API response.
 */
/**
 * @callback MakeFilePrivateCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Make a file private to the project and remove all other permissions.
 * Set `options.strict` to true to make the file private to only the owner.
 *
 * @see [Objects: patch API Documentation]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/objects/patch***REMOVED***
 *
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***boolean***REMOVED*** [options.strict] If true, set the file to be private to
 *     only the owner user. Otherwise, it will be private to the project.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***MakeFilePrivateCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<MakeFilePrivateResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * //-
 * // Set the file private so only project maintainers can see and modify it.
 * //-
 * file.makePrivate(function(err) ***REMOVED******REMOVED***);
 *
 * //-
 * // Set the file private so only the owner can see and modify it.
 * //-
 * file.makePrivate(***REMOVED*** strict: true ***REMOVED***, function(err) ***REMOVED******REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.makePrivate().then(function(data) ***REMOVED***
 *   var apiResponse = data[0];
 * ***REMOVED***);
 */
File.prototype.makePrivate = function(options, callback) ***REMOVED***
  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  var query = ***REMOVED***
    predefinedAcl: options.strict ? 'private' : 'projectPrivate',
  ***REMOVED***;

  if (options.userProject) ***REMOVED***
    query.userProject = options.userProject;
  ***REMOVED***

  this.setMetadata(
    ***REMOVED***
      // You aren't allowed to set both predefinedAcl & acl properties on a file,
      // so acl must explicitly be nullified, destroying all previous acls on the
      // file.
      acl: null,
    ***REMOVED***,
    query,
    callback
  );
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** MakeFilePublicResponse
 * @property ***REMOVED***object***REMOVED*** 0 The full API response.
 */
/**
 * @callback MakeFilePublicCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Set a file to be publicly readable and maintain all previous permissions.
 *
 * @see [ObjectAccessControls: insert API Documentation]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/objectAccessControls/insert***REMOVED***
 *
 * @param ***REMOVED***MakeFilePublicCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<MakeFilePublicResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * file.makePublic(function(err, apiResponse) ***REMOVED******REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.makePublic().then(function(data) ***REMOVED***
 *   var apiResponse = data[0];
 * ***REMOVED***);
 *
 * @example <caption>include:samples/files.js</caption>
 * region_tag:storage_make_public
 * Another example:
 */
File.prototype.makePublic = function(callback) ***REMOVED***
  callback = callback || common.util.noop;

  this.acl.add(
    ***REMOVED***
      entity: 'allUsers',
      role: 'READER',
    ***REMOVED***,
    function(err, resp) ***REMOVED***
      callback(err, resp);
    ***REMOVED***
  );
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** MoveResponse
 * @property ***REMOVED***File***REMOVED*** 0 The destination File.
 * @property ***REMOVED***object***REMOVED*** 1 The full API response.
 */
/**
 * @callback MoveCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***File***REMOVED*** destinationFile The destination File.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Move this file to another location. By default, this will rename the file
 * and keep it in the same bucket, but you can choose to move it to another
 * Bucket by providing a Bucket or File object or a URL beginning with "gs://".
 *
 * **Warning**:
 * There is currently no atomic `move` method in the Cloud Storage API,
 * so this method is a composition of ***REMOVED***@link File#copy***REMOVED*** (to the new
 * location) and ***REMOVED***@link File#delete***REMOVED*** (from the old location). While
 * unlikely, it is possible that an error returned to your callback could be
 * triggered from either one of these API calls failing, which could leave a
 * duplicate file lingering.
 *
 * @see [Objects: copy API Documentation]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/objects/copy***REMOVED***
 *
 * @throws ***REMOVED***Error***REMOVED*** If the destination file is not provided.
 *
 * @param ***REMOVED***string|Bucket|File***REMOVED*** destination Destination file.
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options. See an
 *     [Object resource](https://cloud.google.com/storage/docs/json_api/v1/objects#resource).
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***MoveCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<MoveResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * //-
 * // You can pass in a variety of types for the destination.
 * //
 * // For all of the below examples, assume we are working with the following
 * // Bucket and File objects.
 * //-
 * var bucket = storage.bucket('my-bucket');
 * var file = bucket.file('my-image.png');
 *
 * //-
 * // If you pass in a string for the destination, the file is moved to its
 * // current bucket, under the new name provided.
 * //-
 * file.move('my-image-new.png', function(err, destinationFile, apiResponse) ***REMOVED***
 *   // `my-bucket` no longer contains:
 *   // - "my-image.png"
 *   // but contains instead:
 *   // - "my-image-new.png"
 *
 *   // `destinationFile` is an instance of a File object that refers to your
 *   // new file.
 * ***REMOVED***);
 *
 * //-
 * // If you pass in a string starting with "gs://" for the destination, the
 * // file is copied to the other bucket and under the new name provided.
 * //-
 * var newLocation = 'gs://another-bucket/my-image-new.png';
 * file.move(newLocation, function(err, destinationFile, apiResponse) ***REMOVED***
 *   // `my-bucket` no longer contains:
 *   // - "my-image.png"
 *   //
 *   // `another-bucket` now contains:
 *   // - "my-image-new.png"
 *
 *   // `destinationFile` is an instance of a File object that refers to your
 *   // new file.
 * ***REMOVED***);
 *
 * //-
 * // If you pass in a Bucket object, the file will be moved to that bucket
 * // using the same name.
 * //-
 * var anotherBucket = gcs.bucket('another-bucket');
 *
 * file.move(anotherBucket, function(err, destinationFile, apiResponse) ***REMOVED***
 *   // `my-bucket` no longer contains:
 *   // - "my-image.png"
 *   //
 *   // `another-bucket` now contains:
 *   // - "my-image.png"
 *
 *   // `destinationFile` is an instance of a File object that refers to your
 *   // new file.
 * ***REMOVED***);
 *
 * //-
 * // If you pass in a File object, you have complete control over the new
 * // bucket and filename.
 * //-
 * var anotherFile = anotherBucket.file('my-awesome-image.png');
 *
 * file.move(anotherFile, function(err, destinationFile, apiResponse) ***REMOVED***
 *   // `my-bucket` no longer contains:
 *   // - "my-image.png"
 *   //
 *   // `another-bucket` now contains:
 *   // - "my-awesome-image.png"
 *
 *   // Note:
 *   // The `destinationFile` parameter is equal to `anotherFile`.
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.move('my-image-new.png').then(function(data) ***REMOVED***
 *   var destinationFile = data[0];
 *   var apiResponse = data[1];
 * ***REMOVED***);
 *
 * @example <caption>include:samples/files.js</caption>
 * region_tag:storage_move_file
 * Another example:
 */
File.prototype.move = function(destination, options, callback) ***REMOVED***
  var self = this;

  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  callback = callback || common.util.noop;

  this.copy(destination, options, function(err, destinationFile, apiResponse) ***REMOVED***
    if (err) ***REMOVED***
      callback(err, null, apiResponse);
      return;
    ***REMOVED***

    self.delete(options, function(err, apiResponse) ***REMOVED***
      callback(err, destinationFile, apiResponse);
    ***REMOVED***);
  ***REMOVED***);
***REMOVED***;

/**
 * Makes request and applies userProject query parameter if necessary.
 *
 * @private
 *
 * @param ***REMOVED***object***REMOVED*** reqOpts - The request options.
 * @param ***REMOVED***function***REMOVED*** callback - The callback function.
 */
File.prototype.request = function(reqOpts, callback) ***REMOVED***
  if (this.userProject && (!reqOpts.qs || !reqOpts.qs.userProject)) ***REMOVED***
    reqOpts.qs = extend(reqOpts.qs, ***REMOVED***userProject: this.userProject***REMOVED***);
  ***REMOVED***

  return common.ServiceObject.prototype.request.call(this, reqOpts, callback);
***REMOVED***;

/**
 * The Storage API allows you to use a custom key for server-side encryption.
 * This method allows you to update the encryption key associated with this
 * file.
 *
 * @see [Customer-supplied Encryption Keys]***REMOVED***@link https://cloud.google.com/storage/docs/encryption#customer-supplied***REMOVED***
 *
 * @param ***REMOVED***string|buffer***REMOVED*** encryptionKey An AES-256 encryption key.
 * @returns ***REMOVED***File***REMOVED***
 *
 * @example <caption>include:samples/encryption.js</caption>
 * region_tag:storage_rotate_encryption_key
 * Example of rotating the encryption key for this file:
 */
File.prototype.rotateEncryptionKey = function(encryptionKey, callback) ***REMOVED***
  var newFile = this.bucket.file(this.id, ***REMOVED***encryptionKey***REMOVED***);

  this.copy(newFile, callback);
***REMOVED***;

/**
 * @callback SaveCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 */
/**
 * Write arbitrary data to a file.
 *
 * *This is a convenience method which wraps ***REMOVED***@link File#createWriteStream***REMOVED***.*
 *
 * @param ***REMOVED*******REMOVED*** data The data to write to a file.
 * @param ***REMOVED***object***REMOVED*** [options] See ***REMOVED***@link File#createWriteStream***REMOVED***'s `options`
 *     parameter.
 * @param ***REMOVED***SaveCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 * var contents = 'This is the contents of the file.';
 *
 * file.save(contents, function(err) ***REMOVED***
 *   if (!err) ***REMOVED***
 *     // File written successfully.
 *   ***REMOVED***
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.save(contents).then(function() ***REMOVED******REMOVED***);
 */
File.prototype.save = function(data, options, callback) ***REMOVED***
  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  this.createWriteStream(options)
    .on('error', callback)
    .on('finish', callback)
    .end(data);
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** SetFileMetadataResponse
 * @property ***REMOVED***object***REMOVED*** 0 The full API response.
 */
/**
 * @callback SetFileMetadataCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Merge the given metadata with the current remote file's metadata. This
 * will set metadata if it was previously unset or update previously set
 * metadata. To unset previously set metadata, set its value to null.
 *
 * You can set custom key/value pairs in the metadata key of the given
 * object, however the other properties outside of this object must adhere
 * to the [official API documentation](https://goo.gl/BOnnCK).
 *
 * See the examples below for more information.
 *
 * @see [Objects: patch API Documentation]***REMOVED***@link https://cloud.google.com/storage/docs/json_api/v1/objects/patch***REMOVED***
 *
 * @param ***REMOVED***object***REMOVED*** [metadata] The metadata you wish to update.
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***SetFileMetadataCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<SetFileMetadataResponse>***REMOVED***
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var myBucket = storage.bucket('my-bucket');
 *
 * var file = myBucket.file('my-file');
 *
 * var metadata = ***REMOVED***
 *   contentType: 'application/x-font-ttf',
 *   metadata: ***REMOVED***
 *     my: 'custom',
 *     properties: 'go here'
 *   ***REMOVED***
 * ***REMOVED***;
 *
 * file.setMetadata(metadata, function(err, apiResponse) ***REMOVED******REMOVED***);
 *
 * // Assuming current metadata = ***REMOVED*** hello: 'world', unsetMe: 'will do' ***REMOVED***
 * file.setMetadata(***REMOVED***
 *   metadata: ***REMOVED***
 *     abc: '123', // will be set.
 *     unsetMe: null, // will be unset (deleted).
 *     hello: 'goodbye' // will be updated from 'hello' to 'goodbye'.
 *   ***REMOVED***
 * ***REMOVED***, function(err, apiResponse) ***REMOVED***
 *   // metadata should now be ***REMOVED*** abc: '123', hello: 'goodbye' ***REMOVED***
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.setMetadata(metadata).then(function(data) ***REMOVED***
 *   var apiResponse = data[0];
 * ***REMOVED***);
 */
File.prototype.setMetadata = function(metadata, options, callback) ***REMOVED***
  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  options = extend(***REMOVED******REMOVED***, this.requestQueryObject, options);

  this.parent.setMetadata.call(this, metadata, options, callback);
***REMOVED***;

/**
 * @typedef ***REMOVED***array***REMOVED*** SetStorageClassResponse
 * @property ***REMOVED***object***REMOVED*** 0 The full API response.
 */
/**
 * @callback SetStorageClassCallback
 * @param ***REMOVED***?Error***REMOVED*** err Request error, if any.
 * @param ***REMOVED***object***REMOVED*** apiResponse The full API response.
 */
/**
 * Set the storage class for this file.
 *
 * @see [Per-Object Storage Class]***REMOVED***@link https://cloud.google.com/storage/docs/per-object-storage-class***REMOVED***
 * @see [Storage Classes]***REMOVED***@link https://cloud.google.com/storage/docs/storage-classes***REMOVED***
 *
 * @param ***REMOVED***string***REMOVED*** storageClass The new storage class. (`multi_regional`,
 *     `regional`, `nearline`, `coldline`)
 * @param ***REMOVED***object***REMOVED*** [options] Configuration options.
 * @param ***REMOVED***string***REMOVED*** [options.userProject] The ID of the project which will be
 *     billed for the request.
 * @param ***REMOVED***SetStorageClassCallback***REMOVED*** [callback] Callback function.
 * @returns ***REMOVED***Promise<SetStorageClassResponse>***REMOVED***
 *
 * @example
 * file.setStorageClass('regional', function(err, apiResponse) ***REMOVED***
 *   if (err) ***REMOVED***
 *     // Error handling omitted.
 *   ***REMOVED***
 *
 *   // The storage class was updated successfully.
 * ***REMOVED***);
 *
 * //-
 * // If the callback is omitted, we'll return a Promise.
 * //-
 * file.setStorageClass('regional').then(function() ***REMOVED******REMOVED***);
 */
File.prototype.setStorageClass = function(storageClass, options, callback) ***REMOVED***
  var self = this;

  if (is.fn(options)) ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  options = extend(true, ***REMOVED******REMOVED***, options);

  // In case we get input like `storageClass`, convert to `storage_class`.
  options.storageClass = storageClass
    .replace(/-/g, '_')
    .replace(/([a-z])([A-Z])/g, function(_, low, up) ***REMOVED***
      return low + '_' + up;
    ***REMOVED***)
    .toUpperCase();

  this.copy(this, options, function(err, file, apiResponse) ***REMOVED***
    if (err) ***REMOVED***
      callback(err, apiResponse);
      return;
    ***REMOVED***

    self.metadata = file.metadata;

    callback(null, apiResponse);
  ***REMOVED***);
***REMOVED***;

/**
 * Set a user project to be billed for all requests made from this File object.
 *
 * @param ***REMOVED***string***REMOVED*** userProject The user project.
 *
 * @example
 * var storage = require('@google-cloud/storage')();
 * var bucket = storage.bucket('albums');
 * var file = bucket.file('my-file');
 *
 * file.setUserProject('grape-spaceship-123');
 */
File.prototype.setUserProject = function(userProject) ***REMOVED***
  this.userProject = userProject;
***REMOVED***;

/**
 * This creates a gcs-resumable-upload upload stream.
 *
 * @see [gcs-resumable-upload]***REMOVED***@link https://github.com/stephenplusplus/gcs-resumable-upload***REMOVED***
 *
 * @param ***REMOVED***Duplexify***REMOVED*** stream - Duplexify stream of data to pipe to the file.
 * @param ***REMOVED***object=***REMOVED*** options - Configuration object.
 *
 * @private
 */
File.prototype.startResumableUpload_ = function(dup, options) ***REMOVED***
  var self = this;

  options = extend(
    ***REMOVED***
      metadata: ***REMOVED******REMOVED***,
    ***REMOVED***,
    options
  );

  var uploadStream = resumableUpload(***REMOVED***
    authClient: this.storage.authClient,
    bucket: this.bucket.name,
    file: this.name,
    generation: this.generation,
    key: this.encryptionKey,
    metadata: options.metadata,
    offset: options.offset,
    predefinedAcl: options.predefinedAcl,
    private: options.private,
    public: options.public,
    uri: options.uri,
    userProject: options.userProject,
  ***REMOVED***);

  uploadStream
    .on('response', function(resp) ***REMOVED***
      dup.emit('response', resp);
    ***REMOVED***)
    .on('metadata', function(metadata) ***REMOVED***
      self.metadata = metadata;
    ***REMOVED***)
    .on('finish', function() ***REMOVED***
      dup.emit('complete');
    ***REMOVED***);

  dup.setWritable(uploadStream);
***REMOVED***;

/**
 * Takes a readable stream and pipes it to a remote file. Unlike
 * `startResumableUpload_`, which uses the resumable upload technique, this
 * method uses a simple upload (all or nothing).
 *
 * @param ***REMOVED***Duplexify***REMOVED*** dup - Duplexify stream of data to pipe to the file.
 * @param ***REMOVED***object=***REMOVED*** options - Configuration object.
 *
 * @private
 */
File.prototype.startSimpleUpload_ = function(dup, options) ***REMOVED***
  var self = this;

  options = extend(
    ***REMOVED***
      metadata: ***REMOVED******REMOVED***,
    ***REMOVED***,
    options
  );

  var reqOpts = ***REMOVED***
    qs: ***REMOVED***
      name: self.name,
    ***REMOVED***,
    uri: format('***REMOVED***uploadBaseUrl***REMOVED***/***REMOVED***bucket***REMOVED***/o', ***REMOVED***
      uploadBaseUrl: STORAGE_UPLOAD_BASE_URL,
      bucket: self.bucket.name,
    ***REMOVED***),
  ***REMOVED***;

  if (is.defined(this.generation)) ***REMOVED***
    reqOpts.qs.ifGenerationMatch = this.generation;
  ***REMOVED***

  if (options.userProject) ***REMOVED***
    reqOpts.qs.userProject = options.userProject;
  ***REMOVED***

  if (options.predefinedAcl) ***REMOVED***
    reqOpts.qs.predefinedAcl = options.predefinedAcl;
  ***REMOVED*** else if (options.private) ***REMOVED***
    reqOpts.qs.predefinedAcl = 'private';
  ***REMOVED*** else if (options.public) ***REMOVED***
    reqOpts.qs.predefinedAcl = 'publicRead';
  ***REMOVED***

  common.util.makeWritableStream(dup, ***REMOVED***
    makeAuthenticatedRequest: function(reqOpts) ***REMOVED***
      self.request(reqOpts, function(err, body, resp) ***REMOVED***
        if (err) ***REMOVED***
          dup.destroy(err);
          return;
        ***REMOVED***

        self.metadata = body;
        dup.emit('response', resp);
        dup.emit('complete');
      ***REMOVED***);
    ***REMOVED***,
    metadata: options.metadata,
    request: reqOpts,
  ***REMOVED***);
***REMOVED***;

/*! Developer Documentation
 *
 * All async methods (except for streams) will return a Promise in the event
 * that a callback is omitted.
 */
common.util.promisifyAll(File, ***REMOVED***
  exclude: ['setEncryptionKey'],
***REMOVED***);

/**
 * Reference to the ***REMOVED***@link File***REMOVED*** class.
 * @name module:@google-cloud/storage.File
 * @see File
 */
module.exports = File;
