'use strict';
const crypto = require('crypto');

module.exports = len => ***REMOVED***
	if (!Number.isFinite(len)) ***REMOVED***
		throw new TypeError('Expected a finite number');
	***REMOVED***

	return crypto.randomBytes(Math.ceil(len / 2)).toString('hex').slice(0, len);
***REMOVED***;
