module.exports = function walk (obj) ***REMOVED***
    if (!obj || typeof obj !== 'object') return obj;
    if (isDate(obj) || isRegex(obj)) return obj;
    if (Array.isArray(obj)) return obj.map(walk);
    return Object.keys(obj).reduce(function (acc, key) ***REMOVED***
        var camel = key[0].toLowerCase() + key.slice(1).replace(/([A-Z]+)/g, function (m, x) ***REMOVED***
            return '_' + x.toLowerCase();
        ***REMOVED***);
        acc[camel] = walk(obj[key]);
        return acc;
    ***REMOVED***, ***REMOVED******REMOVED***);
***REMOVED***;

var isDate = function (obj) ***REMOVED***
    return Object.prototype.toString.call(obj) === '[object Date]';
***REMOVED***;

var isRegex = function (obj) ***REMOVED***
    return Object.prototype.toString.call(obj) === '[object RegExp]';
***REMOVED***;
