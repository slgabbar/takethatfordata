'use strict';
var url = require('url');
var prependHttp = require('prepend-http');

module.exports = function (x) ***REMOVED***
	var withProtocol = prependHttp(x);
	var parsed = url.parse(withProtocol);

	if (withProtocol !== x) ***REMOVED***
		parsed.protocol = null;
	***REMOVED***

	return parsed;
***REMOVED***;
