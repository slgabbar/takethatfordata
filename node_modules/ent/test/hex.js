var test = require('tape');
var punycode = require('punycode');
var ent = require('../');

test('hex', function (t) ***REMOVED***
    for (var i = 0; i < 32; i++) ***REMOVED***
        var a = String.fromCharCode(i);
        if (a.match(/\s/)) ***REMOVED***
            t.equal(ent.decode(a), a);
        ***REMOVED***
        else ***REMOVED***
            var b = '&#x' + i.toString(16) + ';';
            t.equal(ent.decode(b), a);
            t.equal(ent.encode(a), '&#' + i + ';');
        ***REMOVED***
    ***REMOVED***
    
    for (var i = 127; i < 2000; i++) ***REMOVED***
        var a = String.fromCharCode(i);
        var b = '&#x' + i.toString(16) + ';';
        var c = '&#X' + i.toString(16) + ';';
        
        t.equal(ent.decode(b), a);
        t.equal(ent.decode(c), a);
        
        var encoded = ent.encode(a);
        var encoded2 = ent.encode(a + a);
        if (!encoded.match(/^&\w+;/)) ***REMOVED***
            t.equal(encoded, '&#' + i + ';');
            t.equal(encoded2, '&#' + i + ';&#' + i + ';');
        ***REMOVED***
    ***REMOVED***
    t.end();
***REMOVED***);

