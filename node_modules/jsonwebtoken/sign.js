var timespan = require('./lib/timespan');
var xtend = require('xtend');
var jws = require('jws');
var includes = require('lodash.includes');
var isBoolean = require('lodash.isboolean');
var isInteger = require('lodash.isinteger');
var isNumber = require('lodash.isnumber');
var isPlainObject = require('lodash.isplainobject');
var isString = require('lodash.isstring');
var once = require('lodash.once');

var sign_options_schema = ***REMOVED***
  expiresIn: ***REMOVED*** isValid: function(value) ***REMOVED*** return isInteger(value) || isString(value); ***REMOVED***, message: '"expiresIn" should be a number of seconds or string representing a timespan' ***REMOVED***,
  notBefore: ***REMOVED*** isValid: function(value) ***REMOVED*** return isInteger(value) || isString(value); ***REMOVED***, message: '"notBefore" should be a number of seconds or string representing a timespan' ***REMOVED***,
  audience: ***REMOVED*** isValid: function(value) ***REMOVED*** return isString(value) || Array.isArray(value); ***REMOVED***, message: '"audience" must be a string or array' ***REMOVED***,
  algorithm: ***REMOVED*** isValid: includes.bind(null, ['RS256', 'RS384', 'RS512', 'ES256', 'ES384', 'ES512', 'HS256', 'HS384', 'HS512', 'none']), message: '"algorithm" must be a valid string enum value' ***REMOVED***,
  header: ***REMOVED*** isValid: isPlainObject, message: '"header" must be an object' ***REMOVED***,
  encoding: ***REMOVED*** isValid: isString, message: '"encoding" must be a string' ***REMOVED***,
  issuer: ***REMOVED*** isValid: isString, message: '"issuer" must be a string' ***REMOVED***,
  subject: ***REMOVED*** isValid: isString, message: '"subject" must be a string' ***REMOVED***,
  jwtid: ***REMOVED*** isValid: isString, message: '"jwtid" must be a string' ***REMOVED***,
  noTimestamp: ***REMOVED*** isValid: isBoolean, message: '"noTimestamp" must be a boolean' ***REMOVED***,
  keyid: ***REMOVED*** isValid: isString, message: '"keyid" must be a string' ***REMOVED***
***REMOVED***;

var registered_claims_schema = ***REMOVED***
  iat: ***REMOVED*** isValid: isNumber, message: '"iat" should be a number of seconds' ***REMOVED***,
  exp: ***REMOVED*** isValid: isNumber, message: '"exp" should be a number of seconds' ***REMOVED***,
  nbf: ***REMOVED*** isValid: isNumber, message: '"nbf" should be a number of seconds' ***REMOVED***
***REMOVED***;

function validate(schema, allowUnknown, object, parameterName) ***REMOVED***
  if (!isPlainObject(object)) ***REMOVED***
    throw new Error('Expected "' + parameterName + '" to be a plain object.');
  ***REMOVED***
  Object.keys(object)
    .forEach(function(key) ***REMOVED***
      var validator = schema[key];
      if (!validator) ***REMOVED***
        if (!allowUnknown) ***REMOVED***
          throw new Error('"' + key + '" is not allowed in "' + parameterName + '"');
        ***REMOVED***
        return;
      ***REMOVED***
      if (!validator.isValid(object[key])) ***REMOVED***
        throw new Error(validator.message);
      ***REMOVED***
    ***REMOVED***);
***REMOVED***

function validateOptions(options) ***REMOVED***
  return validate(sign_options_schema, false, options, 'options');
***REMOVED***

function validatePayload(payload) ***REMOVED***
  return validate(registered_claims_schema, true, payload, 'payload');
***REMOVED***

var options_to_payload = ***REMOVED***
  'audience': 'aud',
  'issuer': 'iss',
  'subject': 'sub',
  'jwtid': 'jti'
***REMOVED***;

var options_for_objects = [
  'expiresIn',
  'notBefore',
  'noTimestamp',
  'audience',
  'issuer',
  'subject',
  'jwtid',
];

module.exports = function (payload, secretOrPrivateKey, options, callback) ***REMOVED***
  if (typeof options === 'function') ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED*** else ***REMOVED***
    options = options || ***REMOVED******REMOVED***;
  ***REMOVED***

  var isObjectPayload = typeof payload === 'object' &&
                        !Buffer.isBuffer(payload);

  var header = xtend(***REMOVED***
    alg: options.algorithm || 'HS256',
    typ: isObjectPayload ? 'JWT' : undefined,
    kid: options.keyid
  ***REMOVED***, options.header);

  function failure(err) ***REMOVED***
    if (callback) ***REMOVED***
      return callback(err);
    ***REMOVED***
    throw err;
  ***REMOVED***

  if (!secretOrPrivateKey && options.algorithm !== 'none') ***REMOVED***
    return failure(new Error('secretOrPrivateKey must have a value'));
  ***REMOVED***

  if (typeof payload === 'undefined') ***REMOVED***
    return failure(new Error('payload is required'));
  ***REMOVED*** else if (isObjectPayload) ***REMOVED***
    try ***REMOVED***
      validatePayload(payload);
    ***REMOVED***
    catch (error) ***REMOVED***
      return failure(error);
    ***REMOVED***
    payload = xtend(payload);
  ***REMOVED*** else ***REMOVED***
    var invalid_options = options_for_objects.filter(function (opt) ***REMOVED***
      return typeof options[opt] !== 'undefined';
    ***REMOVED***);

    if (invalid_options.length > 0) ***REMOVED***
      return failure(new Error('invalid ' + invalid_options.join(',') + ' option for ' + (typeof payload ) + ' payload'));
    ***REMOVED***
  ***REMOVED***

  if (typeof payload.exp !== 'undefined' && typeof options.expiresIn !== 'undefined') ***REMOVED***
    return failure(new Error('Bad "options.expiresIn" option the payload already has an "exp" property.'));
  ***REMOVED***

  if (typeof payload.nbf !== 'undefined' && typeof options.notBefore !== 'undefined') ***REMOVED***
    return failure(new Error('Bad "options.notBefore" option the payload already has an "nbf" property.'));
  ***REMOVED***

  try ***REMOVED***
    validateOptions(options);
  ***REMOVED***
  catch (error) ***REMOVED***
    return failure(error);
  ***REMOVED***

  var timestamp = payload.iat || Math.floor(Date.now() / 1000);

  if (!options.noTimestamp) ***REMOVED***
    payload.iat = timestamp;
  ***REMOVED*** else ***REMOVED***
    delete payload.iat;
  ***REMOVED***

  if (typeof options.notBefore !== 'undefined') ***REMOVED***
    payload.nbf = timespan(options.notBefore);
    if (typeof payload.nbf === 'undefined') ***REMOVED***
      return failure(new Error('"notBefore" should be a number of seconds or string representing a timespan eg: "1d", "20h", 60'));
    ***REMOVED***
  ***REMOVED***

  if (typeof options.expiresIn !== 'undefined' && typeof payload === 'object') ***REMOVED***
    payload.exp = timespan(options.expiresIn, timestamp);
    if (typeof payload.exp === 'undefined') ***REMOVED***
      return failure(new Error('"expiresIn" should be a number of seconds or string representing a timespan eg: "1d", "20h", 60'));
    ***REMOVED***
  ***REMOVED***

  Object.keys(options_to_payload).forEach(function (key) ***REMOVED***
    var claim = options_to_payload[key];
    if (typeof options[key] !== 'undefined') ***REMOVED***
      if (typeof payload[claim] !== 'undefined') ***REMOVED***
        return failure(new Error('Bad "options.' + key + '" option. The payload already has an "' + claim + '" property.'));
      ***REMOVED***
      payload[claim] = options[key];
    ***REMOVED***
  ***REMOVED***);

  var encoding = options.encoding || 'utf8';

  if (typeof callback === 'function') ***REMOVED***
    callback = callback && once(callback);

    jws.createSign(***REMOVED***
      header: header,
      privateKey: secretOrPrivateKey,
      payload: payload,
      encoding: encoding
    ***REMOVED***).once('error', callback)
      .once('done', function (signature) ***REMOVED***
        callback(null, signature);
      ***REMOVED***);
  ***REMOVED*** else ***REMOVED***
    return jws.sign(***REMOVED***header: header, payload: payload, secret: secretOrPrivateKey, encoding: encoding***REMOVED***);
  ***REMOVED***
***REMOVED***;
