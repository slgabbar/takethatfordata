var test = require('tape');
var traverse = require('../');
var EventEmitter = require('events').EventEmitter;

test('check instanceof on node elems', function (t) ***REMOVED***
    var counts = ***REMOVED*** emitter : 0 ***REMOVED***;
    
    traverse([ new EventEmitter, 3, 4, ***REMOVED*** ev : new EventEmitter ***REMOVED***])
        .forEach(function (node) ***REMOVED***
            if (node instanceof EventEmitter) counts.emitter ++;
        ***REMOVED***)
    ;
    
    t.equal(counts.emitter, 2);
    
    t.end();
***REMOVED***);
