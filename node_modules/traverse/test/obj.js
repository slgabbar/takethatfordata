var test = require('tape');
var traverse = require('../');

test('traverse an object with nested functions', function (t) ***REMOVED***
    t.plan(1);
    
    function Cons (x) ***REMOVED***
        t.equal(x, 10)
    ***REMOVED***;
    traverse(new Cons(10));
***REMOVED***);
