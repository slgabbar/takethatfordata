var test = require('tape');
var traverse = require('../');

test('traverse an Error', function (t) ***REMOVED***
    var obj = new Error("test");
    var results = traverse(obj).map(function (node) ***REMOVED******REMOVED***);
    t.same(results, ***REMOVED*** message: 'test' ***REMOVED***);
    
    t.end();
***REMOVED***);

