var traverse = require('traverse');

var obj = ***REMOVED***
    a : [1,2,3],
    b : 4,
    c : [5,6],
    d : ***REMOVED*** e : [7,8], f : 9 ***REMOVED***,
***REMOVED***;

var leaves = traverse(obj).reduce(function (acc, x) ***REMOVED***
    if (this.isLeaf) acc.push(x);
    return acc;
***REMOVED***, []);

console.dir(leaves);
