'use strict';

function spacerStr (len) ***REMOVED***
    var str = '';
    for(var i = 0; i < len; i += 1) ***REMOVED***
        str += ' ';
    ***REMOVED***
    return str;
***REMOVED***

function StringWriter (config) ***REMOVED***
    this.lines = [];
    this.lineSeparator = config.lineSeparator;
    this.regex = new RegExp(this.lineSeparator, 'g');
    this.spacer = spacerStr(config.outputOffset);
***REMOVED***

StringWriter.prototype.write = function (str) ***REMOVED***
    this.lines.push(this.spacer + str.replace(this.regex, this.lineSeparator + this.spacer));
***REMOVED***;

StringWriter.prototype.toString = function () ***REMOVED***
    var str = this.lines.join(this.lineSeparator);
    this.lines.length = 0;
    return str;
***REMOVED***;

module.exports = StringWriter;
