'use strict';

module.exports = function union(init) ***REMOVED***
  if (!Array.isArray(init)) ***REMOVED***
    throw new TypeError('arr-union expects the first argument to be an array.');
  ***REMOVED***

  var len = arguments.length;
  var i = 0;

  while (++i < len) ***REMOVED***
    var arg = arguments[i];
    if (!arg) continue;

    if (!Array.isArray(arg)) ***REMOVED***
      arg = [arg];
    ***REMOVED***

    for (var j = 0; j < arg.length; j++) ***REMOVED***
      var ele = arg[j];

      if (init.indexOf(ele) >= 0) ***REMOVED***
        continue;
      ***REMOVED***
      init.push(ele);
    ***REMOVED***
  ***REMOVED***
  return init;
***REMOVED***;
