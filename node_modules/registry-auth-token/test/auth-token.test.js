var fs = require('fs')
var path = require('path')
var mocha = require('mocha')
var assert = require('assert')
var requireUncached = require('require-uncached')

var npmRcPath = path.join(__dirname, '..', '.npmrc')
var afterEach = mocha.afterEach
var describe = mocha.describe
var it = mocha.it

var base64 = require('../base64')
var decodeBase64 = base64.decodeBase64
var encodeBase64 = base64.encodeBase64

/* eslint max-nested-callbacks: ["error", 4] */

describe('auth-token', function () ***REMOVED***
  afterEach(function (done) ***REMOVED***
    fs.unlink(npmRcPath, function () ***REMOVED***
      done()
    ***REMOVED***)
  ***REMOVED***)

  it('should read global if no local is found', function () ***REMOVED***
    var getAuthToken = requireUncached('../index')
    getAuthToken()
  ***REMOVED***)

  it('should return undefined if no auth token is given for registry', function (done) ***REMOVED***
    fs.writeFile(npmRcPath, 'registry=http://registry.npmjs.eu/', function (err) ***REMOVED***
      var getAuthToken = requireUncached('../index')
      assert(!err, err)
      assert(!getAuthToken())
      done()
    ***REMOVED***)
  ***REMOVED***)

  describe('legacy auth token', function () ***REMOVED***
    it('should return auth token if it is defined in the legacy way via the `_auth` key', function (done) ***REMOVED***
      var content = [
        '_auth=foobar',
        'registry=http://registry.foobar.eu/'
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken(), ***REMOVED***token: 'foobar', type: 'Basic'***REMOVED***)
        done()
      ***REMOVED***)
    ***REMOVED***)
  ***REMOVED***)

  describe('bearer token', function () ***REMOVED***
    it('should return auth token if registry is defined', function (done) ***REMOVED***
      var content = [
        'registry=http://registry.foobar.eu/',
        '//registry.foobar.eu/:_authToken=foobar', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken(), ***REMOVED***token: 'foobar', type: 'Bearer'***REMOVED***)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should use npmrc passed in', function (done) ***REMOVED***
      var content = [
        'registry=http://registry.foobar.eu/',
        '//registry.foobar.eu/:_authToken=foobar', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        const npmrc = ***REMOVED***
          'registry': 'http://registry.foobar.eu/',
          '//registry.foobar.eu/:_authToken': 'qar'
        ***REMOVED***
        assert.deepEqual(getAuthToken(***REMOVED***npmrc: npmrc***REMOVED***), ***REMOVED***token: 'qar', type: 'Bearer'***REMOVED***)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should return auth token if registry url has port specified', function (done) ***REMOVED***
      var content = [
        'registry=http://localhost:8770/',
        // before the patch this token was selected.
        '//localhost/:_authToken=ohno',
        '//localhost:8770/:_authToken=beepboop', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken(), ***REMOVED***token: 'beepboop', type: 'Bearer'***REMOVED***)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should return auth token defined by reference to an environment variable (with curly braces)', function (done) ***REMOVED***
      var environmentVariable = '__REGISTRY_AUTH_TOKEN_NPM_TOKEN__'
      var content = [
        'registry=http://registry.foobar.cc/',
        '//registry.foobar.cc/:_authToken=$***REMOVED***' + environmentVariable + '***REMOVED***', ''
      ].join('\n')
      process.env[environmentVariable] = 'foobar'

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken(), ***REMOVED***token: 'foobar', type: 'Bearer'***REMOVED***)
        delete process.env[environmentVariable]
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should return auth token defined by reference to an environment variable (without curly braces)', function (done) ***REMOVED***
      var environmentVariable = '__REGISTRY_AUTH_TOKEN_NPM_TOKEN__'
      var content = [
        'registry=http://registry.foobar.cc/',
        '//registry.foobar.cc/:_authToken=$' + environmentVariable, ''
      ].join('\n')
      process.env[environmentVariable] = 'foobar'

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken(), ***REMOVED***token: 'foobar', type: 'Bearer'***REMOVED***)
        delete process.env[environmentVariable]
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should try with and without a slash at the end of registry url', function (done) ***REMOVED***
      var content = [
        'registry=http://registry.foobar.eu',
        '//registry.foobar.eu:_authToken=barbaz', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken(), ***REMOVED***token: 'barbaz', type: 'Bearer'***REMOVED***)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should fetch for the registry given (if defined)', function (done) ***REMOVED***
      var content = [
        '//registry.foobar.eu:_authToken=barbaz',
        '//registry.blah.foo:_authToken=whatev',
        '//registry.last.thing:_authToken=yep', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken('//registry.blah.foo'), ***REMOVED***token: 'whatev', type: 'Bearer'***REMOVED***)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('recursively finds registries for deep url if option is set', function (done, undef) ***REMOVED***
      var opts = ***REMOVED***recursive: true***REMOVED***
      var content = [
        '//registry.blah.com/foo:_authToken=whatev',
        '//registry.blah.org/foo/bar:_authToken=recurseExactlyOneLevel',
        '//registry.blah.edu/foo/bar/baz:_authToken=recurseNoLevel',
        '//registry.blah.eu:_authToken=yep', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken('https://registry.blah.edu/foo/bar/baz', opts), ***REMOVED***token: 'recurseNoLevel', type: 'Bearer'***REMOVED***)
        assert.deepEqual(getAuthToken('https://registry.blah.org/foo/bar/baz', opts), ***REMOVED***token: 'recurseExactlyOneLevel', type: 'Bearer'***REMOVED***)
        assert.deepEqual(getAuthToken('https://registry.blah.com/foo/bar/baz', opts), ***REMOVED***token: 'whatev', type: 'Bearer'***REMOVED***)
        assert.deepEqual(getAuthToken('http://registry.blah.eu/what/ever', opts), ***REMOVED***token: 'yep', type: 'Bearer'***REMOVED***)
        assert.deepEqual(getAuthToken('http://registry.blah.eu//what/ever', opts), undefined, 'does not hang')
        assert.equal(getAuthToken('//some.registry', opts), undef)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should try both with and without trailing slash', function (done) ***REMOVED***
      fs.writeFile(npmRcPath, '//registry.blah.com:_authToken=whatev', function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken('https://registry.blah.com'), ***REMOVED***token: 'whatev', type: 'Bearer'***REMOVED***)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should prefer bearer token over basic token', function (done) ***REMOVED***
      var content = [
        'registry=http://registry.foobar.eu/',
        'registry=http://registry.foobar.eu/',
        '//registry.foobar.eu/:_authToken=bearerToken',
        '//registry.foobar.eu/:_password=' + encodeBase64('foobar'),
        '//registry.foobar.eu/:username=foobar', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(getAuthToken('//registry.foobar.eu'), ***REMOVED***token: 'bearerToken', type: 'Bearer'***REMOVED***)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('"nerf darts" registry urls', function (done, undef) ***REMOVED***
      fs.writeFile(npmRcPath, '//contoso.pkgs.visualstudio.com/_packaging/MyFeed/npm/:_authToken=heider', function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.deepEqual(
          getAuthToken('https://contoso.pkgs.visualstudio.com/_packaging/MyFeed/npm/registry'),
          ***REMOVED***token: 'heider', type: 'Bearer'***REMOVED***
        )
        done()
      ***REMOVED***)
    ***REMOVED***)
  ***REMOVED***)

  describe('basic token', function () ***REMOVED***
    it('should return undefined if password or username are missing', function (done, undef) ***REMOVED***
      var content = [
        'registry=http://registry.foobar.eu/',
        '//registry.foobar.eu/:_password=' + encodeBase64('foobar'),
        '//registry.foobar.com/:username=foobar', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        assert.equal(getAuthToken('//registry.foobar.eu'), undef)
        assert.equal(getAuthToken('//registry.foobar.com'), undef)
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should return basic token if username and password are defined', function (done) ***REMOVED***
      var content = [
        'registry=http://registry.foobar.eu/',
        '//registry.foobar.eu/:_password=' + encodeBase64('foobar'),
        '//registry.foobar.eu/:username=foobar', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        var token = getAuthToken()
        assert.deepEqual(token, ***REMOVED***
          token: 'Zm9vYmFyOmZvb2Jhcg==',
          type: 'Basic',
          username: 'foobar',
          password: 'foobar'
        ***REMOVED***)
        assert.equal(decodeBase64(token.token), 'foobar:foobar')
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should return basic token if registry url has port specified', function (done) ***REMOVED***
      var content = [
        'registry=http://localhost:8770/',
        // before the patch this token was selected.
        '//localhost/:_authToken=ohno',
        '//localhost:8770/:_password=' + encodeBase64('foobar'),
        '//localhost:8770/:username=foobar', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        var token = getAuthToken()
        assert.deepEqual(token, ***REMOVED***
          token: 'Zm9vYmFyOmZvb2Jhcg==',
          type: 'Basic',
          username: 'foobar',
          password: 'foobar'
        ***REMOVED***)
        assert.equal(decodeBase64(token.token), 'foobar:foobar')
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should return password defined by reference to an environment variable (with curly braces)', function (done) ***REMOVED***
      var environmentVariable = '__REGISTRY_PASSWORD__'
      var content = [
        'registry=http://registry.foobar.cc/',
        '//registry.foobar.cc/:username=username',
        '//registry.foobar.cc/:_password=$***REMOVED***' + environmentVariable + '***REMOVED***', ''
      ].join('\n')
      process.env[environmentVariable] = encodeBase64('password')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        var token = getAuthToken()
        assert.deepEqual(token, ***REMOVED***
          type: 'Basic',
          username: 'username',
          password: 'password',
          token: 'dXNlcm5hbWU6cGFzc3dvcmQ='
        ***REMOVED***)
        assert.equal(decodeBase64(token.token), 'username:password')
        delete process.env[environmentVariable]
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should return password defined by reference to an environment variable (without curly braces)', function (done) ***REMOVED***
      var environmentVariable = '__REGISTRY_PASSWORD__'
      var content = [
        'registry=http://registry.foobar.cc/',
        '//registry.foobar.cc/:username=username',
        '//registry.foobar.cc/:_password=$' + environmentVariable, ''
      ].join('\n')
      process.env[environmentVariable] = encodeBase64('password')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        var token = getAuthToken()
        assert.deepEqual(token, ***REMOVED***
          type: 'Basic',
          username: 'username',
          password: 'password',
          token: 'dXNlcm5hbWU6cGFzc3dvcmQ='
        ***REMOVED***)
        assert.equal(decodeBase64(token.token), 'username:password')
        delete process.env[environmentVariable]
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should try with and without a slash at the end of registry url', function (done) ***REMOVED***
      var content = [
        'registry=http://registry.foobar.eu',
        '//registry.foobar.eu:_password=' + encodeBase64('barbay'),
        '//registry.foobar.eu:username=barbaz', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        var token = getAuthToken()
        assert.deepEqual(token, ***REMOVED***
          token: 'YmFyYmF6OmJhcmJheQ==',
          type: 'Basic',
          password: 'barbay',
          username: 'barbaz'
        ***REMOVED***)
        assert.equal(decodeBase64(token.token), 'barbaz:barbay')
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('should fetch for the registry given (if defined)', function (done) ***REMOVED***
      var content = [
        '//registry.foobar.eu:_authToken=barbaz',
        '//registry.blah.foo:_password=' + encodeBase64('barbay'),
        '//registry.blah.foo:username=barbaz',
        '//registry.last.thing:_authToken=yep', ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        var token = getAuthToken('//registry.blah.foo')
        assert.deepEqual(token, ***REMOVED***
          token: 'YmFyYmF6OmJhcmJheQ==',
          type: 'Basic',
          password: 'barbay',
          username: 'barbaz'
        ***REMOVED***)
        assert.equal(decodeBase64(token.token), 'barbaz:barbay')
        done()
      ***REMOVED***)
    ***REMOVED***)

    it('recursively finds registries for deep url if option is set', function (done, undef) ***REMOVED***
      var opts = ***REMOVED***recursive: true***REMOVED***
      var content = [
        '//registry.blah.com/foo:_password=' + encodeBase64('barbay'),
        '//registry.blah.com/foo:username=barbaz',
        '//registry.blah.eu:username=barbaz',
        '//registry.blah.eu:_password=' + encodeBase64('foobaz'), ''
      ].join('\n')

      fs.writeFile(npmRcPath, content, function (err) ***REMOVED***
        var getAuthToken = requireUncached('../index')
        assert(!err, err)
        var token = getAuthToken('https://registry.blah.com/foo/bar/baz', opts)
        assert.deepEqual(token, ***REMOVED***
          token: 'YmFyYmF6OmJhcmJheQ==',
          type: 'Basic',
          password: 'barbay',
          username: 'barbaz'
        ***REMOVED***)
        assert.equal(decodeBase64(token.token), 'barbaz:barbay')
        token = getAuthToken('https://registry.blah.eu/foo/bar/baz', opts)
        assert.deepEqual(token, ***REMOVED***
          token: 'YmFyYmF6OmZvb2Jheg==',
          type: 'Basic',
          password: 'foobaz',
          username: 'barbaz'
        ***REMOVED***)
        assert.equal(decodeBase64(token.token), 'barbaz:foobaz')
        assert.equal(getAuthToken('//some.registry', opts), undef)
        done()
      ***REMOVED***)
    ***REMOVED***)
  ***REMOVED***)
***REMOVED***)
