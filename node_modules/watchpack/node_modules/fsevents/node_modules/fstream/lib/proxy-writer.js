// A writer for when we don't know what kind of thing
// the thing is.  That is, it's not explicitly set,
// so we're going to make it whatever the thing already
// is, or "File"
//
// Until then, collect all events.

module.exports = ProxyWriter

var Writer = require('./writer.js')
var getType = require('./get-type.js')
var inherits = require('inherits')
var collect = require('./collect.js')
var fs = require('fs')

inherits(ProxyWriter, Writer)

function ProxyWriter (props) ***REMOVED***
  var self = this
  if (!(self instanceof ProxyWriter)) ***REMOVED***
    throw new Error('ProxyWriter must be called as constructor.')
  ***REMOVED***

  self.props = props
  self._needDrain = false

  Writer.call(self, props)
***REMOVED***

ProxyWriter.prototype._stat = function () ***REMOVED***
  var self = this
  var props = self.props
  // stat the thing to see what the proxy should be.
  var stat = props.follow ? 'stat' : 'lstat'

  fs[stat](props.path, function (er, current) ***REMOVED***
    var type
    if (er || !current) ***REMOVED***
      type = 'File'
    ***REMOVED*** else ***REMOVED***
      type = getType(current)
    ***REMOVED***

    props[type] = true
    props.type = self.type = type

    self._old = current
    self._addProxy(Writer(props, current))
  ***REMOVED***)
***REMOVED***

ProxyWriter.prototype._addProxy = function (proxy) ***REMOVED***
  // console.error("~~ set proxy", this.path)
  var self = this
  if (self._proxy) ***REMOVED***
    return self.error('proxy already set')
  ***REMOVED***

  self._proxy = proxy
  ;[
    'ready',
    'error',
    'close',
    'pipe',
    'drain',
    'warn'
  ].forEach(function (ev) ***REMOVED***
    proxy.on(ev, self.emit.bind(self, ev))
  ***REMOVED***)

  self.emit('proxy', proxy)

  var calls = self._buffer
  calls.forEach(function (c) ***REMOVED***
    // console.error("~~ ~~ proxy buffered call", c[0], c[1])
    proxy[c[0]].apply(proxy, c[1])
  ***REMOVED***)
  self._buffer.length = 0
  if (self._needsDrain) self.emit('drain')
***REMOVED***

ProxyWriter.prototype.add = function (entry) ***REMOVED***
  // console.error("~~ proxy add")
  collect(entry)

  if (!this._proxy) ***REMOVED***
    this._buffer.push(['add', [entry]])
    this._needDrain = true
    return false
  ***REMOVED***
  return this._proxy.add(entry)
***REMOVED***

ProxyWriter.prototype.write = function (c) ***REMOVED***
  // console.error('~~ proxy write')
  if (!this._proxy) ***REMOVED***
    this._buffer.push(['write', [c]])
    this._needDrain = true
    return false
  ***REMOVED***
  return this._proxy.write(c)
***REMOVED***

ProxyWriter.prototype.end = function (c) ***REMOVED***
  // console.error('~~ proxy end')
  if (!this._proxy) ***REMOVED***
    this._buffer.push(['end', [c]])
    return false
  ***REMOVED***
  return this._proxy.end(c)
***REMOVED***
