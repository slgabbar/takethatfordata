/*!
 * arr-diff <https://github.com/jonschlinkert/arr-diff>
 *
 * Copyright (c) 2014 Jon Schlinkert, contributors.
 * Licensed under the MIT License
 */

'use strict';

var flatten = require('arr-flatten');
var slice = [].slice;

/**
 * Return the difference between the first array and
 * additional arrays.
 *
 * ```js
 * var diff = require('***REMOVED***%= name %***REMOVED***');
 *
 * var a = ['a', 'b', 'c', 'd'];
 * var b = ['b', 'c'];
 *
 * console.log(diff(a, b))
 * //=> ['a', 'd']
 * ```
 *
 * @param  ***REMOVED***Array***REMOVED*** `a`
 * @param  ***REMOVED***Array***REMOVED*** `b`
 * @return ***REMOVED***Array***REMOVED***
 * @api public
 */

function diff(arr, arrays) ***REMOVED***
  var argsLen = arguments.length;
  var len = arr.length, i = -1;
  var res = [], arrays;

  if (argsLen === 1) ***REMOVED***
    return arr;
  ***REMOVED***

  if (argsLen > 2) ***REMOVED***
    arrays = flatten(slice.call(arguments, 1));
  ***REMOVED***

  while (++i < len) ***REMOVED***
    if (!~arrays.indexOf(arr[i])) ***REMOVED***
      res.push(arr[i]);
    ***REMOVED***
  ***REMOVED***
  return res;
***REMOVED***

/**
 * Expose `diff`
 */

module.exports = diff;
