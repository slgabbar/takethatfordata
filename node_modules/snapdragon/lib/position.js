'use strict';

var define = require('define-property');

/**
 * Store position for a node
 */

module.exports = function Position(start, parser) ***REMOVED***
  this.start = start;
  this.end = ***REMOVED*** line: parser.line, column: parser.column ***REMOVED***;
  define(this, 'content', parser.orig);
  define(this, 'source', parser.options.source);
***REMOVED***;
