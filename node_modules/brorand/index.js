var r;

module.exports = function rand(len) ***REMOVED***
  if (!r)
    r = new Rand(null);

  return r.generate(len);
***REMOVED***;

function Rand(rand) ***REMOVED***
  this.rand = rand;
***REMOVED***
module.exports.Rand = Rand;

Rand.prototype.generate = function generate(len) ***REMOVED***
  return this._rand(len);
***REMOVED***;

// Emulate crypto API using randy
Rand.prototype._rand = function _rand(n) ***REMOVED***
  if (this.rand.getBytes)
    return this.rand.getBytes(n);

  var res = new Uint8Array(n);
  for (var i = 0; i < res.length; i++)
    res[i] = this.rand.getByte();
  return res;
***REMOVED***;

if (typeof self === 'object') ***REMOVED***
  if (self.crypto && self.crypto.getRandomValues) ***REMOVED***
    // Modern browsers
    Rand.prototype._rand = function _rand(n) ***REMOVED***
      var arr = new Uint8Array(n);
      self.crypto.getRandomValues(arr);
      return arr;
    ***REMOVED***;
  ***REMOVED*** else if (self.msCrypto && self.msCrypto.getRandomValues) ***REMOVED***
    // IE
    Rand.prototype._rand = function _rand(n) ***REMOVED***
      var arr = new Uint8Array(n);
      self.msCrypto.getRandomValues(arr);
      return arr;
    ***REMOVED***;

  // Safari's WebWorkers do not have `crypto`
  ***REMOVED*** else if (typeof window === 'object') ***REMOVED***
    // Old junk
    Rand.prototype._rand = function() ***REMOVED***
      throw new Error('Not implemented yet');
    ***REMOVED***;
  ***REMOVED***
***REMOVED*** else ***REMOVED***
  // Node.js or Web worker with no crypto support
  try ***REMOVED***
    var crypto = require('crypto');
    if (typeof crypto.randomBytes !== 'function')
      throw new Error('Not supported');

    Rand.prototype._rand = function _rand(n) ***REMOVED***
      return crypto.randomBytes(n);
    ***REMOVED***;
  ***REMOVED*** catch (e) ***REMOVED***
  ***REMOVED***
***REMOVED***
