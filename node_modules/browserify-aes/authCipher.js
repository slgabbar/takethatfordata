var aes = require('./aes')
var Buffer = require('safe-buffer').Buffer
var Transform = require('cipher-base')
var inherits = require('inherits')
var GHASH = require('./ghash')
var xor = require('buffer-xor')
var incr32 = require('./incr32')

function xorTest (a, b) ***REMOVED***
  var out = 0
  if (a.length !== b.length) out++

  var len = Math.min(a.length, b.length)
  for (var i = 0; i < len; ++i) ***REMOVED***
    out += (a[i] ^ b[i])
  ***REMOVED***

  return out
***REMOVED***

function calcIv (self, iv, ck) ***REMOVED***
  if (iv.length === 12) ***REMOVED***
    self._finID = Buffer.concat([iv, Buffer.from([0, 0, 0, 1])])
    return Buffer.concat([iv, Buffer.from([0, 0, 0, 2])])
  ***REMOVED***
  var ghash = new GHASH(ck)
  var len = iv.length
  var toPad = len % 16
  ghash.update(iv)
  if (toPad) ***REMOVED***
    toPad = 16 - toPad
    ghash.update(Buffer.alloc(toPad, 0))
  ***REMOVED***
  ghash.update(Buffer.alloc(8, 0))
  var ivBits = len * 8
  var tail = Buffer.alloc(8)
  tail.writeUIntBE(ivBits, 0, 8)
  ghash.update(tail)
  self._finID = ghash.state
  var out = Buffer.from(self._finID)
  incr32(out)
  return out
***REMOVED***
function StreamCipher (mode, key, iv, decrypt) ***REMOVED***
  Transform.call(this)

  var h = Buffer.alloc(4, 0)

  this._cipher = new aes.AES(key)
  var ck = this._cipher.encryptBlock(h)
  this._ghash = new GHASH(ck)
  iv = calcIv(this, iv, ck)

  this._prev = Buffer.from(iv)
  this._cache = Buffer.allocUnsafe(0)
  this._secCache = Buffer.allocUnsafe(0)
  this._decrypt = decrypt
  this._alen = 0
  this._len = 0
  this._mode = mode

  this._authTag = null
  this._called = false
***REMOVED***

inherits(StreamCipher, Transform)

StreamCipher.prototype._update = function (chunk) ***REMOVED***
  if (!this._called && this._alen) ***REMOVED***
    var rump = 16 - (this._alen % 16)
    if (rump < 16) ***REMOVED***
      rump = Buffer.alloc(rump, 0)
      this._ghash.update(rump)
    ***REMOVED***
  ***REMOVED***

  this._called = true
  var out = this._mode.encrypt(this, chunk)
  if (this._decrypt) ***REMOVED***
    this._ghash.update(chunk)
  ***REMOVED*** else ***REMOVED***
    this._ghash.update(out)
  ***REMOVED***
  this._len += chunk.length
  return out
***REMOVED***

StreamCipher.prototype._final = function () ***REMOVED***
  if (this._decrypt && !this._authTag) throw new Error('Unsupported state or unable to authenticate data')

  var tag = xor(this._ghash.final(this._alen * 8, this._len * 8), this._cipher.encryptBlock(this._finID))
  if (this._decrypt && xorTest(tag, this._authTag)) throw new Error('Unsupported state or unable to authenticate data')

  this._authTag = tag
  this._cipher.scrub()
***REMOVED***

StreamCipher.prototype.getAuthTag = function getAuthTag () ***REMOVED***
  if (this._decrypt || !Buffer.isBuffer(this._authTag)) throw new Error('Attempting to get auth tag in unsupported state')

  return this._authTag
***REMOVED***

StreamCipher.prototype.setAuthTag = function setAuthTag (tag) ***REMOVED***
  if (!this._decrypt) throw new Error('Attempting to set auth tag in unsupported state')

  this._authTag = tag
***REMOVED***

StreamCipher.prototype.setAAD = function setAAD (buf) ***REMOVED***
  if (this._called) throw new Error('Attempting to set AAD in unsupported state')

  this._ghash.update(buf)
  this._alen += buf.length
***REMOVED***

module.exports = StreamCipher
