'use strict';
const path = require('path');
const pathExists = require('path-exists');
const pLocate = require('p-locate');

module.exports = (iterable, opts) => ***REMOVED***
	opts = Object.assign(***REMOVED***
		cwd: process.cwd()
	***REMOVED***, opts);

	return pLocate(iterable, el => pathExists(path.resolve(opts.cwd, el)), opts);
***REMOVED***;

module.exports.sync = (iterable, opts) => ***REMOVED***
	opts = Object.assign(***REMOVED***
		cwd: process.cwd()
	***REMOVED***, opts);

	for (const el of iterable) ***REMOVED***
		if (pathExists.sync(path.resolve(opts.cwd, el))) ***REMOVED***
			return el;
		***REMOVED***
	***REMOVED***
***REMOVED***;
