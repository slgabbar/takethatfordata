var test  = require('tape');
var chalk = require('chalk');
var cp = require('child_process');

var red = chalk.red,
    green = chalk.green,
    cyan = chalk.cyan;

test(cyan('Directly Execute bin/ps-tree.js'), function (t) ***REMOVED***
  var first = cp.exec('node -v', function (error, stdout, stderr) ***REMOVED******REMOVED***);
  var child = cp.exec('node ./bin/ps-tree.js', function (error, data) ***REMOVED***
    // console.log('data: ' + data.length);
    if (error !== null) ***REMOVED***
      console.log(red('exec error: ' + error));
    ***REMOVED***
  ***REMOVED***);

  t.true(child.pid, green('✓ Called ./bin/ps-tree.js directly. worked as expected'));
  t.end();
***REMOVED***);
