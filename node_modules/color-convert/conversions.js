/* MIT license */
var cssKeywords = require('color-name');

// NOTE: conversions should only return primitive values (i.e. arrays, or
//       values that give correct `typeof` results).
//       do not use box values types (i.e. Number(), String(), etc.)

var reverseKeywords = ***REMOVED******REMOVED***;
for (var key in cssKeywords) ***REMOVED***
	if (cssKeywords.hasOwnProperty(key)) ***REMOVED***
		reverseKeywords[cssKeywords[key]] = key;
	***REMOVED***
***REMOVED***

var convert = module.exports = ***REMOVED***
	rgb: ***REMOVED***channels: 3, labels: 'rgb'***REMOVED***,
	hsl: ***REMOVED***channels: 3, labels: 'hsl'***REMOVED***,
	hsv: ***REMOVED***channels: 3, labels: 'hsv'***REMOVED***,
	hwb: ***REMOVED***channels: 3, labels: 'hwb'***REMOVED***,
	cmyk: ***REMOVED***channels: 4, labels: 'cmyk'***REMOVED***,
	xyz: ***REMOVED***channels: 3, labels: 'xyz'***REMOVED***,
	lab: ***REMOVED***channels: 3, labels: 'lab'***REMOVED***,
	lch: ***REMOVED***channels: 3, labels: 'lch'***REMOVED***,
	hex: ***REMOVED***channels: 1, labels: ['hex']***REMOVED***,
	keyword: ***REMOVED***channels: 1, labels: ['keyword']***REMOVED***,
	ansi16: ***REMOVED***channels: 1, labels: ['ansi16']***REMOVED***,
	ansi256: ***REMOVED***channels: 1, labels: ['ansi256']***REMOVED***,
	hcg: ***REMOVED***channels: 3, labels: ['h', 'c', 'g']***REMOVED***,
	apple: ***REMOVED***channels: 3, labels: ['r16', 'g16', 'b16']***REMOVED***,
	gray: ***REMOVED***channels: 1, labels: ['gray']***REMOVED***
***REMOVED***;

// hide .channels and .labels properties
for (var model in convert) ***REMOVED***
	if (convert.hasOwnProperty(model)) ***REMOVED***
		if (!('channels' in convert[model])) ***REMOVED***
			throw new Error('missing channels property: ' + model);
		***REMOVED***

		if (!('labels' in convert[model])) ***REMOVED***
			throw new Error('missing channel labels property: ' + model);
		***REMOVED***

		if (convert[model].labels.length !== convert[model].channels) ***REMOVED***
			throw new Error('channel and label counts mismatch: ' + model);
		***REMOVED***

		var channels = convert[model].channels;
		var labels = convert[model].labels;
		delete convert[model].channels;
		delete convert[model].labels;
		Object.defineProperty(convert[model], 'channels', ***REMOVED***value: channels***REMOVED***);
		Object.defineProperty(convert[model], 'labels', ***REMOVED***value: labels***REMOVED***);
	***REMOVED***
***REMOVED***

convert.rgb.hsl = function (rgb) ***REMOVED***
	var r = rgb[0] / 255;
	var g = rgb[1] / 255;
	var b = rgb[2] / 255;
	var min = Math.min(r, g, b);
	var max = Math.max(r, g, b);
	var delta = max - min;
	var h;
	var s;
	var l;

	if (max === min) ***REMOVED***
		h = 0;
	***REMOVED*** else if (r === max) ***REMOVED***
		h = (g - b) / delta;
	***REMOVED*** else if (g === max) ***REMOVED***
		h = 2 + (b - r) / delta;
	***REMOVED*** else if (b === max) ***REMOVED***
		h = 4 + (r - g) / delta;
	***REMOVED***

	h = Math.min(h * 60, 360);

	if (h < 0) ***REMOVED***
		h += 360;
	***REMOVED***

	l = (min + max) / 2;

	if (max === min) ***REMOVED***
		s = 0;
	***REMOVED*** else if (l <= 0.5) ***REMOVED***
		s = delta / (max + min);
	***REMOVED*** else ***REMOVED***
		s = delta / (2 - max - min);
	***REMOVED***

	return [h, s * 100, l * 100];
***REMOVED***;

convert.rgb.hsv = function (rgb) ***REMOVED***
	var r = rgb[0];
	var g = rgb[1];
	var b = rgb[2];
	var min = Math.min(r, g, b);
	var max = Math.max(r, g, b);
	var delta = max - min;
	var h;
	var s;
	var v;

	if (max === 0) ***REMOVED***
		s = 0;
	***REMOVED*** else ***REMOVED***
		s = (delta / max * 1000) / 10;
	***REMOVED***

	if (max === min) ***REMOVED***
		h = 0;
	***REMOVED*** else if (r === max) ***REMOVED***
		h = (g - b) / delta;
	***REMOVED*** else if (g === max) ***REMOVED***
		h = 2 + (b - r) / delta;
	***REMOVED*** else if (b === max) ***REMOVED***
		h = 4 + (r - g) / delta;
	***REMOVED***

	h = Math.min(h * 60, 360);

	if (h < 0) ***REMOVED***
		h += 360;
	***REMOVED***

	v = ((max / 255) * 1000) / 10;

	return [h, s, v];
***REMOVED***;

convert.rgb.hwb = function (rgb) ***REMOVED***
	var r = rgb[0];
	var g = rgb[1];
	var b = rgb[2];
	var h = convert.rgb.hsl(rgb)[0];
	var w = 1 / 255 * Math.min(r, Math.min(g, b));

	b = 1 - 1 / 255 * Math.max(r, Math.max(g, b));

	return [h, w * 100, b * 100];
***REMOVED***;

convert.rgb.cmyk = function (rgb) ***REMOVED***
	var r = rgb[0] / 255;
	var g = rgb[1] / 255;
	var b = rgb[2] / 255;
	var c;
	var m;
	var y;
	var k;

	k = Math.min(1 - r, 1 - g, 1 - b);
	c = (1 - r - k) / (1 - k) || 0;
	m = (1 - g - k) / (1 - k) || 0;
	y = (1 - b - k) / (1 - k) || 0;

	return [c * 100, m * 100, y * 100, k * 100];
***REMOVED***;

/**
 * See https://en.m.wikipedia.org/wiki/Euclidean_distance#Squared_Euclidean_distance
 * */
function comparativeDistance(x, y) ***REMOVED***
	return (
		Math.pow(x[0] - y[0], 2) +
		Math.pow(x[1] - y[1], 2) +
		Math.pow(x[2] - y[2], 2)
	);
***REMOVED***

convert.rgb.keyword = function (rgb) ***REMOVED***
	var reversed = reverseKeywords[rgb];
	if (reversed) ***REMOVED***
		return reversed;
	***REMOVED***

	var currentClosestDistance = Infinity;
	var currentClosestKeyword;

	for (var keyword in cssKeywords) ***REMOVED***
		if (cssKeywords.hasOwnProperty(keyword)) ***REMOVED***
			var value = cssKeywords[keyword];

			// Compute comparative distance
			var distance = comparativeDistance(rgb, value);

			// Check if its less, if so set as closest
			if (distance < currentClosestDistance) ***REMOVED***
				currentClosestDistance = distance;
				currentClosestKeyword = keyword;
			***REMOVED***
		***REMOVED***
	***REMOVED***

	return currentClosestKeyword;
***REMOVED***;

convert.keyword.rgb = function (keyword) ***REMOVED***
	return cssKeywords[keyword];
***REMOVED***;

convert.rgb.xyz = function (rgb) ***REMOVED***
	var r = rgb[0] / 255;
	var g = rgb[1] / 255;
	var b = rgb[2] / 255;

	// assume sRGB
	r = r > 0.04045 ? Math.pow(((r + 0.055) / 1.055), 2.4) : (r / 12.92);
	g = g > 0.04045 ? Math.pow(((g + 0.055) / 1.055), 2.4) : (g / 12.92);
	b = b > 0.04045 ? Math.pow(((b + 0.055) / 1.055), 2.4) : (b / 12.92);

	var x = (r * 0.4124) + (g * 0.3576) + (b * 0.1805);
	var y = (r * 0.2126) + (g * 0.7152) + (b * 0.0722);
	var z = (r * 0.0193) + (g * 0.1192) + (b * 0.9505);

	return [x * 100, y * 100, z * 100];
***REMOVED***;

convert.rgb.lab = function (rgb) ***REMOVED***
	var xyz = convert.rgb.xyz(rgb);
	var x = xyz[0];
	var y = xyz[1];
	var z = xyz[2];
	var l;
	var a;
	var b;

	x /= 95.047;
	y /= 100;
	z /= 108.883;

	x = x > 0.008856 ? Math.pow(x, 1 / 3) : (7.787 * x) + (16 / 116);
	y = y > 0.008856 ? Math.pow(y, 1 / 3) : (7.787 * y) + (16 / 116);
	z = z > 0.008856 ? Math.pow(z, 1 / 3) : (7.787 * z) + (16 / 116);

	l = (116 * y) - 16;
	a = 500 * (x - y);
	b = 200 * (y - z);

	return [l, a, b];
***REMOVED***;

convert.hsl.rgb = function (hsl) ***REMOVED***
	var h = hsl[0] / 360;
	var s = hsl[1] / 100;
	var l = hsl[2] / 100;
	var t1;
	var t2;
	var t3;
	var rgb;
	var val;

	if (s === 0) ***REMOVED***
		val = l * 255;
		return [val, val, val];
	***REMOVED***

	if (l < 0.5) ***REMOVED***
		t2 = l * (1 + s);
	***REMOVED*** else ***REMOVED***
		t2 = l + s - l * s;
	***REMOVED***

	t1 = 2 * l - t2;

	rgb = [0, 0, 0];
	for (var i = 0; i < 3; i++) ***REMOVED***
		t3 = h + 1 / 3 * -(i - 1);
		if (t3 < 0) ***REMOVED***
			t3++;
		***REMOVED***
		if (t3 > 1) ***REMOVED***
			t3--;
		***REMOVED***

		if (6 * t3 < 1) ***REMOVED***
			val = t1 + (t2 - t1) * 6 * t3;
		***REMOVED*** else if (2 * t3 < 1) ***REMOVED***
			val = t2;
		***REMOVED*** else if (3 * t3 < 2) ***REMOVED***
			val = t1 + (t2 - t1) * (2 / 3 - t3) * 6;
		***REMOVED*** else ***REMOVED***
			val = t1;
		***REMOVED***

		rgb[i] = val * 255;
	***REMOVED***

	return rgb;
***REMOVED***;

convert.hsl.hsv = function (hsl) ***REMOVED***
	var h = hsl[0];
	var s = hsl[1] / 100;
	var l = hsl[2] / 100;
	var smin = s;
	var lmin = Math.max(l, 0.01);
	var sv;
	var v;

	l *= 2;
	s *= (l <= 1) ? l : 2 - l;
	smin *= lmin <= 1 ? lmin : 2 - lmin;
	v = (l + s) / 2;
	sv = l === 0 ? (2 * smin) / (lmin + smin) : (2 * s) / (l + s);

	return [h, sv * 100, v * 100];
***REMOVED***;

convert.hsv.rgb = function (hsv) ***REMOVED***
	var h = hsv[0] / 60;
	var s = hsv[1] / 100;
	var v = hsv[2] / 100;
	var hi = Math.floor(h) % 6;

	var f = h - Math.floor(h);
	var p = 255 * v * (1 - s);
	var q = 255 * v * (1 - (s * f));
	var t = 255 * v * (1 - (s * (1 - f)));
	v *= 255;

	switch (hi) ***REMOVED***
		case 0:
			return [v, t, p];
		case 1:
			return [q, v, p];
		case 2:
			return [p, v, t];
		case 3:
			return [p, q, v];
		case 4:
			return [t, p, v];
		case 5:
			return [v, p, q];
	***REMOVED***
***REMOVED***;

convert.hsv.hsl = function (hsv) ***REMOVED***
	var h = hsv[0];
	var s = hsv[1] / 100;
	var v = hsv[2] / 100;
	var vmin = Math.max(v, 0.01);
	var lmin;
	var sl;
	var l;

	l = (2 - s) * v;
	lmin = (2 - s) * vmin;
	sl = s * vmin;
	sl /= (lmin <= 1) ? lmin : 2 - lmin;
	sl = sl || 0;
	l /= 2;

	return [h, sl * 100, l * 100];
***REMOVED***;

// http://dev.w3.org/csswg/css-color/#hwb-to-rgb
convert.hwb.rgb = function (hwb) ***REMOVED***
	var h = hwb[0] / 360;
	var wh = hwb[1] / 100;
	var bl = hwb[2] / 100;
	var ratio = wh + bl;
	var i;
	var v;
	var f;
	var n;

	// wh + bl cant be > 1
	if (ratio > 1) ***REMOVED***
		wh /= ratio;
		bl /= ratio;
	***REMOVED***

	i = Math.floor(6 * h);
	v = 1 - bl;
	f = 6 * h - i;

	if ((i & 0x01) !== 0) ***REMOVED***
		f = 1 - f;
	***REMOVED***

	n = wh + f * (v - wh); // linear interpolation

	var r;
	var g;
	var b;
	switch (i) ***REMOVED***
		default:
		case 6:
		case 0: r = v; g = n; b = wh; break;
		case 1: r = n; g = v; b = wh; break;
		case 2: r = wh; g = v; b = n; break;
		case 3: r = wh; g = n; b = v; break;
		case 4: r = n; g = wh; b = v; break;
		case 5: r = v; g = wh; b = n; break;
	***REMOVED***

	return [r * 255, g * 255, b * 255];
***REMOVED***;

convert.cmyk.rgb = function (cmyk) ***REMOVED***
	var c = cmyk[0] / 100;
	var m = cmyk[1] / 100;
	var y = cmyk[2] / 100;
	var k = cmyk[3] / 100;
	var r;
	var g;
	var b;

	r = 1 - Math.min(1, c * (1 - k) + k);
	g = 1 - Math.min(1, m * (1 - k) + k);
	b = 1 - Math.min(1, y * (1 - k) + k);

	return [r * 255, g * 255, b * 255];
***REMOVED***;

convert.xyz.rgb = function (xyz) ***REMOVED***
	var x = xyz[0] / 100;
	var y = xyz[1] / 100;
	var z = xyz[2] / 100;
	var r;
	var g;
	var b;

	r = (x * 3.2406) + (y * -1.5372) + (z * -0.4986);
	g = (x * -0.9689) + (y * 1.8758) + (z * 0.0415);
	b = (x * 0.0557) + (y * -0.2040) + (z * 1.0570);

	// assume sRGB
	r = r > 0.0031308
		? ((1.055 * Math.pow(r, 1.0 / 2.4)) - 0.055)
		: r * 12.92;

	g = g > 0.0031308
		? ((1.055 * Math.pow(g, 1.0 / 2.4)) - 0.055)
		: g * 12.92;

	b = b > 0.0031308
		? ((1.055 * Math.pow(b, 1.0 / 2.4)) - 0.055)
		: b * 12.92;

	r = Math.min(Math.max(0, r), 1);
	g = Math.min(Math.max(0, g), 1);
	b = Math.min(Math.max(0, b), 1);

	return [r * 255, g * 255, b * 255];
***REMOVED***;

convert.xyz.lab = function (xyz) ***REMOVED***
	var x = xyz[0];
	var y = xyz[1];
	var z = xyz[2];
	var l;
	var a;
	var b;

	x /= 95.047;
	y /= 100;
	z /= 108.883;

	x = x > 0.008856 ? Math.pow(x, 1 / 3) : (7.787 * x) + (16 / 116);
	y = y > 0.008856 ? Math.pow(y, 1 / 3) : (7.787 * y) + (16 / 116);
	z = z > 0.008856 ? Math.pow(z, 1 / 3) : (7.787 * z) + (16 / 116);

	l = (116 * y) - 16;
	a = 500 * (x - y);
	b = 200 * (y - z);

	return [l, a, b];
***REMOVED***;

convert.lab.xyz = function (lab) ***REMOVED***
	var l = lab[0];
	var a = lab[1];
	var b = lab[2];
	var x;
	var y;
	var z;

	y = (l + 16) / 116;
	x = a / 500 + y;
	z = y - b / 200;

	var y2 = Math.pow(y, 3);
	var x2 = Math.pow(x, 3);
	var z2 = Math.pow(z, 3);
	y = y2 > 0.008856 ? y2 : (y - 16 / 116) / 7.787;
	x = x2 > 0.008856 ? x2 : (x - 16 / 116) / 7.787;
	z = z2 > 0.008856 ? z2 : (z - 16 / 116) / 7.787;

	x *= 95.047;
	y *= 100;
	z *= 108.883;

	return [x, y, z];
***REMOVED***;

convert.lab.lch = function (lab) ***REMOVED***
	var l = lab[0];
	var a = lab[1];
	var b = lab[2];
	var hr;
	var h;
	var c;

	hr = Math.atan2(b, a);
	h = hr * 360 / 2 / Math.PI;

	if (h < 0) ***REMOVED***
		h += 360;
	***REMOVED***

	c = Math.sqrt(a * a + b * b);

	return [l, c, h];
***REMOVED***;

convert.lch.lab = function (lch) ***REMOVED***
	var l = lch[0];
	var c = lch[1];
	var h = lch[2];
	var a;
	var b;
	var hr;

	hr = h / 360 * 2 * Math.PI;
	a = c * Math.cos(hr);
	b = c * Math.sin(hr);

	return [l, a, b];
***REMOVED***;

convert.rgb.ansi16 = function (args) ***REMOVED***
	var r = args[0];
	var g = args[1];
	var b = args[2];
	var value = 1 in arguments ? arguments[1] : convert.rgb.hsv(args)[2]; // hsv -> ansi16 optimization

	value = Math.round(value / 50);

	if (value === 0) ***REMOVED***
		return 30;
	***REMOVED***

	var ansi = 30
		+ ((Math.round(b / 255) << 2)
		| (Math.round(g / 255) << 1)
		| Math.round(r / 255));

	if (value === 2) ***REMOVED***
		ansi += 60;
	***REMOVED***

	return ansi;
***REMOVED***;

convert.hsv.ansi16 = function (args) ***REMOVED***
	// optimization here; we already know the value and don't need to get
	// it converted for us.
	return convert.rgb.ansi16(convert.hsv.rgb(args), args[2]);
***REMOVED***;

convert.rgb.ansi256 = function (args) ***REMOVED***
	var r = args[0];
	var g = args[1];
	var b = args[2];

	// we use the extended greyscale palette here, with the exception of
	// black and white. normal palette only has 4 greyscale shades.
	if (r === g && g === b) ***REMOVED***
		if (r < 8) ***REMOVED***
			return 16;
		***REMOVED***

		if (r > 248) ***REMOVED***
			return 231;
		***REMOVED***

		return Math.round(((r - 8) / 247) * 24) + 232;
	***REMOVED***

	var ansi = 16
		+ (36 * Math.round(r / 255 * 5))
		+ (6 * Math.round(g / 255 * 5))
		+ Math.round(b / 255 * 5);

	return ansi;
***REMOVED***;

convert.ansi16.rgb = function (args) ***REMOVED***
	var color = args % 10;

	// handle greyscale
	if (color === 0 || color === 7) ***REMOVED***
		if (args > 50) ***REMOVED***
			color += 3.5;
		***REMOVED***

		color = color / 10.5 * 255;

		return [color, color, color];
	***REMOVED***

	var mult = (~~(args > 50) + 1) * 0.5;
	var r = ((color & 1) * mult) * 255;
	var g = (((color >> 1) & 1) * mult) * 255;
	var b = (((color >> 2) & 1) * mult) * 255;

	return [r, g, b];
***REMOVED***;

convert.ansi256.rgb = function (args) ***REMOVED***
	// handle greyscale
	if (args >= 232) ***REMOVED***
		var c = (args - 232) * 10 + 8;
		return [c, c, c];
	***REMOVED***

	args -= 16;

	var rem;
	var r = Math.floor(args / 36) / 5 * 255;
	var g = Math.floor((rem = args % 36) / 6) / 5 * 255;
	var b = (rem % 6) / 5 * 255;

	return [r, g, b];
***REMOVED***;

convert.rgb.hex = function (args) ***REMOVED***
	var integer = ((Math.round(args[0]) & 0xFF) << 16)
		+ ((Math.round(args[1]) & 0xFF) << 8)
		+ (Math.round(args[2]) & 0xFF);

	var string = integer.toString(16).toUpperCase();
	return '000000'.substring(string.length) + string;
***REMOVED***;

convert.hex.rgb = function (args) ***REMOVED***
	var match = args.toString(16).match(/[a-f0-9]***REMOVED***6***REMOVED***|[a-f0-9]***REMOVED***3***REMOVED***/i);
	if (!match) ***REMOVED***
		return [0, 0, 0];
	***REMOVED***

	var colorString = match[0];

	if (match[0].length === 3) ***REMOVED***
		colorString = colorString.split('').map(function (char) ***REMOVED***
			return char + char;
		***REMOVED***).join('');
	***REMOVED***

	var integer = parseInt(colorString, 16);
	var r = (integer >> 16) & 0xFF;
	var g = (integer >> 8) & 0xFF;
	var b = integer & 0xFF;

	return [r, g, b];
***REMOVED***;

convert.rgb.hcg = function (rgb) ***REMOVED***
	var r = rgb[0] / 255;
	var g = rgb[1] / 255;
	var b = rgb[2] / 255;
	var max = Math.max(Math.max(r, g), b);
	var min = Math.min(Math.min(r, g), b);
	var chroma = (max - min);
	var grayscale;
	var hue;

	if (chroma < 1) ***REMOVED***
		grayscale = min / (1 - chroma);
	***REMOVED*** else ***REMOVED***
		grayscale = 0;
	***REMOVED***

	if (chroma <= 0) ***REMOVED***
		hue = 0;
	***REMOVED*** else
	if (max === r) ***REMOVED***
		hue = ((g - b) / chroma) % 6;
	***REMOVED*** else
	if (max === g) ***REMOVED***
		hue = 2 + (b - r) / chroma;
	***REMOVED*** else ***REMOVED***
		hue = 4 + (r - g) / chroma + 4;
	***REMOVED***

	hue /= 6;
	hue %= 1;

	return [hue * 360, chroma * 100, grayscale * 100];
***REMOVED***;

convert.hsl.hcg = function (hsl) ***REMOVED***
	var s = hsl[1] / 100;
	var l = hsl[2] / 100;
	var c = 1;
	var f = 0;

	if (l < 0.5) ***REMOVED***
		c = 2.0 * s * l;
	***REMOVED*** else ***REMOVED***
		c = 2.0 * s * (1.0 - l);
	***REMOVED***

	if (c < 1.0) ***REMOVED***
		f = (l - 0.5 * c) / (1.0 - c);
	***REMOVED***

	return [hsl[0], c * 100, f * 100];
***REMOVED***;

convert.hsv.hcg = function (hsv) ***REMOVED***
	var s = hsv[1] / 100;
	var v = hsv[2] / 100;

	var c = s * v;
	var f = 0;

	if (c < 1.0) ***REMOVED***
		f = (v - c) / (1 - c);
	***REMOVED***

	return [hsv[0], c * 100, f * 100];
***REMOVED***;

convert.hcg.rgb = function (hcg) ***REMOVED***
	var h = hcg[0] / 360;
	var c = hcg[1] / 100;
	var g = hcg[2] / 100;

	if (c === 0.0) ***REMOVED***
		return [g * 255, g * 255, g * 255];
	***REMOVED***

	var pure = [0, 0, 0];
	var hi = (h % 1) * 6;
	var v = hi % 1;
	var w = 1 - v;
	var mg = 0;

	switch (Math.floor(hi)) ***REMOVED***
		case 0:
			pure[0] = 1; pure[1] = v; pure[2] = 0; break;
		case 1:
			pure[0] = w; pure[1] = 1; pure[2] = 0; break;
		case 2:
			pure[0] = 0; pure[1] = 1; pure[2] = v; break;
		case 3:
			pure[0] = 0; pure[1] = w; pure[2] = 1; break;
		case 4:
			pure[0] = v; pure[1] = 0; pure[2] = 1; break;
		default:
			pure[0] = 1; pure[1] = 0; pure[2] = w;
	***REMOVED***

	mg = (1.0 - c) * g;

	return [
		(c * pure[0] + mg) * 255,
		(c * pure[1] + mg) * 255,
		(c * pure[2] + mg) * 255
	];
***REMOVED***;

convert.hcg.hsv = function (hcg) ***REMOVED***
	var c = hcg[1] / 100;
	var g = hcg[2] / 100;

	var v = c + g * (1.0 - c);
	var f = 0;

	if (v > 0.0) ***REMOVED***
		f = c / v;
	***REMOVED***

	return [hcg[0], f * 100, v * 100];
***REMOVED***;

convert.hcg.hsl = function (hcg) ***REMOVED***
	var c = hcg[1] / 100;
	var g = hcg[2] / 100;

	var l = g * (1.0 - c) + 0.5 * c;
	var s = 0;

	if (l > 0.0 && l < 0.5) ***REMOVED***
		s = c / (2 * l);
	***REMOVED*** else
	if (l >= 0.5 && l < 1.0) ***REMOVED***
		s = c / (2 * (1 - l));
	***REMOVED***

	return [hcg[0], s * 100, l * 100];
***REMOVED***;

convert.hcg.hwb = function (hcg) ***REMOVED***
	var c = hcg[1] / 100;
	var g = hcg[2] / 100;
	var v = c + g * (1.0 - c);
	return [hcg[0], (v - c) * 100, (1 - v) * 100];
***REMOVED***;

convert.hwb.hcg = function (hwb) ***REMOVED***
	var w = hwb[1] / 100;
	var b = hwb[2] / 100;
	var v = 1 - b;
	var c = v - w;
	var g = 0;

	if (c < 1) ***REMOVED***
		g = (v - c) / (1 - c);
	***REMOVED***

	return [hwb[0], c * 100, g * 100];
***REMOVED***;

convert.apple.rgb = function (apple) ***REMOVED***
	return [(apple[0] / 65535) * 255, (apple[1] / 65535) * 255, (apple[2] / 65535) * 255];
***REMOVED***;

convert.rgb.apple = function (rgb) ***REMOVED***
	return [(rgb[0] / 255) * 65535, (rgb[1] / 255) * 65535, (rgb[2] / 255) * 65535];
***REMOVED***;

convert.gray.rgb = function (args) ***REMOVED***
	return [args[0] / 100 * 255, args[0] / 100 * 255, args[0] / 100 * 255];
***REMOVED***;

convert.gray.hsl = convert.gray.hsv = function (args) ***REMOVED***
	return [0, 0, args[0]];
***REMOVED***;

convert.gray.hwb = function (gray) ***REMOVED***
	return [0, 100, gray[0]];
***REMOVED***;

convert.gray.cmyk = function (gray) ***REMOVED***
	return [0, 0, 0, gray[0]];
***REMOVED***;

convert.gray.lab = function (gray) ***REMOVED***
	return [gray[0], 0, 0];
***REMOVED***;

convert.gray.hex = function (gray) ***REMOVED***
	var val = Math.round(gray[0] / 100 * 255) & 0xFF;
	var integer = (val << 16) + (val << 8) + val;

	var string = integer.toString(16).toUpperCase();
	return '000000'.substring(string.length) + string;
***REMOVED***;

convert.rgb.gray = function (rgb) ***REMOVED***
	var val = (rgb[0] + rgb[1] + rgb[2]) / 3;
	return [val / 255 * 100];
***REMOVED***;
