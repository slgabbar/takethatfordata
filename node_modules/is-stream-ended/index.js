'use strict';

module.exports = function (stream) ***REMOVED***
  var ended;

  if (typeof stream.ended !== 'undefined') ***REMOVED***
    ended = stream.ended;
  ***REMOVED*** else ***REMOVED***
    ended = stream._readableState.ended;
  ***REMOVED***

  return Boolean(ended).valueOf();
***REMOVED***;
