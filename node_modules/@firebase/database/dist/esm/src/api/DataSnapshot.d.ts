import ***REMOVED*** Node ***REMOVED*** from '../core/snap/Node';
import ***REMOVED*** Reference ***REMOVED*** from './Reference';
import ***REMOVED*** Index ***REMOVED*** from '../core/snap/indexes/Index';
/**
 * Class representing a firebase data snapshot.  It wraps a SnapshotNode and
 * surfaces the public methods (val, forEach, etc.) we want to expose.
 */
export declare class DataSnapshot ***REMOVED***
    private readonly node_;
    private readonly ref_;
    private readonly index_;
    /**
     * @param ***REMOVED***!Node***REMOVED*** node_ A SnapshotNode to wrap.
     * @param ***REMOVED***!Reference***REMOVED*** ref_ The ref of the location this snapshot came from.
     * @param ***REMOVED***!Index***REMOVED*** index_ The iteration order for this snapshot
     */
    constructor(node_: Node, ref_: Reference, index_: Index);
    /**
     * Retrieves the snapshot contents as JSON.  Returns null if the snapshot is
     * empty.
     *
     * @return ***REMOVED*******REMOVED*** JSON representation of the DataSnapshot contents, or null if empty.
     */
    val(): any;
    /**
     * Returns the snapshot contents as JSON, including priorities of node.  Suitable for exporting
     * the entire node contents.
     * @return ***REMOVED*******REMOVED*** JSON representation of the DataSnapshot contents, or null if empty.
     */
    exportVal(): any;
    toJSON(): any;
    /**
     * Returns whether the snapshot contains a non-null value.
     *
     * @return ***REMOVED***boolean***REMOVED*** Whether the snapshot contains a non-null value, or is empty.
     */
    exists(): boolean;
    /**
     * Returns a DataSnapshot of the specified child node's contents.
     *
     * @param ***REMOVED***!string***REMOVED*** childPathString Path to a child.
     * @return ***REMOVED***!DataSnapshot***REMOVED*** DataSnapshot for child node.
     */
    child(childPathString: string): DataSnapshot;
    /**
     * Returns whether the snapshot contains a child at the specified path.
     *
     * @param ***REMOVED***!string***REMOVED*** childPathString Path to a child.
     * @return ***REMOVED***boolean***REMOVED*** Whether the child exists.
     */
    hasChild(childPathString: string): boolean;
    /**
     * Returns the priority of the object, or null if no priority was set.
     *
     * @return ***REMOVED***string|number|null***REMOVED*** The priority.
     */
    getPriority(): string | number | null;
    /**
     * Iterates through child nodes and calls the specified action for each one.
     *
     * @param ***REMOVED***function(!DataSnapshot)***REMOVED*** action Callback function to be called
     * for each child.
     * @return ***REMOVED***boolean***REMOVED*** True if forEach was canceled by action returning true for
     * one of the child nodes.
     */
    forEach(action: (d: DataSnapshot) => void): boolean;
    /**
     * Returns whether this DataSnapshot has children.
     * @return ***REMOVED***boolean***REMOVED*** True if the DataSnapshot contains 1 or more child nodes.
     */
    hasChildren(): boolean;
    readonly key: string;
    /**
     * Returns the number of children for this DataSnapshot.
     * @return ***REMOVED***number***REMOVED*** The number of children that this DataSnapshot contains.
     */
    numChildren(): number;
    /**
     * @return ***REMOVED***Reference***REMOVED*** The Firebase reference for the location this snapshot's data came from.
     */
    getRef(): Reference;
    readonly ref: Reference;
***REMOVED***
