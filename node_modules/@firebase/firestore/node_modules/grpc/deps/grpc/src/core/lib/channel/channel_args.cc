/*
 *
 * Copyright 2015 gRPC authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 */

#include <grpc/support/port_platform.h>

#include <limits.h>
#include <string.h>

#include <grpc/compression.h>
#include <grpc/grpc.h>
#include <grpc/support/alloc.h>
#include <grpc/support/log.h>
#include <grpc/support/string_util.h>
#include <grpc/support/useful.h>

#include "src/core/lib/channel/channel_args.h"
#include "src/core/lib/support/string.h"

static grpc_arg copy_arg(const grpc_arg* src) ***REMOVED***
  grpc_arg dst;
  dst.type = src->type;
  dst.key = gpr_strdup(src->key);
  switch (dst.type) ***REMOVED***
    case GRPC_ARG_STRING:
      dst.value.string = gpr_strdup(src->value.string);
      break;
    case GRPC_ARG_INTEGER:
      dst.value.integer = src->value.integer;
      break;
    case GRPC_ARG_POINTER:
      dst.value.pointer = src->value.pointer;
      dst.value.pointer.p =
          src->value.pointer.vtable->copy(src->value.pointer.p);
      break;
  ***REMOVED***
  return dst;
***REMOVED***

grpc_channel_args* grpc_channel_args_copy_and_add(const grpc_channel_args* src,
                                                  const grpc_arg* to_add,
                                                  size_t num_to_add) ***REMOVED***
  return grpc_channel_args_copy_and_add_and_remove(src, nullptr, 0, to_add,
                                                   num_to_add);
***REMOVED***

grpc_channel_args* grpc_channel_args_copy_and_remove(
    const grpc_channel_args* src, const char** to_remove,
    size_t num_to_remove) ***REMOVED***
  return grpc_channel_args_copy_and_add_and_remove(src, to_remove,
                                                   num_to_remove, nullptr, 0);
***REMOVED***

static bool should_remove_arg(const grpc_arg* arg, const char** to_remove,
                              size_t num_to_remove) ***REMOVED***
  for (size_t i = 0; i < num_to_remove; ++i) ***REMOVED***
    if (strcmp(arg->key, to_remove[i]) == 0) return true;
  ***REMOVED***
  return false;
***REMOVED***

grpc_channel_args* grpc_channel_args_copy_and_add_and_remove(
    const grpc_channel_args* src, const char** to_remove, size_t num_to_remove,
    const grpc_arg* to_add, size_t num_to_add) ***REMOVED***
  // Figure out how many args we'll be copying.
  size_t num_args_to_copy = 0;
  if (src != nullptr) ***REMOVED***
    for (size_t i = 0; i < src->num_args; ++i) ***REMOVED***
      if (!should_remove_arg(&src->args[i], to_remove, num_to_remove)) ***REMOVED***
        ++num_args_to_copy;
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
  // Create result.
  grpc_channel_args* dst =
      (grpc_channel_args*)gpr_malloc(sizeof(grpc_channel_args));
  dst->num_args = num_args_to_copy + num_to_add;
  if (dst->num_args == 0) ***REMOVED***
    dst->args = nullptr;
    return dst;
  ***REMOVED***
  dst->args = (grpc_arg*)gpr_malloc(sizeof(grpc_arg) * dst->num_args);
  // Copy args from src that are not being removed.
  size_t dst_idx = 0;
  if (src != nullptr) ***REMOVED***
    for (size_t i = 0; i < src->num_args; ++i) ***REMOVED***
      if (!should_remove_arg(&src->args[i], to_remove, num_to_remove)) ***REMOVED***
        dst->args[dst_idx++] = copy_arg(&src->args[i]);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
  // Add args from to_add.
  for (size_t i = 0; i < num_to_add; ++i) ***REMOVED***
    dst->args[dst_idx++] = copy_arg(&to_add[i]);
  ***REMOVED***
  GPR_ASSERT(dst_idx == dst->num_args);
  return dst;
***REMOVED***

grpc_channel_args* grpc_channel_args_copy(const grpc_channel_args* src) ***REMOVED***
  return grpc_channel_args_copy_and_add(src, nullptr, 0);
***REMOVED***

grpc_channel_args* grpc_channel_args_union(const grpc_channel_args* a,
                                           const grpc_channel_args* b) ***REMOVED***
  const size_t max_out = (a->num_args + b->num_args);
  grpc_arg* uniques = (grpc_arg*)gpr_malloc(sizeof(*uniques) * max_out);
  for (size_t i = 0; i < a->num_args; ++i) uniques[i] = a->args[i];

  size_t uniques_idx = a->num_args;
  for (size_t i = 0; i < b->num_args; ++i) ***REMOVED***
    const char* b_key = b->args[i].key;
    if (grpc_channel_args_find(a, b_key) == nullptr) ***REMOVED***  // not found
      uniques[uniques_idx++] = b->args[i];
    ***REMOVED***
  ***REMOVED***
  grpc_channel_args* result =
      grpc_channel_args_copy_and_add(nullptr, uniques, uniques_idx);
  gpr_free(uniques);
  return result;
***REMOVED***

static int cmp_arg(const grpc_arg* a, const grpc_arg* b) ***REMOVED***
  int c = GPR_ICMP(a->type, b->type);
  if (c != 0) return c;
  c = strcmp(a->key, b->key);
  if (c != 0) return c;
  switch (a->type) ***REMOVED***
    case GRPC_ARG_STRING:
      return strcmp(a->value.string, b->value.string);
    case GRPC_ARG_INTEGER:
      return GPR_ICMP(a->value.integer, b->value.integer);
    case GRPC_ARG_POINTER:
      c = GPR_ICMP(a->value.pointer.p, b->value.pointer.p);
      if (c != 0) ***REMOVED***
        c = GPR_ICMP(a->value.pointer.vtable, b->value.pointer.vtable);
        if (c == 0) ***REMOVED***
          c = a->value.pointer.vtable->cmp(a->value.pointer.p,
                                           b->value.pointer.p);
        ***REMOVED***
      ***REMOVED***
      return c;
  ***REMOVED***
  GPR_UNREACHABLE_CODE(return 0);
***REMOVED***

/* stabilizing comparison function: since channel_args ordering matters for
 * keys with the same name, we need to preserve that ordering */
static int cmp_key_stable(const void* ap, const void* bp) ***REMOVED***
  const grpc_arg* const* a = (const grpc_arg* const*)ap;
  const grpc_arg* const* b = (const grpc_arg* const*)bp;
  int c = strcmp((*a)->key, (*b)->key);
  if (c == 0) c = GPR_ICMP(*a, *b);
  return c;
***REMOVED***

grpc_channel_args* grpc_channel_args_normalize(const grpc_channel_args* a) ***REMOVED***
  grpc_arg** args = (grpc_arg**)gpr_malloc(sizeof(grpc_arg*) * a->num_args);
  for (size_t i = 0; i < a->num_args; i++) ***REMOVED***
    args[i] = &a->args[i];
  ***REMOVED***
  if (a->num_args > 1)
    qsort(args, a->num_args, sizeof(grpc_arg*), cmp_key_stable);

  grpc_channel_args* b =
      (grpc_channel_args*)gpr_malloc(sizeof(grpc_channel_args));
  b->num_args = a->num_args;
  b->args = (grpc_arg*)gpr_malloc(sizeof(grpc_arg) * b->num_args);
  for (size_t i = 0; i < a->num_args; i++) ***REMOVED***
    b->args[i] = copy_arg(args[i]);
  ***REMOVED***

  gpr_free(args);
  return b;
***REMOVED***

void grpc_channel_args_destroy(grpc_exec_ctx* exec_ctx, grpc_channel_args* a) ***REMOVED***
  size_t i;
  if (!a) return;
  for (i = 0; i < a->num_args; i++) ***REMOVED***
    switch (a->args[i].type) ***REMOVED***
      case GRPC_ARG_STRING:
        gpr_free(a->args[i].value.string);
        break;
      case GRPC_ARG_INTEGER:
        break;
      case GRPC_ARG_POINTER:
        a->args[i].value.pointer.vtable->destroy(exec_ctx,
                                                 a->args[i].value.pointer.p);
        break;
    ***REMOVED***
    gpr_free(a->args[i].key);
  ***REMOVED***
  gpr_free(a->args);
  gpr_free(a);
***REMOVED***

grpc_compression_algorithm grpc_channel_args_get_compression_algorithm(
    const grpc_channel_args* a) ***REMOVED***
  size_t i;
  if (a == nullptr) return GRPC_COMPRESS_NONE;
  for (i = 0; i < a->num_args; ++i) ***REMOVED***
    if (a->args[i].type == GRPC_ARG_INTEGER &&
        !strcmp(GRPC_COMPRESSION_CHANNEL_DEFAULT_ALGORITHM, a->args[i].key)) ***REMOVED***
      return (grpc_compression_algorithm)a->args[i].value.integer;
      break;
    ***REMOVED***
  ***REMOVED***
  return GRPC_COMPRESS_NONE;
***REMOVED***

grpc_stream_compression_algorithm
grpc_channel_args_get_stream_compression_algorithm(const grpc_channel_args* a) ***REMOVED***
  size_t i;
  if (a == nullptr) return GRPC_STREAM_COMPRESS_NONE;
  for (i = 0; i < a->num_args; ++i) ***REMOVED***
    if (a->args[i].type == GRPC_ARG_INTEGER &&
        !strcmp(GRPC_STREAM_COMPRESSION_CHANNEL_DEFAULT_ALGORITHM,
                a->args[i].key)) ***REMOVED***
      return (grpc_stream_compression_algorithm)a->args[i].value.integer;
      break;
    ***REMOVED***
  ***REMOVED***
  return GRPC_STREAM_COMPRESS_NONE;
***REMOVED***

grpc_channel_args* grpc_channel_args_set_compression_algorithm(
    grpc_channel_args* a, grpc_compression_algorithm algorithm) ***REMOVED***
  GPR_ASSERT(algorithm < GRPC_COMPRESS_ALGORITHMS_COUNT);
  grpc_arg tmp;
  tmp.type = GRPC_ARG_INTEGER;
  tmp.key = (char*)GRPC_COMPRESSION_CHANNEL_DEFAULT_ALGORITHM;
  tmp.value.integer = algorithm;
  return grpc_channel_args_copy_and_add(a, &tmp, 1);
***REMOVED***

grpc_channel_args* grpc_channel_args_set_stream_compression_algorithm(
    grpc_channel_args* a, grpc_stream_compression_algorithm algorithm) ***REMOVED***
  GPR_ASSERT(algorithm < GRPC_STREAM_COMPRESS_ALGORITHMS_COUNT);
  grpc_arg tmp;
  tmp.type = GRPC_ARG_INTEGER;
  tmp.key = (char*)GRPC_STREAM_COMPRESSION_CHANNEL_DEFAULT_ALGORITHM;
  tmp.value.integer = algorithm;
  return grpc_channel_args_copy_and_add(a, &tmp, 1);
***REMOVED***

/** Returns 1 if the argument for compression algorithm's enabled states bitset
 * was found in \a a, returning the arg's value in \a states. Otherwise, returns
 * 0. */
static int find_compression_algorithm_states_bitset(const grpc_channel_args* a,
                                                    int** states_arg) ***REMOVED***
  if (a != nullptr) ***REMOVED***
    size_t i;
    for (i = 0; i < a->num_args; ++i) ***REMOVED***
      if (a->args[i].type == GRPC_ARG_INTEGER &&
          !strcmp(GRPC_COMPRESSION_CHANNEL_ENABLED_ALGORITHMS_BITSET,
                  a->args[i].key)) ***REMOVED***
        *states_arg = &a->args[i].value.integer;
        **states_arg |= 0x1; /* forcefully enable support for no compression */
        return 1;
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
  return 0; /* GPR_FALSE */
***REMOVED***

/** Returns 1 if the argument for compression algorithm's enabled states bitset
 * was found in \a a, returning the arg's value in \a states. Otherwise, returns
 * 0. */
static int find_stream_compression_algorithm_states_bitset(
    const grpc_channel_args* a, int** states_arg) ***REMOVED***
  if (a != nullptr) ***REMOVED***
    size_t i;
    for (i = 0; i < a->num_args; ++i) ***REMOVED***
      if (a->args[i].type == GRPC_ARG_INTEGER &&
          !strcmp(GRPC_STREAM_COMPRESSION_CHANNEL_ENABLED_ALGORITHMS_BITSET,
                  a->args[i].key)) ***REMOVED***
        *states_arg = &a->args[i].value.integer;
        **states_arg |= 0x1; /* forcefully enable support for no compression */
        return 1;
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
  return 0; /* GPR_FALSE */
***REMOVED***

grpc_channel_args* grpc_channel_args_compression_algorithm_set_state(
    grpc_exec_ctx* exec_ctx, grpc_channel_args** a,
    grpc_compression_algorithm algorithm, int state) ***REMOVED***
  int* states_arg = nullptr;
  grpc_channel_args* result = *a;
  const int states_arg_found =
      find_compression_algorithm_states_bitset(*a, &states_arg);

  if (grpc_channel_args_get_compression_algorithm(*a) == algorithm &&
      state == 0) ***REMOVED***
    const char* algo_name = nullptr;
    GPR_ASSERT(grpc_compression_algorithm_name(algorithm, &algo_name) != 0);
    gpr_log(GPR_ERROR,
            "Tried to disable default compression algorithm '%s'. The "
            "operation has been ignored.",
            algo_name);
  ***REMOVED*** else if (states_arg_found) ***REMOVED***
    if (state != 0) ***REMOVED***
      GPR_BITSET((unsigned*)states_arg, algorithm);
    ***REMOVED*** else if (algorithm != GRPC_COMPRESS_NONE) ***REMOVED***
      GPR_BITCLEAR((unsigned*)states_arg, algorithm);
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    /* create a new arg */
    grpc_arg tmp;
    tmp.type = GRPC_ARG_INTEGER;
    tmp.key = (char*)GRPC_COMPRESSION_CHANNEL_ENABLED_ALGORITHMS_BITSET;
    /* all enabled by default */
    tmp.value.integer = (1u << GRPC_COMPRESS_ALGORITHMS_COUNT) - 1;
    if (state != 0) ***REMOVED***
      GPR_BITSET((unsigned*)&tmp.value.integer, algorithm);
    ***REMOVED*** else if (algorithm != GRPC_COMPRESS_NONE) ***REMOVED***
      GPR_BITCLEAR((unsigned*)&tmp.value.integer, algorithm);
    ***REMOVED***
    result = grpc_channel_args_copy_and_add(*a, &tmp, 1);
    grpc_channel_args_destroy(exec_ctx, *a);
    *a = result;
  ***REMOVED***
  return result;
***REMOVED***

grpc_channel_args* grpc_channel_args_stream_compression_algorithm_set_state(
    grpc_exec_ctx* exec_ctx, grpc_channel_args** a,
    grpc_stream_compression_algorithm algorithm, int state) ***REMOVED***
  int* states_arg = nullptr;
  grpc_channel_args* result = *a;
  const int states_arg_found =
      find_stream_compression_algorithm_states_bitset(*a, &states_arg);

  if (grpc_channel_args_get_stream_compression_algorithm(*a) == algorithm &&
      state == 0) ***REMOVED***
    const char* algo_name = nullptr;
    GPR_ASSERT(grpc_stream_compression_algorithm_name(algorithm, &algo_name) !=
               0);
    gpr_log(GPR_ERROR,
            "Tried to disable default stream compression algorithm '%s'. The "
            "operation has been ignored.",
            algo_name);
  ***REMOVED*** else if (states_arg_found) ***REMOVED***
    if (state != 0) ***REMOVED***
      GPR_BITSET((unsigned*)states_arg, algorithm);
    ***REMOVED*** else if (algorithm != GRPC_STREAM_COMPRESS_NONE) ***REMOVED***
      GPR_BITCLEAR((unsigned*)states_arg, algorithm);
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    /* create a new arg */
    grpc_arg tmp;
    tmp.type = GRPC_ARG_INTEGER;
    tmp.key = (char*)GRPC_STREAM_COMPRESSION_CHANNEL_ENABLED_ALGORITHMS_BITSET;
    /* all enabled by default */
    tmp.value.integer = (1u << GRPC_STREAM_COMPRESS_ALGORITHMS_COUNT) - 1;
    if (state != 0) ***REMOVED***
      GPR_BITSET((unsigned*)&tmp.value.integer, algorithm);
    ***REMOVED*** else if (algorithm != GRPC_STREAM_COMPRESS_NONE) ***REMOVED***
      GPR_BITCLEAR((unsigned*)&tmp.value.integer, algorithm);
    ***REMOVED***
    result = grpc_channel_args_copy_and_add(*a, &tmp, 1);
    grpc_channel_args_destroy(exec_ctx, *a);
    *a = result;
  ***REMOVED***
  return result;
***REMOVED***

uint32_t grpc_channel_args_compression_algorithm_get_states(
    const grpc_channel_args* a) ***REMOVED***
  int* states_arg;
  if (find_compression_algorithm_states_bitset(a, &states_arg)) ***REMOVED***
    return (uint32_t)*states_arg;
  ***REMOVED*** else ***REMOVED***
    return (1u << GRPC_COMPRESS_ALGORITHMS_COUNT) - 1; /* All algs. enabled */
  ***REMOVED***
***REMOVED***

uint32_t grpc_channel_args_stream_compression_algorithm_get_states(
    const grpc_channel_args* a) ***REMOVED***
  int* states_arg;
  if (find_stream_compression_algorithm_states_bitset(a, &states_arg)) ***REMOVED***
    return (uint32_t)*states_arg;
  ***REMOVED*** else ***REMOVED***
    return (1u << GRPC_STREAM_COMPRESS_ALGORITHMS_COUNT) -
           1; /* All algs. enabled */
  ***REMOVED***
***REMOVED***

grpc_channel_args* grpc_channel_args_set_socket_mutator(
    grpc_channel_args* a, grpc_socket_mutator* mutator) ***REMOVED***
  grpc_arg tmp = grpc_socket_mutator_to_arg(mutator);
  return grpc_channel_args_copy_and_add(a, &tmp, 1);
***REMOVED***

int grpc_channel_args_compare(const grpc_channel_args* a,
                              const grpc_channel_args* b) ***REMOVED***
  int c = GPR_ICMP(a->num_args, b->num_args);
  if (c != 0) return c;
  for (size_t i = 0; i < a->num_args; i++) ***REMOVED***
    c = cmp_arg(&a->args[i], &b->args[i]);
    if (c != 0) return c;
  ***REMOVED***
  return 0;
***REMOVED***

const grpc_arg* grpc_channel_args_find(const grpc_channel_args* args,
                                       const char* name) ***REMOVED***
  if (args != nullptr) ***REMOVED***
    for (size_t i = 0; i < args->num_args; ++i) ***REMOVED***
      if (strcmp(args->args[i].key, name) == 0) ***REMOVED***
        return &args->args[i];
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
  return nullptr;
***REMOVED***

int grpc_channel_arg_get_integer(const grpc_arg* arg,
                                 const grpc_integer_options options) ***REMOVED***
  if (arg == nullptr) return options.default_value;
  if (arg->type != GRPC_ARG_INTEGER) ***REMOVED***
    gpr_log(GPR_ERROR, "%s ignored: it must be an integer", arg->key);
    return options.default_value;
  ***REMOVED***
  if (arg->value.integer < options.min_value) ***REMOVED***
    gpr_log(GPR_ERROR, "%s ignored: it must be >= %d", arg->key,
            options.min_value);
    return options.default_value;
  ***REMOVED***
  if (arg->value.integer > options.max_value) ***REMOVED***
    gpr_log(GPR_ERROR, "%s ignored: it must be <= %d", arg->key,
            options.max_value);
    return options.default_value;
  ***REMOVED***
  return arg->value.integer;
***REMOVED***

bool grpc_channel_arg_get_bool(const grpc_arg* arg, bool default_value) ***REMOVED***
  if (arg == nullptr) return default_value;
  if (arg->type != GRPC_ARG_INTEGER) ***REMOVED***
    gpr_log(GPR_ERROR, "%s ignored: it must be an integer", arg->key);
    return default_value;
  ***REMOVED***
  switch (arg->value.integer) ***REMOVED***
    case 0:
      return false;
    case 1:
      return true;
    default:
      gpr_log(GPR_ERROR, "%s treated as bool but set to %d (assuming true)",
              arg->key, arg->value.integer);
      return true;
  ***REMOVED***
***REMOVED***

bool grpc_channel_args_want_minimal_stack(const grpc_channel_args* args) ***REMOVED***
  return grpc_channel_arg_get_bool(
      grpc_channel_args_find(args, GRPC_ARG_MINIMAL_STACK), false);
***REMOVED***

grpc_arg grpc_channel_arg_string_create(char* name, char* value) ***REMOVED***
  grpc_arg arg;
  arg.type = GRPC_ARG_STRING;
  arg.key = name;
  arg.value.string = value;
  return arg;
***REMOVED***

grpc_arg grpc_channel_arg_integer_create(char* name, int value) ***REMOVED***
  grpc_arg arg;
  arg.type = GRPC_ARG_INTEGER;
  arg.key = name;
  arg.value.integer = value;
  return arg;
***REMOVED***

grpc_arg grpc_channel_arg_pointer_create(
    char* name, void* value, const grpc_arg_pointer_vtable* vtable) ***REMOVED***
  grpc_arg arg;
  arg.type = GRPC_ARG_POINTER;
  arg.key = name;
  arg.value.pointer.p = value;
  arg.value.pointer.vtable = vtable;
  return arg;
***REMOVED***
