/*
 *
 * Copyright 2015 gRPC authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 */

#include "src/core/lib/debug/trace.h"

#include <string.h>

#include <grpc/grpc.h>
#include <grpc/support/alloc.h>
#include <grpc/support/log.h>
#include "src/core/lib/support/env.h"

int grpc_tracer_set_enabled(const char* name, int enabled);

namespace grpc_core ***REMOVED***

TraceFlag* TraceFlagList::root_tracer_ = nullptr;

bool TraceFlagList::Set(const char* name, bool enabled) ***REMOVED***
  TraceFlag* t;
  if (0 == strcmp(name, "all")) ***REMOVED***
    for (t = root_tracer_; t; t = t->next_tracer_) ***REMOVED***
      t->set_enabled(enabled);
    ***REMOVED***
  ***REMOVED*** else if (0 == strcmp(name, "list_tracers")) ***REMOVED***
    LogAllTracers();
  ***REMOVED*** else if (0 == strcmp(name, "refcount")) ***REMOVED***
    for (t = root_tracer_; t; t = t->next_tracer_) ***REMOVED***
      if (strstr(t->name_, "refcount") != nullptr) ***REMOVED***
        t->set_enabled(enabled);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    bool found = false;
    for (t = root_tracer_; t; t = t->next_tracer_) ***REMOVED***
      if (0 == strcmp(name, t->name_)) ***REMOVED***
        t->set_enabled(enabled);
        found = true;
      ***REMOVED***
    ***REMOVED***
    if (!found) ***REMOVED***
      gpr_log(GPR_ERROR, "Unknown trace var: '%s'", name);
      return false; /* early return */
    ***REMOVED***
  ***REMOVED***
  return true;
***REMOVED***

void TraceFlagList::Add(TraceFlag* flag) ***REMOVED***
  flag->next_tracer_ = root_tracer_;
  root_tracer_ = flag;
***REMOVED***

void TraceFlagList::LogAllTracers() ***REMOVED***
  gpr_log(GPR_DEBUG, "available tracers:");
  TraceFlag* t;
  for (t = root_tracer_; t != nullptr; t = t->next_tracer_) ***REMOVED***
    gpr_log(GPR_DEBUG, "\t%s", t->name_);
  ***REMOVED***
***REMOVED***

// Flags register themselves on the list during construction
TraceFlag::TraceFlag(bool default_enabled, const char* name) : name_(name) ***REMOVED***
  set_enabled(default_enabled);
  TraceFlagList::Add(this);
***REMOVED***

***REMOVED***  // namespace grpc_core

static void add(const char* beg, const char* end, char*** ss, size_t* ns) ***REMOVED***
  size_t n = *ns;
  size_t np = n + 1;
  char* s;
  size_t len;
  GPR_ASSERT(end >= beg);
  len = (size_t)(end - beg);
  s = (char*)gpr_malloc(len + 1);
  memcpy(s, beg, len);
  s[len] = 0;
  *ss = (char**)gpr_realloc(*ss, sizeof(char**) * np);
  (*ss)[n] = s;
  *ns = np;
***REMOVED***

static void split(const char* s, char*** ss, size_t* ns) ***REMOVED***
  const char* c = strchr(s, ',');
  if (c == nullptr) ***REMOVED***
    add(s, s + strlen(s), ss, ns);
  ***REMOVED*** else ***REMOVED***
    add(s, c, ss, ns);
    split(c + 1, ss, ns);
  ***REMOVED***
***REMOVED***

static void parse(const char* s) ***REMOVED***
  char** strings = nullptr;
  size_t nstrings = 0;
  size_t i;
  split(s, &strings, &nstrings);

  for (i = 0; i < nstrings; i++) ***REMOVED***
    if (strings[i][0] == '-') ***REMOVED***
      grpc_core::TraceFlagList::Set(strings[i] + 1, false);
    ***REMOVED*** else ***REMOVED***
      grpc_core::TraceFlagList::Set(strings[i], true);
    ***REMOVED***
  ***REMOVED***

  for (i = 0; i < nstrings; i++) ***REMOVED***
    gpr_free(strings[i]);
  ***REMOVED***
  gpr_free(strings);
***REMOVED***

void grpc_tracer_init(const char* env_var) ***REMOVED***
  char* e = gpr_getenv(env_var);
  if (e != nullptr) ***REMOVED***
    parse(e);
    gpr_free(e);
  ***REMOVED***
***REMOVED***

void grpc_tracer_shutdown(void) ***REMOVED******REMOVED***

int grpc_tracer_set_enabled(const char* name, int enabled) ***REMOVED***
  return grpc_core::TraceFlagList::Set(name, enabled != 0);
***REMOVED***
