/*
 *
 * Copyright 2015 gRPC authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 */

#include "src/core/lib/iomgr/exec_ctx.h"

#include <grpc/support/log.h>
#include <grpc/support/sync.h>
#include <grpc/support/thd.h>

#include "src/core/lib/iomgr/combiner.h"
#include "src/core/lib/profiling/timers.h"

bool grpc_exec_ctx_ready_to_finish(grpc_exec_ctx* exec_ctx) ***REMOVED***
  if ((exec_ctx->flags & GRPC_EXEC_CTX_FLAG_IS_FINISHED) == 0) ***REMOVED***
    if (exec_ctx->check_ready_to_finish(exec_ctx,
                                        exec_ctx->check_ready_to_finish_arg)) ***REMOVED***
      exec_ctx->flags |= GRPC_EXEC_CTX_FLAG_IS_FINISHED;
      return true;
    ***REMOVED***
    return false;
  ***REMOVED*** else ***REMOVED***
    return true;
  ***REMOVED***
***REMOVED***

bool grpc_never_ready_to_finish(grpc_exec_ctx* exec_ctx, void* arg_ignored) ***REMOVED***
  return false;
***REMOVED***

bool grpc_always_ready_to_finish(grpc_exec_ctx* exec_ctx, void* arg_ignored) ***REMOVED***
  return true;
***REMOVED***

bool grpc_exec_ctx_has_work(grpc_exec_ctx* exec_ctx) ***REMOVED***
  return exec_ctx->active_combiner != nullptr ||
         !grpc_closure_list_empty(exec_ctx->closure_list);
***REMOVED***

void grpc_exec_ctx_finish(grpc_exec_ctx* exec_ctx) ***REMOVED***
  exec_ctx->flags |= GRPC_EXEC_CTX_FLAG_IS_FINISHED;
  grpc_exec_ctx_flush(exec_ctx);
***REMOVED***

static void exec_ctx_run(grpc_exec_ctx* exec_ctx, grpc_closure* closure,
                         grpc_error* error) ***REMOVED***
#ifndef NDEBUG
  closure->scheduled = false;
  if (grpc_trace_closure.enabled()) ***REMOVED***
    gpr_log(GPR_DEBUG, "running closure %p: created [%s:%d]: %s [%s:%d]",
            closure, closure->file_created, closure->line_created,
            closure->run ? "run" : "scheduled", closure->file_initiated,
            closure->line_initiated);
  ***REMOVED***
#endif
  closure->cb(exec_ctx, closure->cb_arg, error);
#ifndef NDEBUG
  if (grpc_trace_closure.enabled()) ***REMOVED***
    gpr_log(GPR_DEBUG, "closure %p finished", closure);
  ***REMOVED***
#endif
  GRPC_ERROR_UNREF(error);
***REMOVED***

bool grpc_exec_ctx_flush(grpc_exec_ctx* exec_ctx) ***REMOVED***
  bool did_something = 0;
  GPR_TIMER_BEGIN("grpc_exec_ctx_flush", 0);
  for (;;) ***REMOVED***
    if (!grpc_closure_list_empty(exec_ctx->closure_list)) ***REMOVED***
      grpc_closure* c = exec_ctx->closure_list.head;
      exec_ctx->closure_list.head = exec_ctx->closure_list.tail = nullptr;
      while (c != nullptr) ***REMOVED***
        grpc_closure* next = c->next_data.next;
        grpc_error* error = c->error_data.error;
        did_something = true;
        exec_ctx_run(exec_ctx, c, error);
        c = next;
      ***REMOVED***
    ***REMOVED*** else if (!grpc_combiner_continue_exec_ctx(exec_ctx)) ***REMOVED***
      break;
    ***REMOVED***
  ***REMOVED***
  GPR_ASSERT(exec_ctx->active_combiner == nullptr);
  GPR_TIMER_END("grpc_exec_ctx_flush", 0);
  return did_something;
***REMOVED***

static void exec_ctx_sched(grpc_exec_ctx* exec_ctx, grpc_closure* closure,
                           grpc_error* error) ***REMOVED***
  grpc_closure_list_append(&exec_ctx->closure_list, closure, error);
***REMOVED***

static gpr_timespec g_start_time;

void grpc_exec_ctx_global_init(void) ***REMOVED***
  g_start_time = gpr_now(GPR_CLOCK_MONOTONIC);
***REMOVED***

void grpc_exec_ctx_global_shutdown(void) ***REMOVED******REMOVED***

static gpr_atm timespec_to_atm_round_down(gpr_timespec ts) ***REMOVED***
  ts = gpr_time_sub(ts, g_start_time);
  double x =
      GPR_MS_PER_SEC * (double)ts.tv_sec + (double)ts.tv_nsec / GPR_NS_PER_MS;
  if (x < 0) return 0;
  if (x > GPR_ATM_MAX) return GPR_ATM_MAX;
  return (gpr_atm)x;
***REMOVED***

static gpr_atm timespec_to_atm_round_up(gpr_timespec ts) ***REMOVED***
  ts = gpr_time_sub(ts, g_start_time);
  double x = GPR_MS_PER_SEC * (double)ts.tv_sec +
             (double)ts.tv_nsec / GPR_NS_PER_MS +
             (double)(GPR_NS_PER_SEC - 1) / (double)GPR_NS_PER_SEC;
  if (x < 0) return 0;
  if (x > GPR_ATM_MAX) return GPR_ATM_MAX;
  return (gpr_atm)x;
***REMOVED***

grpc_millis grpc_exec_ctx_now(grpc_exec_ctx* exec_ctx) ***REMOVED***
  if (!exec_ctx->now_is_valid) ***REMOVED***
    exec_ctx->now = timespec_to_atm_round_down(gpr_now(GPR_CLOCK_MONOTONIC));
    exec_ctx->now_is_valid = true;
  ***REMOVED***
  return exec_ctx->now;
***REMOVED***

void grpc_exec_ctx_invalidate_now(grpc_exec_ctx* exec_ctx) ***REMOVED***
  exec_ctx->now_is_valid = false;
***REMOVED***

gpr_timespec grpc_millis_to_timespec(grpc_millis millis,
                                     gpr_clock_type clock_type) ***REMOVED***
  // special-case infinities as grpc_millis can be 32bit on some platforms
  // while gpr_time_from_millis always takes an int64_t.
  if (millis == GRPC_MILLIS_INF_FUTURE) ***REMOVED***
    return gpr_inf_future(clock_type);
  ***REMOVED***
  if (millis == GRPC_MILLIS_INF_PAST) ***REMOVED***
    return gpr_inf_past(clock_type);
  ***REMOVED***

  if (clock_type == GPR_TIMESPAN) ***REMOVED***
    return gpr_time_from_millis(millis, GPR_TIMESPAN);
  ***REMOVED***
  return gpr_time_add(gpr_convert_clock_type(g_start_time, clock_type),
                      gpr_time_from_millis(millis, GPR_TIMESPAN));
***REMOVED***

grpc_millis grpc_timespec_to_millis_round_down(gpr_timespec ts) ***REMOVED***
  return timespec_to_atm_round_down(
      gpr_convert_clock_type(ts, g_start_time.clock_type));
***REMOVED***

grpc_millis grpc_timespec_to_millis_round_up(gpr_timespec ts) ***REMOVED***
  return timespec_to_atm_round_up(
      gpr_convert_clock_type(ts, g_start_time.clock_type));
***REMOVED***

static const grpc_closure_scheduler_vtable exec_ctx_scheduler_vtable = ***REMOVED***
    exec_ctx_run, exec_ctx_sched, "exec_ctx"***REMOVED***;
static grpc_closure_scheduler exec_ctx_scheduler = ***REMOVED***&exec_ctx_scheduler_vtable***REMOVED***;
grpc_closure_scheduler* grpc_schedule_on_exec_ctx = &exec_ctx_scheduler;
