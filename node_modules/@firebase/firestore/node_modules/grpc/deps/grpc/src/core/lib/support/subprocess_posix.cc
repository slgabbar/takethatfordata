/*
 *
 * Copyright 2015 gRPC authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 */

#include <grpc/support/port_platform.h>

#ifdef GPR_POSIX_SUBPROCESS

#include <grpc/support/subprocess.h>

#include <assert.h>
#include <errno.h>
#include <signal.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <unistd.h>

#include <grpc/support/alloc.h>
#include <grpc/support/log.h>

struct gpr_subprocess ***REMOVED***
  int pid;
  bool joined;
***REMOVED***;

const char* gpr_subprocess_binary_extension() ***REMOVED*** return ""; ***REMOVED***

gpr_subprocess* gpr_subprocess_create(int argc, const char** argv) ***REMOVED***
  gpr_subprocess* r;
  int pid;
  char** exec_args;

  pid = fork();
  if (pid == -1) ***REMOVED***
    return nullptr;
  ***REMOVED*** else if (pid == 0) ***REMOVED***
    exec_args = (char**)gpr_malloc(((size_t)argc + 1) * sizeof(char*));
    memcpy(exec_args, argv, (size_t)argc * sizeof(char*));
    exec_args[argc] = nullptr;
    execv(exec_args[0], exec_args);
    /* if we reach here, an error has occurred */
    gpr_log(GPR_ERROR, "execv '%s' failed: %s", exec_args[0], strerror(errno));
    _exit(1);
    return nullptr;
  ***REMOVED*** else ***REMOVED***
    r = (gpr_subprocess*)gpr_zalloc(sizeof(gpr_subprocess));
    r->pid = pid;
    return r;
  ***REMOVED***
***REMOVED***

void gpr_subprocess_destroy(gpr_subprocess* p) ***REMOVED***
  if (!p->joined) ***REMOVED***
    kill(p->pid, SIGKILL);
    gpr_subprocess_join(p);
  ***REMOVED***
  gpr_free(p);
***REMOVED***

int gpr_subprocess_join(gpr_subprocess* p) ***REMOVED***
  int status;
retry:
  if (waitpid(p->pid, &status, 0) == -1) ***REMOVED***
    if (errno == EINTR) ***REMOVED***
      goto retry;
    ***REMOVED***
    gpr_log(GPR_ERROR, "waitpid failed for pid %d: %s", p->pid,
            strerror(errno));
    return -1;
  ***REMOVED***
  p->joined = true;
  return status;
***REMOVED***

void gpr_subprocess_interrupt(gpr_subprocess* p) ***REMOVED***
  if (!p->joined) ***REMOVED***
    kill(p->pid, SIGINT);
  ***REMOVED***
***REMOVED***

#endif /* GPR_POSIX_SUBPROCESS */
