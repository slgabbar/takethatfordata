/*
 *
 * Copyright 2016 gRPC authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 */

#include <grpc/support/port_platform.h>

#ifdef GPR_WINDOWS_SUBPROCESS

#include <string.h>
#include <tchar.h>
#include <windows.h>

#include <grpc/support/alloc.h>
#include <grpc/support/log.h>
#include <grpc/support/subprocess.h>
#include "src/core/lib/support/string.h"
#include "src/core/lib/support/string_windows.h"

struct gpr_subprocess ***REMOVED***
  PROCESS_INFORMATION pi;
  int joined;
  int interrupted;
***REMOVED***;

const char* gpr_subprocess_binary_extension() ***REMOVED*** return ".exe"; ***REMOVED***

gpr_subprocess* gpr_subprocess_create(int argc, const char** argv) ***REMOVED***
  gpr_subprocess* r;

  STARTUPINFO si;
  PROCESS_INFORMATION pi;

  char* args = gpr_strjoin_sep(argv, (size_t)argc, " ", NULL);
  TCHAR* args_tchar;

  args_tchar = gpr_char_to_tchar(args);
  gpr_free(args);

  memset(&si, 0, sizeof(si));
  si.cb = sizeof(si);
  memset(&pi, 0, sizeof(pi));

  if (!CreateProcess(NULL, args_tchar, NULL, NULL, FALSE,
                     CREATE_NEW_PROCESS_GROUP, NULL, NULL, &si, &pi)) ***REMOVED***
    gpr_free(args_tchar);
    return NULL;
  ***REMOVED***
  gpr_free(args_tchar);

  r = (gpr_subprocess*)gpr_malloc(sizeof(gpr_subprocess));
  memset(r, 0, sizeof(*r));
  r->pi = pi;
  return r;
***REMOVED***

void gpr_subprocess_destroy(gpr_subprocess* p) ***REMOVED***
  if (p) ***REMOVED***
    if (!p->joined) ***REMOVED***
      gpr_subprocess_interrupt(p);
      gpr_subprocess_join(p);
    ***REMOVED***
    if (p->pi.hProcess) ***REMOVED***
      CloseHandle(p->pi.hProcess);
    ***REMOVED***
    if (p->pi.hThread) ***REMOVED***
      CloseHandle(p->pi.hThread);
    ***REMOVED***
    gpr_free(p);
  ***REMOVED***
***REMOVED***

int gpr_subprocess_join(gpr_subprocess* p) ***REMOVED***
  DWORD dwExitCode;
  if (GetExitCodeProcess(p->pi.hProcess, &dwExitCode)) ***REMOVED***
    if (dwExitCode == STILL_ACTIVE) ***REMOVED***
      if (WaitForSingleObject(p->pi.hProcess, INFINITE) == WAIT_OBJECT_0) ***REMOVED***
        p->joined = 1;
        goto getExitCode;
      ***REMOVED***
      return -1;  // failed to join
    ***REMOVED*** else ***REMOVED***
      goto getExitCode;
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    return -1;  // failed to get exit code
  ***REMOVED***

getExitCode:
  if (p->interrupted) ***REMOVED***
    return 0;
  ***REMOVED***
  if (GetExitCodeProcess(p->pi.hProcess, &dwExitCode)) ***REMOVED***
    return (int)dwExitCode;
  ***REMOVED*** else ***REMOVED***
    return -1;  // failed to get exit code
  ***REMOVED***
***REMOVED***

void gpr_subprocess_interrupt(gpr_subprocess* p) ***REMOVED***
  DWORD dwExitCode;
  if (GetExitCodeProcess(p->pi.hProcess, &dwExitCode)) ***REMOVED***
    if (dwExitCode == STILL_ACTIVE) ***REMOVED***
      gpr_log(GPR_INFO, "sending ctrl-break");
      GenerateConsoleCtrlEvent(CTRL_BREAK_EVENT, p->pi.dwProcessId);
      p->joined = 1;
      p->interrupted = 1;
    ***REMOVED***
  ***REMOVED***
  return;
***REMOVED***

#endif /* GPR_WINDOWS_SUBPROCESS */
