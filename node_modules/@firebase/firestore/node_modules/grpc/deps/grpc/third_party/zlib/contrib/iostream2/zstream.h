/*
 *
 * Copyright (c) 1997
 * Christian Michelsen Research AS
 * Advanced Computing
 * Fantoftvegen 38, 5036 BERGEN, Norway
 * http://www.cmr.no
 *
 * Permission to use, copy, modify, distribute and sell this software
 * and its documentation for any purpose is hereby granted without fee,
 * provided that the above copyright notice appear in all copies and
 * that both that copyright notice and this permission notice appear
 * in supporting documentation.  Christian Michelsen Research AS makes no
 * representations about the suitability of this software for any
 * purpose.  It is provided "as is" without express or implied warranty.
 *
 */

#ifndef ZSTREAM__H
#define ZSTREAM__H

/*
 * zstream.h - C++ interface to the 'zlib' general purpose compression library
 * $Id: zstream.h 1.1 1997-06-25 12:00:56+02 tyge Exp tyge $
 */

#include <strstream.h>
#include <string.h>
#include <stdio.h>
#include "zlib.h"

#if defined(_WIN32)
#   include <fcntl.h>
#   include <io.h>
#   define SET_BINARY_MODE(file) setmode(fileno(file), O_BINARY)
#else
#   define SET_BINARY_MODE(file)
#endif

class zstringlen ***REMOVED***
public:
    zstringlen(class izstream&);
    zstringlen(class ozstream&, const char*);
    size_t value() const ***REMOVED*** return val.word; ***REMOVED***
private:
    struct Val ***REMOVED*** unsigned char byte; size_t word; ***REMOVED*** val;
***REMOVED***;

//  ----------------------------- izstream -----------------------------

class izstream
***REMOVED***
    public:
        izstream() : m_fp(0) ***REMOVED******REMOVED***
        izstream(FILE* fp) : m_fp(0) ***REMOVED*** open(fp); ***REMOVED***
        izstream(const char* name) : m_fp(0) ***REMOVED*** open(name); ***REMOVED***
        ~izstream() ***REMOVED*** close(); ***REMOVED***

        /* Opens a gzip (.gz) file for reading.
         * open() can be used to read a file which is not in gzip format;
         * in this case read() will directly read from the file without
         * decompression. errno can be checked to distinguish two error
         * cases (if errno is zero, the zlib error is Z_MEM_ERROR).
         */
        void open(const char* name) ***REMOVED***
            if (m_fp) close();
            m_fp = ::gzopen(name, "rb");
        ***REMOVED***

        void open(FILE* fp) ***REMOVED***
            SET_BINARY_MODE(fp);
            if (m_fp) close();
            m_fp = ::gzdopen(fileno(fp), "rb");
        ***REMOVED***

        /* Flushes all pending input if necessary, closes the compressed file
         * and deallocates all the (de)compression state. The return value is
         * the zlib error number (see function error() below).
         */
        int close() ***REMOVED***
            int r = ::gzclose(m_fp);
            m_fp = 0; return r;
        ***REMOVED***

        /* Binary read the given number of bytes from the compressed file.
         */
        int read(void* buf, size_t len) ***REMOVED***
            return ::gzread(m_fp, buf, len);
        ***REMOVED***

        /* Returns the error message for the last error which occurred on the
         * given compressed file. errnum is set to zlib error number. If an
         * error occurred in the file system and not in the compression library,
         * errnum is set to Z_ERRNO and the application may consult errno
         * to get the exact error code.
         */
        const char* error(int* errnum) ***REMOVED***
            return ::gzerror(m_fp, errnum);
        ***REMOVED***

        gzFile fp() ***REMOVED*** return m_fp; ***REMOVED***

    private:
        gzFile m_fp;
***REMOVED***;

/*
 * Binary read the given (array of) object(s) from the compressed file.
 * If the input file was not in gzip format, read() copies the objects number
 * of bytes into the buffer.
 * returns the number of uncompressed bytes actually read
 * (0 for end of file, -1 for error).
 */
template <class T, class Items>
inline int read(izstream& zs, T* x, Items items) ***REMOVED***
    return ::gzread(zs.fp(), x, items*sizeof(T));
***REMOVED***

/*
 * Binary input with the '>' operator.
 */
template <class T>
inline izstream& operator>(izstream& zs, T& x) ***REMOVED***
    ::gzread(zs.fp(), &x, sizeof(T));
    return zs;
***REMOVED***


inline zstringlen::zstringlen(izstream& zs) ***REMOVED***
    zs > val.byte;
    if (val.byte == 255) zs > val.word;
    else val.word = val.byte;
***REMOVED***

/*
 * Read length of string + the string with the '>' operator.
 */
inline izstream& operator>(izstream& zs, char* x) ***REMOVED***
    zstringlen len(zs);
    ::gzread(zs.fp(), x, len.value());
    x[len.value()] = '\0';
    return zs;
***REMOVED***

inline char* read_string(izstream& zs) ***REMOVED***
    zstringlen len(zs);
    char* x = new char[len.value()+1];
    ::gzread(zs.fp(), x, len.value());
    x[len.value()] = '\0';
    return x;
***REMOVED***

// ----------------------------- ozstream -----------------------------

class ozstream
***REMOVED***
    public:
        ozstream() : m_fp(0), m_os(0) ***REMOVED***
        ***REMOVED***
        ozstream(FILE* fp, int level = Z_DEFAULT_COMPRESSION)
            : m_fp(0), m_os(0) ***REMOVED***
            open(fp, level);
        ***REMOVED***
        ozstream(const char* name, int level = Z_DEFAULT_COMPRESSION)
            : m_fp(0), m_os(0) ***REMOVED***
            open(name, level);
        ***REMOVED***
        ~ozstream() ***REMOVED***
            close();
        ***REMOVED***

        /* Opens a gzip (.gz) file for writing.
         * The compression level parameter should be in 0..9
         * errno can be checked to distinguish two error cases
         * (if errno is zero, the zlib error is Z_MEM_ERROR).
         */
        void open(const char* name, int level = Z_DEFAULT_COMPRESSION) ***REMOVED***
            char mode[4] = "wb\0";
            if (level != Z_DEFAULT_COMPRESSION) mode[2] = '0'+level;
            if (m_fp) close();
            m_fp = ::gzopen(name, mode);
        ***REMOVED***

        /* open from a FILE pointer.
         */
        void open(FILE* fp, int level = Z_DEFAULT_COMPRESSION) ***REMOVED***
            SET_BINARY_MODE(fp);
            char mode[4] = "wb\0";
            if (level != Z_DEFAULT_COMPRESSION) mode[2] = '0'+level;
            if (m_fp) close();
            m_fp = ::gzdopen(fileno(fp), mode);
        ***REMOVED***

        /* Flushes all pending output if necessary, closes the compressed file
         * and deallocates all the (de)compression state. The return value is
         * the zlib error number (see function error() below).
         */
        int close() ***REMOVED***
            if (m_os) ***REMOVED***
                ::gzwrite(m_fp, m_os->str(), m_os->pcount());
                delete[] m_os->str(); delete m_os; m_os = 0;
            ***REMOVED***
            int r = ::gzclose(m_fp); m_fp = 0; return r;
        ***REMOVED***

        /* Binary write the given number of bytes into the compressed file.
         */
        int write(const void* buf, size_t len) ***REMOVED***
            return ::gzwrite(m_fp, (voidp) buf, len);
        ***REMOVED***

        /* Flushes all pending output into the compressed file. The parameter
         * _flush is as in the deflate() function. The return value is the zlib
         * error number (see function gzerror below). flush() returns Z_OK if
         * the flush_ parameter is Z_FINISH and all output could be flushed.
         * flush() should be called only when strictly necessary because it can
         * degrade compression.
         */
        int flush(int _flush) ***REMOVED***
            os_flush();
            return ::gzflush(m_fp, _flush);
        ***REMOVED***

        /* Returns the error message for the last error which occurred on the
         * given compressed file. errnum is set to zlib error number. If an
         * error occurred in the file system and not in the compression library,
         * errnum is set to Z_ERRNO and the application may consult errno
         * to get the exact error code.
         */
        const char* error(int* errnum) ***REMOVED***
            return ::gzerror(m_fp, errnum);
        ***REMOVED***

        gzFile fp() ***REMOVED*** return m_fp; ***REMOVED***

        ostream& os() ***REMOVED***
            if (m_os == 0) m_os = new ostrstream;
            return *m_os;
        ***REMOVED***

        void os_flush() ***REMOVED***
            if (m_os && m_os->pcount()>0) ***REMOVED***
                ostrstream* oss = new ostrstream;
                oss->fill(m_os->fill());
                oss->flags(m_os->flags());
                oss->precision(m_os->precision());
                oss->width(m_os->width());
                ::gzwrite(m_fp, m_os->str(), m_os->pcount());
                delete[] m_os->str(); delete m_os; m_os = oss;
            ***REMOVED***
        ***REMOVED***

    private:
        gzFile m_fp;
        ostrstream* m_os;
***REMOVED***;

/*
 * Binary write the given (array of) object(s) into the compressed file.
 * returns the number of uncompressed bytes actually written
 * (0 in case of error).
 */
template <class T, class Items>
inline int write(ozstream& zs, const T* x, Items items) ***REMOVED***
    return ::gzwrite(zs.fp(), (voidp) x, items*sizeof(T));
***REMOVED***

/*
 * Binary output with the '<' operator.
 */
template <class T>
inline ozstream& operator<(ozstream& zs, const T& x) ***REMOVED***
    ::gzwrite(zs.fp(), (voidp) &x, sizeof(T));
    return zs;
***REMOVED***

inline zstringlen::zstringlen(ozstream& zs, const char* x) ***REMOVED***
    val.byte = 255;  val.word = ::strlen(x);
    if (val.word < 255) zs < (val.byte = val.word);
    else zs < val;
***REMOVED***

/*
 * Write length of string + the string with the '<' operator.
 */
inline ozstream& operator<(ozstream& zs, const char* x) ***REMOVED***
    zstringlen len(zs, x);
    ::gzwrite(zs.fp(), (voidp) x, len.value());
    return zs;
***REMOVED***

#ifdef _MSC_VER
inline ozstream& operator<(ozstream& zs, char* const& x) ***REMOVED***
    return zs < (const char*) x;
***REMOVED***
#endif

/*
 * Ascii write with the << operator;
 */
template <class T>
inline ostream& operator<<(ozstream& zs, const T& x) ***REMOVED***
    zs.os_flush();
    return zs.os() << x;
***REMOVED***

#endif
