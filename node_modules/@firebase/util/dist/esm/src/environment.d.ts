/**
 * Returns navigator.userAgent string or '' if it's not defined.
 * @return ***REMOVED***string***REMOVED*** user agent string
 */
export declare const getUA: () => string;
/**
 * Detect Cordova / PhoneGap / Ionic frameworks on a mobile device.
 *
 * Deliberately does not rely on checking `file://` URLs (as this fails PhoneGap in the Ripple emulator) nor
 * Cordova `onDeviceReady`, which would normally wait for a callback.
 *
 * @return ***REMOVED***boolean***REMOVED*** isMobileCordova
 */
export declare const isMobileCordova: () => boolean;
/**
 * Detect React Native.
 *
 * @return ***REMOVED***boolean***REMOVED*** True if ReactNative environment is detected.
 */
export declare const isReactNative: () => boolean;
/**
 * Detect Node.js.
 *
 * @return ***REMOVED***boolean***REMOVED*** True if Node.js environment is detected.
 */
export declare const isNodeSdk: () => boolean;
