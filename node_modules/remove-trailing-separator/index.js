var isWin = process.platform === 'win32';

module.exports = function (str) ***REMOVED***
	var i = str.length - 1;
	if (i < 2) ***REMOVED***
		return str;
	***REMOVED***
	while (isSeparator(str, i)) ***REMOVED***
		i--;
	***REMOVED***
	return str.substr(0, i + 1);
***REMOVED***;

function isSeparator(str, i) ***REMOVED***
	var char = str[i];
	return i > 0 && (char === '/' || (isWin && char === '\\'));
***REMOVED***
