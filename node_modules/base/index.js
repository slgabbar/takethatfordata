'use strict';

var util = require('util');
var define = require('define-property');
var CacheBase = require('cache-base');
var Emitter = require('component-emitter');
var isObject = require('isobject');
var merge = require('mixin-deep');
var pascal = require('pascalcase');
var cu = require('class-utils');

/**
 * Optionally define a custom `cache` namespace to use.
 */

function namespace(name) ***REMOVED***
  var Cache = name ? CacheBase.namespace(name) : CacheBase;
  var fns = [];

  /**
   * Create an instance of `Base` with the given `config` and `options`.
   *
   * ```js
   * // initialize with `config` and `options`
   * var app = new Base(***REMOVED***isApp: true***REMOVED***, ***REMOVED***abc: true***REMOVED***);
   * app.set('foo', 'bar');
   *
   * // values defined with the given `config` object will be on the root of the instance
   * console.log(app.baz); //=> undefined
   * console.log(app.foo); //=> 'bar'
   * // or use `.get`
   * console.log(app.get('isApp')); //=> true
   * console.log(app.get('foo')); //=> 'bar'
   *
   * // values defined with the given `options` object will be on `app.options
   * console.log(app.options.abc); //=> true
   * ```
   *
   * @param ***REMOVED***Object***REMOVED*** `config` If supplied, this object is passed to [cache-base][] to merge onto the the instance upon instantiation.
   * @param ***REMOVED***Object***REMOVED*** `options` If supplied, this object is used to initialize the `base.options` object.
   * @api public
   */

  function Base(config, options) ***REMOVED***
    if (!(this instanceof Base)) ***REMOVED***
      return new Base(config, options);
    ***REMOVED***
    Cache.call(this, config);
    this.is('base');
    this.initBase(config, options);
  ***REMOVED***

  /**
   * Inherit cache-base
   */

  util.inherits(Base, Cache);

  /**
   * Add static emitter methods
   */

  Emitter(Base);

  /**
   * Initialize `Base` defaults with the given `config` object
   */

  Base.prototype.initBase = function(config, options) ***REMOVED***
    this.options = merge(***REMOVED******REMOVED***, this.options, options);
    this.cache = this.cache || ***REMOVED******REMOVED***;
    this.define('registered', ***REMOVED******REMOVED***);
    if (name) this[name] = ***REMOVED******REMOVED***;

    // make `app._callbacks` non-enumerable
    this.define('_callbacks', this._callbacks);
    if (isObject(config)) ***REMOVED***
      this.visit('set', config);
    ***REMOVED***
    Base.run(this, 'use', fns);
  ***REMOVED***;

  /**
   * Set the given `name` on `app._name` and `app.is*` properties. Used for doing
   * lookups in plugins.
   *
   * ```js
   * app.is('foo');
   * console.log(app._name);
   * //=> 'foo'
   * console.log(app.isFoo);
   * //=> true
   * app.is('bar');
   * console.log(app.isFoo);
   * //=> true
   * console.log(app.isBar);
   * //=> true
   * console.log(app._name);
   * //=> 'bar'
   * ```
   * @name .is
   * @param ***REMOVED***String***REMOVED*** `name`
   * @return ***REMOVED***Boolean***REMOVED***
   * @api public
   */

  Base.prototype.is = function(name) ***REMOVED***
    if (typeof name !== 'string') ***REMOVED***
      throw new TypeError('expected name to be a string');
    ***REMOVED***
    this.define('is' + pascal(name), true);
    this.define('_name', name);
    this.define('_appname', name);
    return this;
  ***REMOVED***;

  /**
   * Returns true if a plugin has already been registered on an instance.
   *
   * Plugin implementors are encouraged to use this first thing in a plugin
   * to prevent the plugin from being called more than once on the same
   * instance.
   *
   * ```js
   * var base = new Base();
   * base.use(function(app) ***REMOVED***
   *   if (app.isRegistered('myPlugin')) return;
   *   // do stuff to `app`
   * ***REMOVED***);
   *
   * // to also record the plugin as being registered
   * base.use(function(app) ***REMOVED***
   *   if (app.isRegistered('myPlugin', true)) return;
   *   // do stuff to `app`
   * ***REMOVED***);
   * ```
   * @name .isRegistered
   * @emits `plugin` Emits the name of the plugin being registered. Useful for unit tests, to ensure plugins are only registered once.
   * @param ***REMOVED***String***REMOVED*** `name` The plugin name.
   * @param ***REMOVED***Boolean***REMOVED*** `register` If the plugin if not already registered, to record it as being registered pass `true` as the second argument.
   * @return ***REMOVED***Boolean***REMOVED*** Returns true if a plugin is already registered.
   * @api public
   */

  Base.prototype.isRegistered = function(name, register) ***REMOVED***
    if (this.registered.hasOwnProperty(name)) ***REMOVED***
      return true;
    ***REMOVED***
    if (register !== false) ***REMOVED***
      this.registered[name] = true;
      this.emit('plugin', name);
    ***REMOVED***
    return false;
  ***REMOVED***;

  /**
   * Define a plugin function to be called immediately upon init. Plugins are chainable
   * and expose the following arguments to the plugin function:
   *
   * - `app`: the current instance of `Base`
   * - `base`: the [first ancestor instance](#base) of `Base`
   *
   * ```js
   * var app = new Base()
   *   .use(foo)
   *   .use(bar)
   *   .use(baz)
   * ```
   * @name .use
   * @param ***REMOVED***Function***REMOVED*** `fn` plugin function to call
   * @return ***REMOVED***Object***REMOVED*** Returns the item instance for chaining.
   * @api public
   */

  Base.prototype.use = function(fn) ***REMOVED***
    fn.call(this, this);
    return this;
  ***REMOVED***;

  /**
   * The `.define` method is used for adding non-enumerable property on the instance.
   * Dot-notation is **not supported** with `define`.
   *
   * ```js
   * // arbitrary `render` function using lodash `template`
   * app.define('render', function(str, locals) ***REMOVED***
   *   return _.template(str)(locals);
   * ***REMOVED***);
   * ```
   * @name .define
   * @param ***REMOVED***String***REMOVED*** `key` The name of the property to define.
   * @param ***REMOVED***any***REMOVED*** `value`
   * @return ***REMOVED***Object***REMOVED*** Returns the instance for chaining.
   * @api public
   */

  Base.prototype.define = function(key, val) ***REMOVED***
    if (isObject(key)) ***REMOVED***
      return this.visit('define', key);
    ***REMOVED***
    define(this, key, val);
    return this;
  ***REMOVED***;

  /**
   * Mix property `key` onto the Base prototype. If base is inherited using
   * `Base.extend` this method will be overridden by a new `mixin` method that will
   * only add properties to the prototype of the inheriting application.
   *
   * ```js
   * app.mixin('foo', function() ***REMOVED***
   *   // do stuff
   * ***REMOVED***);
   * ```
   * @name .mixin
   * @param ***REMOVED***String***REMOVED*** `key`
   * @param ***REMOVED***Object|Array***REMOVED*** `val`
   * @return ***REMOVED***Object***REMOVED*** Returns the `base` instance for chaining.
   * @api public
   */

  Base.prototype.mixin = function(key, val) ***REMOVED***
    Base.prototype[key] = val;
    return this;
  ***REMOVED***;

  /**
   * Non-enumberable mixin array, used by the static [Base.mixin]() method.
   */

  Base.prototype.mixins = Base.prototype.mixins || [];

  /**
   * Getter/setter used when creating nested instances of `Base`, for storing a reference
   * to the first ancestor instance. This works by setting an instance of `Base` on the `parent`
   * property of a "child" instance. The `base` property defaults to the current instance if
   * no `parent` property is defined.
   *
   * ```js
   * // create an instance of `Base`, this is our first ("base") instance
   * var first = new Base();
   * first.foo = 'bar'; // arbitrary property, to make it easier to see what's happening later
   *
   * // create another instance
   * var second = new Base();
   * // create a reference to the first instance (`first`)
   * second.parent = first;
   *
   * // create another instance
   * var third = new Base();
   * // create a reference to the previous instance (`second`)
   * // repeat this pattern every time a "child" instance is created
   * third.parent = second;
   *
   * // we can always access the first instance using the `base` property
   * console.log(first.base.foo);
   * //=> 'bar'
   * console.log(second.base.foo);
   * //=> 'bar'
   * console.log(third.base.foo);
   * //=> 'bar'
   * // and now you know how to get to third base ;)
   * ```
   * @name .base
   * @api public
   */

  Object.defineProperty(Base.prototype, 'base', ***REMOVED***
    configurable: true,
    get: function() ***REMOVED***
      return this.parent ? this.parent.base : this;
    ***REMOVED***
  ***REMOVED***);

  /**
   * Static method for adding global plugin functions that will
   * be added to an instance when created.
   *
   * ```js
   * Base.use(function(app) ***REMOVED***
   *   app.foo = 'bar';
   * ***REMOVED***);
   * var app = new Base();
   * console.log(app.foo);
   * //=> 'bar'
   * ```
   * @name #use
   * @param ***REMOVED***Function***REMOVED*** `fn` Plugin function to use on each instance.
   * @return ***REMOVED***Object***REMOVED*** Returns the `Base` constructor for chaining
   * @api public
   */

  define(Base, 'use', function(fn) ***REMOVED***
    fns.push(fn);
    return Base;
  ***REMOVED***);

  /**
   * Run an array of functions by passing each function
   * to a method on the given object specified by the given property.
   *
   * @param  ***REMOVED***Object***REMOVED*** `obj` Object containing method to use.
   * @param  ***REMOVED***String***REMOVED*** `prop` Name of the method on the object to use.
   * @param  ***REMOVED***Array***REMOVED*** `arr` Array of functions to pass to the method.
   */

  define(Base, 'run', function(obj, prop, arr) ***REMOVED***
    var len = arr.length, i = 0;
    while (len--) ***REMOVED***
      obj[prop](arr[i++]);
    ***REMOVED***
    return Base;
  ***REMOVED***);

  /**
   * Static method for inheriting the prototype and static methods of the `Base` class.
   * This method greatly simplifies the process of creating inheritance-based applications.
   * See [static-extend][] for more details.
   *
   * ```js
   * var extend = cu.extend(Parent);
   * Parent.extend(Child);
   *
   * // optional methods
   * Parent.extend(Child, ***REMOVED***
   *   foo: function() ***REMOVED******REMOVED***,
   *   bar: function() ***REMOVED******REMOVED***
   * ***REMOVED***);
   * ```
   * @name #extend
   * @param ***REMOVED***Function***REMOVED*** `Ctor` constructor to extend
   * @param ***REMOVED***Object***REMOVED*** `methods` Optional prototype properties to mix in.
   * @return ***REMOVED***Object***REMOVED*** Returns the `Base` constructor for chaining
   * @api public
   */

  define(Base, 'extend', cu.extend(Base, function(Ctor, Parent) ***REMOVED***
    Ctor.prototype.mixins = Ctor.prototype.mixins || [];

    define(Ctor, 'mixin', function(fn) ***REMOVED***
      var mixin = fn(Ctor.prototype, Ctor);
      if (typeof mixin === 'function') ***REMOVED***
        Ctor.prototype.mixins.push(mixin);
      ***REMOVED***
      return Ctor;
    ***REMOVED***);

    define(Ctor, 'mixins', function(Child) ***REMOVED***
      Base.run(Child, 'mixin', Ctor.prototype.mixins);
      return Ctor;
    ***REMOVED***);

    Ctor.prototype.mixin = function(key, value) ***REMOVED***
      Ctor.prototype[key] = value;
      return this;
    ***REMOVED***;
    return Base;
  ***REMOVED***));

  /**
   * Used for adding methods to the `Base` prototype, and/or to the prototype of child instances.
   * When a mixin function returns a function, the returned function is pushed onto the `.mixins`
   * array, making it available to be used on inheriting classes whenever `Base.mixins()` is
   * called (e.g. `Base.mixins(Child)`).
   *
   * ```js
   * Base.mixin(function(proto) ***REMOVED***
   *   proto.foo = function(msg) ***REMOVED***
   *     return 'foo ' + msg;
   *   ***REMOVED***;
   * ***REMOVED***);
   * ```
   * @name #mixin
   * @param ***REMOVED***Function***REMOVED*** `fn` Function to call
   * @return ***REMOVED***Object***REMOVED*** Returns the `Base` constructor for chaining
   * @api public
   */

  define(Base, 'mixin', function(fn) ***REMOVED***
    var mixin = fn(Base.prototype, Base);
    if (typeof mixin === 'function') ***REMOVED***
      Base.prototype.mixins.push(mixin);
    ***REMOVED***
    return Base;
  ***REMOVED***);

  /**
   * Static method for running global mixin functions against a child constructor.
   * Mixins must be registered before calling this method.
   *
   * ```js
   * Base.extend(Child);
   * Base.mixins(Child);
   * ```
   * @name #mixins
   * @param ***REMOVED***Function***REMOVED*** `Child` Constructor function of a child class
   * @return ***REMOVED***Object***REMOVED*** Returns the `Base` constructor for chaining
   * @api public
   */

  define(Base, 'mixins', function(Child) ***REMOVED***
    Base.run(Child, 'mixin', Base.prototype.mixins);
    return Base;
  ***REMOVED***);

  /**
   * Similar to `util.inherit`, but copies all static properties, prototype properties, and
   * getters/setters from `Provider` to `Receiver`. See [class-utils][]***REMOVED***#inherit***REMOVED*** for more details.
   *
   * ```js
   * Base.inherit(Foo, Bar);
   * ```
   * @name #inherit
   * @param ***REMOVED***Function***REMOVED*** `Receiver` Receiving (child) constructor
   * @param ***REMOVED***Function***REMOVED*** `Provider` Providing (parent) constructor
   * @return ***REMOVED***Object***REMOVED*** Returns the `Base` constructor for chaining
   * @api public
   */

  define(Base, 'inherit', cu.inherit);
  define(Base, 'bubble', cu.bubble);
  return Base;
***REMOVED***

/**
 * Expose `Base` with default settings
 */

module.exports = namespace();

/**
 * Allow users to define a namespace
 */

module.exports.namespace = namespace;
