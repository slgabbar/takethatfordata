var test = require("tape")
var setTimeout = require("timers").setTimeout

var now = require("../index")
var seeded = require("../seed")

test("date", function (assert) ***REMOVED***
    var ts = now()
    var ts2 = Date.now()
    assert.equal(ts, ts2)
    assert.end()
***REMOVED***)

test("seeded", function (assert) ***REMOVED***
    var time = seeded(40)
    var ts = time()

    within(assert, time(), 40, 5)
    setTimeout(function () ***REMOVED***
        within(assert, time(), 90, 10)
        assert.end()
    ***REMOVED***, 50)
***REMOVED***)

function within(assert, a, b, offset) ***REMOVED***
    assert.ok(a + offset > b)
    assert.ok(a - offset < b)
***REMOVED***
