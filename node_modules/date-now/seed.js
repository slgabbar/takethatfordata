var now = require("./index")

module.exports = seeded

/* Returns a Date.now() like function that's in sync with
    the seed value
*/
function seeded(seed) ***REMOVED***
    var current = now()

    return time

    function time() ***REMOVED***
        return seed + (now() - current)
    ***REMOVED***
***REMOVED***
