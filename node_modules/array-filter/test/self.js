var filter = require('..');
var test = require('tape');

test('self', function(t) ***REMOVED***
  var arr = [1];
  arr.filter = undefined;
  var self = ***REMOVED******REMOVED***;
  filter(arr, function(el) ***REMOVED***
    t.equal(this, self);
    t.end();
  ***REMOVED***, self);
***REMOVED***);
