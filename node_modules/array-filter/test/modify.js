var filter = require('..');
var test = require('tape');

test('modify', function(t) ***REMOVED***
  var arr = ['foo'];
  arr.filter = undefined;
  var mod = filter(arr, function(el, i, arr) ***REMOVED***
    arr[i] = 'bar';
    return true;
  ***REMOVED***);
  t.deepEqual(mod, ['foo']);
  t.end();
***REMOVED***);
