var filter = require('..');
var test = require('tape');

test('filter', function(t) ***REMOVED***
  var arr = [1, 2, 3, 4, 5];
  arr.filter = undefined;
  var even = filter(arr, function(el) ***REMOVED***
    return el % 2 == 0;
  ***REMOVED***);
  t.deepEqual(even, [2, 4]);
  t.end();
***REMOVED***);
