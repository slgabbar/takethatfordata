var filter = require('..');
var test = require('tape');

test('skip over holes', function(t) ***REMOVED***
  var arr = new Array(5);
  arr.filter = undefined;
  var res = filter(arr, function(el) ***REMOVED***
    return false;
  ***REMOVED***);
  t.deepEqual(res, []);
  t.end();
***REMOVED***);
