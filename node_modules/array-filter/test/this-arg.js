var filter = require('..');
var test = require('tape');

test('thisArg', function(t) ***REMOVED***
  var self = ***REMOVED******REMOVED***;
  var arr = [1];
  arr.filter = undefined;
  filter(arr, function(el) ***REMOVED***
    t.equal(this, self);
    t.end();
  ***REMOVED***, self);
***REMOVED***);

test('thisArg native', function(t) ***REMOVED***
  var self = ***REMOVED******REMOVED***;
  var arr = [1];
  filter(arr, function(el) ***REMOVED***
    t.equal(this, self);
    t.end();
  ***REMOVED***, self);
***REMOVED***);
