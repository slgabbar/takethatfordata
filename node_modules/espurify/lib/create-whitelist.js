'use strict';

var defaultProps = require('./ast-properties');
var objectKeys = require('core-js/library/fn/object/keys');
var assign = require('core-js/library/fn/object/assign');

module.exports = function createWhitelist (options) ***REMOVED***
    var opts = assign(***REMOVED******REMOVED***, options);
    var typeName, i, len;
    var keys = objectKeys(defaultProps);
    var result = ***REMOVED******REMOVED***;
    for (i = 0, len = keys.length; i < len; i += 1) ***REMOVED***
        typeName = keys[i];
        result[typeName] = defaultProps[typeName].concat(opts.extra);
    ***REMOVED***
    return result;
***REMOVED***;
