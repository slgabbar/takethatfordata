// a simple benchmark utility comparing performance between the different builds.

// Uint8Array observations (compared to node Buffers)
// - seems to be pretty much equal for byte ops

// DataView observations (compared to Uint8Array):
// - allocation is about 2 times slower
// - writing is about 5 times (int32) to 10 times (varint) slower
// - reading is about 3 times slower

// UTF8 encoding observations (compared to node's string/buffer API)
// - the longer the string, the poorer is read/write performance
// - either utfx doesn't cut it yet, or node's bindings simply outperform everything js here

var ByteBuffer = require("../index.js"),
    prettyHrTime = require("pretty-hrtime");

var impls = [
    ***REMOVED*** name: "ByteBufferNB", impl: ByteBuffer.ByteBufferNB ***REMOVED***,
    ***REMOVED*** name: "ByteBufferAB", impl: ByteBuffer.ByteBufferAB ***REMOVED***,
    ***REMOVED*** name: "ByteBufferAB_DataView", impl: ByteBuffer.ByteBufferAB_DataView ***REMOVED***
];

var bench = ***REMOVED******REMOVED***;

bench["allocate"] = function(ByteBuffer, n) ***REMOVED***
    n = n || 10000;
    for (var i=0; i<n; ++i)
        new ByteBuffer();
    return n;
***REMOVED***;

bench["writeInt32"] = function(ByteBuffer, n) ***REMOVED***
    n = n || 1000000;
    var bb = new ByteBuffer(4);
    for (var i=0; i<n; ++i)
        bb.writeInt32(0x7fffffff, 0);
    return n;
***REMOVED***;

bench["readInt32"] = function(ByteBuffer, n) ***REMOVED***
    n = n || 1000000;
    var bb = new ByteBuffer(4).writeInt32(0x7fffffff).flip();
    for (var i=0; i<n; ++i)
        bb.readInt32(0);
    return n;
***REMOVED***;

bench["writeVarint32"] = function(ByteBuffer, n) ***REMOVED***
    n = n || 1000000;
    var bb = new ByteBuffer(6);
    for (var i=0; i<n; ++i)
        bb.writeVarint32(0x7fffffff, 0);
    return n;
***REMOVED***;

bench["readVarint32"] = function(ByteBuffer, n) ***REMOVED***
    n = n || 1000000;
    var bb = new ByteBuffer(6).writeInt32(0x7fffffff).flip();
    for (var i=0; i<n; ++i)
        bb.readVarint32(0);
    return n;
***REMOVED***;

bench["writeString"] = function(ByteBuffer, n) ***REMOVED***
    n = n || 100000;
    var bb = new ByteBuffer(26);
    for (var i=0; i<n; ++i)
        bb.writeString("abcdefghijklmnopqrstuvwxyz", 0);
    return n;
***REMOVED***;

bench["readString"] = function(ByteBuffer, n) ***REMOVED***
    n = n || 100000;
    var bb = new ByteBuffer(26).writeString("abcdefghijklmnopqrstuvwxyz").flip();
    for (var i=0; i<n; ++i)
        bb.readString(26, ByteBuffer.METRICS_BYTES, 0);
    return n;
***REMOVED***;

bench["calculateString"] = function(ByteBuffer, n) ***REMOVED***
    n = n || 100000;
    for (var i=0; i<n; ++i)
        ByteBuffer.calculateString("abcdefghijklmnopqrstuvwxyz");
    return n;
***REMOVED***;

Object.keys(bench).forEach(function(key) ***REMOVED***
    var func = bench[key];
    console.log(key);
    impls.forEach(function(impl) ***REMOVED***
        var n, diff;
        var start = process.hrtime();
        n = func(impl.impl);
        diff = process.hrtime(start);
        console.log("- "+impl.name+": "+prettyHrTime(diff));
    ***REMOVED***);
    console.log();
***REMOVED***);
