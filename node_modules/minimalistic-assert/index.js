module.exports = assert;

function assert(val, msg) ***REMOVED***
  if (!val)
    throw new Error(msg || 'Assertion failed');
***REMOVED***

assert.equal = function assertEqual(l, r, msg) ***REMOVED***
  if (l != r)
    throw new Error(msg || ('Assertion failed: ' + l + ' != ' + r));
***REMOVED***;
