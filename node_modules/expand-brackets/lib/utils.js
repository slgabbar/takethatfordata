'use strict';

var toRegex = require('to-regex');
var regexNot = require('regex-not');
var cached;

/**
 * Get the last element from `array`
 * @param ***REMOVED***Array***REMOVED*** `array`
 * @return ***REMOVED*******REMOVED***
 */

exports.last = function(arr) ***REMOVED***
  return arr[arr.length - 1];
***REMOVED***;

/**
 * Create and cache regex to use for text nodes
 */

exports.createRegex = function(pattern, include) ***REMOVED***
  if (cached) return cached;
  var opts = ***REMOVED***contains: true, strictClose: false***REMOVED***;
  var not = regexNot.create(pattern, opts);
  var re;

  if (typeof include === 'string') ***REMOVED***
    re = toRegex('^(?:' + include + '|' + not + ')', opts);
  ***REMOVED*** else ***REMOVED***
    re = toRegex(not, opts);
  ***REMOVED***

  return (cached = re);
***REMOVED***;
