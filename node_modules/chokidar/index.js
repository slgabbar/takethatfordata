'use strict';
var EventEmitter = require('events').EventEmitter;
var fs = require('fs');
var sysPath = require('path');
var asyncEach = require('async-each');
var anymatch = require('anymatch');
var globParent = require('glob-parent');
var isGlob = require('is-glob');
var isAbsolute = require('path-is-absolute');
var inherits = require('inherits');
var braces = require('braces');
var normalizePath = require('normalize-path')

var NodeFsHandler = require('./lib/nodefs-handler');
var FsEventsHandler = require('./lib/fsevents-handler');

var arrify = function(value) ***REMOVED***
  if (value == null) return [];
  return Array.isArray(value) ? value : [value];
***REMOVED***;

var flatten = function(list, result) ***REMOVED***
  if (result == null) result = [];
  list.forEach(function(item) ***REMOVED***
    if (Array.isArray(item)) ***REMOVED***
      flatten(item, result);
    ***REMOVED*** else ***REMOVED***
      result.push(item);
    ***REMOVED***
  ***REMOVED***);
  return result;
***REMOVED***;

// Little isString util for use in Array#every.
var isString = function(thing) ***REMOVED***
  return typeof thing === 'string';
***REMOVED***;

// Public: Main class.
// Watches files & directories for changes.
//
// * _opts - object, chokidar options hash
//
// Emitted events:
// `add`, `addDir`, `change`, `unlink`, `unlinkDir`, `all`, `error`
//
// Examples
//
//  var watcher = new FSWatcher()
//    .add(directories)
//    .on('add', path => console.log('File', path, 'was added'))
//    .on('change', path => console.log('File', path, 'was changed'))
//    .on('unlink', path => console.log('File', path, 'was removed'))
//    .on('all', (event, path) => console.log(path, ' emitted ', event))
//
function FSWatcher(_opts) ***REMOVED***
  EventEmitter.call(this);
  var opts = ***REMOVED******REMOVED***;
  // in case _opts that is passed in is a frozen object
  if (_opts) for (var opt in _opts) opts[opt] = _opts[opt];
  this._watched = Object.create(null);
  this._closers = Object.create(null);
  this._ignoredPaths = Object.create(null);
  Object.defineProperty(this, '_globIgnored', ***REMOVED***
    get: function() ***REMOVED*** return Object.keys(this._ignoredPaths); ***REMOVED***
  ***REMOVED***);
  this.closed = false;
  this._throttled = Object.create(null);
  this._symlinkPaths = Object.create(null);

  function undef(key) ***REMOVED***
    return opts[key] === undefined;
  ***REMOVED***

  // Set up default options.
  if (undef('persistent')) opts.persistent = true;
  if (undef('ignoreInitial')) opts.ignoreInitial = false;
  if (undef('ignorePermissionErrors')) opts.ignorePermissionErrors = false;
  if (undef('interval')) opts.interval = 100;
  if (undef('binaryInterval')) opts.binaryInterval = 300;
  if (undef('disableGlobbing')) opts.disableGlobbing = false;
  this.enableBinaryInterval = opts.binaryInterval !== opts.interval;

  // Enable fsevents on OS X when polling isn't explicitly enabled.
  if (undef('useFsEvents')) opts.useFsEvents = !opts.usePolling;

  // If we can't use fsevents, ensure the options reflect it's disabled.
  if (!FsEventsHandler.canUse()) opts.useFsEvents = false;

  // Use polling on Mac if not using fsevents.
  // Other platforms use non-polling fs.watch.
  if (undef('usePolling') && !opts.useFsEvents) ***REMOVED***
    opts.usePolling = process.platform === 'darwin';
  ***REMOVED***

  // Global override (useful for end-developers that need to force polling for all
  // instances of chokidar, regardless of usage/dependency depth)
  var envPoll = process.env.CHOKIDAR_USEPOLLING;
  if (envPoll !== undefined) ***REMOVED***
    var envLower = envPoll.toLowerCase();

    if (envLower === 'false' || envLower === '0') ***REMOVED***
      opts.usePolling = false;
    ***REMOVED*** else if (envLower === 'true' || envLower === '1') ***REMOVED***
      opts.usePolling = true;
    ***REMOVED*** else ***REMOVED***
      opts.usePolling = !!envLower
    ***REMOVED***
  ***REMOVED***
  var envInterval = process.env.CHOKIDAR_INTERVAL;
  if (envInterval) ***REMOVED***
    opts.interval = parseInt(envInterval);
  ***REMOVED***

  // Editor atomic write normalization enabled by default with fs.watch
  if (undef('atomic')) opts.atomic = !opts.usePolling && !opts.useFsEvents;
  if (opts.atomic) this._pendingUnlinks = Object.create(null);

  if (undef('followSymlinks')) opts.followSymlinks = true;

  if (undef('awaitWriteFinish')) opts.awaitWriteFinish = false;
  if (opts.awaitWriteFinish === true) opts.awaitWriteFinish = ***REMOVED******REMOVED***;
  var awf = opts.awaitWriteFinish;
  if (awf) ***REMOVED***
    if (!awf.stabilityThreshold) awf.stabilityThreshold = 2000;
    if (!awf.pollInterval) awf.pollInterval = 100;

    this._pendingWrites = Object.create(null);
  ***REMOVED***
  if (opts.ignored) opts.ignored = arrify(opts.ignored);

  this._isntIgnored = function(path, stat) ***REMOVED***
    return !this._isIgnored(path, stat);
  ***REMOVED***.bind(this);

  var readyCalls = 0;
  this._emitReady = function() ***REMOVED***
    if (++readyCalls >= this._readyCount) ***REMOVED***
      this._emitReady = Function.prototype;
      this._readyEmitted = true;
      // use process.nextTick to allow time for listener to be bound
      process.nextTick(this.emit.bind(this, 'ready'));
    ***REMOVED***
  ***REMOVED***.bind(this);

  this.options = opts;

  // You’re frozen when your heart’s not open.
  Object.freeze(opts);
***REMOVED***

inherits(FSWatcher, EventEmitter);

// Common helpers
// --------------

// Private method: Normalize and emit events
//
// * event     - string, type of event
// * path      - string, file or directory path
// * val[1..3] - arguments to be passed with event
//
// Returns the error if defined, otherwise the value of the
// FSWatcher instance's `closed` flag
FSWatcher.prototype._emit = function(event, path, val1, val2, val3) ***REMOVED***
  if (this.options.cwd) path = sysPath.relative(this.options.cwd, path);
  var args = [event, path];
  if (val3 !== undefined) args.push(val1, val2, val3);
  else if (val2 !== undefined) args.push(val1, val2);
  else if (val1 !== undefined) args.push(val1);

  var awf = this.options.awaitWriteFinish;
  if (awf && this._pendingWrites[path]) ***REMOVED***
    this._pendingWrites[path].lastChange = new Date();
    return this;
  ***REMOVED***

  if (this.options.atomic) ***REMOVED***
    if (event === 'unlink') ***REMOVED***
      this._pendingUnlinks[path] = args;
      setTimeout(function() ***REMOVED***
        Object.keys(this._pendingUnlinks).forEach(function(path) ***REMOVED***
          this.emit.apply(this, this._pendingUnlinks[path]);
          this.emit.apply(this, ['all'].concat(this._pendingUnlinks[path]));
          delete this._pendingUnlinks[path];
        ***REMOVED***.bind(this));
      ***REMOVED***.bind(this), typeof this.options.atomic === "number"
        ? this.options.atomic
        : 100);
      return this;
    ***REMOVED*** else if (event === 'add' && this._pendingUnlinks[path]) ***REMOVED***
      event = args[0] = 'change';
      delete this._pendingUnlinks[path];
    ***REMOVED***
  ***REMOVED***

  var emitEvent = function() ***REMOVED***
    this.emit.apply(this, args);
    if (event !== 'error') this.emit.apply(this, ['all'].concat(args));
  ***REMOVED***.bind(this);

  if (awf && (event === 'add' || event === 'change') && this._readyEmitted) ***REMOVED***
    var awfEmit = function(err, stats) ***REMOVED***
      if (err) ***REMOVED***
        event = args[0] = 'error';
        args[1] = err;
        emitEvent();
      ***REMOVED*** else if (stats) ***REMOVED***
        // if stats doesn't exist the file must have been deleted
        if (args.length > 2) ***REMOVED***
          args[2] = stats;
        ***REMOVED*** else ***REMOVED***
          args.push(stats);
        ***REMOVED***
        emitEvent();
      ***REMOVED***
    ***REMOVED***;

    this._awaitWriteFinish(path, awf.stabilityThreshold, event, awfEmit);
    return this;
  ***REMOVED***

  if (event === 'change') ***REMOVED***
    if (!this._throttle('change', path, 50)) return this;
  ***REMOVED***

  if (
    this.options.alwaysStat && val1 === undefined &&
    (event === 'add' || event === 'addDir' || event === 'change')
  ) ***REMOVED***
    var fullPath = this.options.cwd ? sysPath.join(this.options.cwd, path) : path;
    fs.stat(fullPath, function(error, stats) ***REMOVED***
      // Suppress event when fs.stat fails, to avoid sending undefined 'stat'
      if (error || !stats) return;

      args.push(stats);
      emitEvent();
    ***REMOVED***);
  ***REMOVED*** else ***REMOVED***
    emitEvent();
  ***REMOVED***

  return this;
***REMOVED***;

// Private method: Common handler for errors
//
// * error  - object, Error instance
//
// Returns the error if defined, otherwise the value of the
// FSWatcher instance's `closed` flag
FSWatcher.prototype._handleError = function(error) ***REMOVED***
  var code = error && error.code;
  var ipe = this.options.ignorePermissionErrors;
  if (error &&
    code !== 'ENOENT' &&
    code !== 'ENOTDIR' &&
    (!ipe || (code !== 'EPERM' && code !== 'EACCES'))
  ) this.emit('error', error);
  return error || this.closed;
***REMOVED***;

// Private method: Helper utility for throttling
//
// * action  - string, type of action being throttled
// * path    - string, path being acted upon
// * timeout - int, duration of time to suppress duplicate actions
//
// Returns throttle tracking object or false if action should be suppressed
FSWatcher.prototype._throttle = function(action, path, timeout) ***REMOVED***
  if (!(action in this._throttled)) ***REMOVED***
    this._throttled[action] = Object.create(null);
  ***REMOVED***
  var throttled = this._throttled[action];
  if (path in throttled) return false;
  function clear() ***REMOVED***
    delete throttled[path];
    clearTimeout(timeoutObject);
  ***REMOVED***
  var timeoutObject = setTimeout(clear, timeout);
  throttled[path] = ***REMOVED***timeoutObject: timeoutObject, clear: clear***REMOVED***;
  return throttled[path];
***REMOVED***;

// Private method: Awaits write operation to finish
//
// * path    - string, path being acted upon
// * threshold - int, time in milliseconds a file size must be fixed before
//                    acknowledgeing write operation is finished
// * awfEmit - function, to be called when ready for event to be emitted
// Polls a newly created file for size variations. When files size does not
// change for 'threshold' milliseconds calls callback.
FSWatcher.prototype._awaitWriteFinish = function(path, threshold, event, awfEmit) ***REMOVED***
  var timeoutHandler;

  var fullPath = path;
  if (this.options.cwd && !isAbsolute(path)) ***REMOVED***
    fullPath = sysPath.join(this.options.cwd, path);
  ***REMOVED***

  var now = new Date();

  var awaitWriteFinish = (function (prevStat) ***REMOVED***
    fs.stat(fullPath, function(err, curStat) ***REMOVED***
      if (err) ***REMOVED***
        if (err.code !== 'ENOENT') awfEmit(err);
        return;
      ***REMOVED***

      var now = new Date();

      if (prevStat && curStat.size != prevStat.size) ***REMOVED***
        this._pendingWrites[path].lastChange = now;
      ***REMOVED***

      if (now - this._pendingWrites[path].lastChange >= threshold) ***REMOVED***
        delete this._pendingWrites[path];
        awfEmit(null, curStat);
      ***REMOVED*** else ***REMOVED***
        timeoutHandler = setTimeout(
          awaitWriteFinish.bind(this, curStat),
          this.options.awaitWriteFinish.pollInterval
        );
      ***REMOVED***
    ***REMOVED***.bind(this));
  ***REMOVED***.bind(this));

  if (!(path in this._pendingWrites)) ***REMOVED***
    this._pendingWrites[path] = ***REMOVED***
      lastChange: now,
      cancelWait: function() ***REMOVED***
        delete this._pendingWrites[path];
        clearTimeout(timeoutHandler);
        return event;
      ***REMOVED***.bind(this)
    ***REMOVED***;
    timeoutHandler = setTimeout(
      awaitWriteFinish.bind(this),
      this.options.awaitWriteFinish.pollInterval
    );
  ***REMOVED***
***REMOVED***;

// Private method: Determines whether user has asked to ignore this path
//
// * path  - string, path to file or directory
// * stats - object, result of fs.stat
//
// Returns boolean
var dotRe = /\..*\.(sw[px])$|\~$|\.subl.*\.tmp/;
FSWatcher.prototype._isIgnored = function(path, stats) ***REMOVED***
  if (this.options.atomic && dotRe.test(path)) return true;

  if (!this._userIgnored) ***REMOVED***
    var cwd = this.options.cwd;
    var ignored = this.options.ignored;
    if (cwd && ignored) ***REMOVED***
      ignored = ignored.map(function (path) ***REMOVED***
        if (typeof path !== 'string') return path;
        return isAbsolute(path) ? path : sysPath.join(cwd, path);
      ***REMOVED***);
    ***REMOVED***
    var paths = arrify(ignored)
      .filter(function(path) ***REMOVED***
        return typeof path === 'string' && !isGlob(path);
      ***REMOVED***).map(function(path) ***REMOVED***
        return path + '/**';
      ***REMOVED***);
    this._userIgnored = anymatch(
      this._globIgnored.concat(ignored).concat(paths)
    );
  ***REMOVED***

  return this._userIgnored([path, stats]);
***REMOVED***;

// Private method: Provides a set of common helpers and properties relating to
// symlink and glob handling
//
// * path - string, file, directory, or glob pattern being watched
// * depth - int, at any depth > 0, this isn't a glob
//
// Returns object containing helpers for this path
var replacerRe = /^\.[\/\\]/;
FSWatcher.prototype._getWatchHelpers = function(path, depth) ***REMOVED***
  path = path.replace(replacerRe, '');
  var watchPath = depth || this.options.disableGlobbing || !isGlob(path) ? path : globParent(path);
  var fullWatchPath = sysPath.resolve(watchPath);
  var hasGlob = watchPath !== path;
  var globFilter = hasGlob ? anymatch(path) : false;
  var follow = this.options.followSymlinks;
  var globSymlink = hasGlob && follow ? null : false;

  var checkGlobSymlink = function(entry) ***REMOVED***
    // only need to resolve once
    // first entry should always have entry.parentDir === ''
    if (globSymlink == null) ***REMOVED***
      globSymlink = entry.fullParentDir === fullWatchPath ? false : ***REMOVED***
        realPath: entry.fullParentDir,
        linkPath: fullWatchPath
      ***REMOVED***;
    ***REMOVED***

    if (globSymlink) ***REMOVED***
      return entry.fullPath.replace(globSymlink.realPath, globSymlink.linkPath);
    ***REMOVED***

    return entry.fullPath;
  ***REMOVED***;

  var entryPath = function(entry) ***REMOVED***
    return sysPath.join(watchPath,
      sysPath.relative(watchPath, checkGlobSymlink(entry))
    );
  ***REMOVED***;

  var filterPath = function(entry) ***REMOVED***
    if (entry.stat && entry.stat.isSymbolicLink()) return filterDir(entry);
    var resolvedPath = entryPath(entry);
    return (!hasGlob || globFilter(resolvedPath)) &&
      this._isntIgnored(resolvedPath, entry.stat) &&
      (this.options.ignorePermissionErrors ||
        this._hasReadPermissions(entry.stat));
  ***REMOVED***.bind(this);

  var getDirParts = function(path) ***REMOVED***
    if (!hasGlob) return false;
    var parts = [];
    var expandedPath = braces.expand(path);
    expandedPath.forEach(function(path) ***REMOVED***
      parts.push(sysPath.relative(watchPath, path).split(/[\/\\]/));
    ***REMOVED***);
    return parts;
  ***REMOVED***;

  var dirParts = getDirParts(path);
  if (dirParts) ***REMOVED***
    dirParts.forEach(function(parts) ***REMOVED***
      if (parts.length > 1) parts.pop();
    ***REMOVED***);
  ***REMOVED***
  var unmatchedGlob;

  var filterDir = function(entry) ***REMOVED***
    if (hasGlob) ***REMOVED***
      var entryParts = getDirParts(checkGlobSymlink(entry));
      var globstar = false;
      unmatchedGlob = !dirParts.some(function(parts) ***REMOVED***
        return parts.every(function(part, i) ***REMOVED***
          if (part === '**') globstar = true;
          return globstar || !entryParts[0][i] || anymatch(part, entryParts[0][i]);
        ***REMOVED***);
      ***REMOVED***);
    ***REMOVED***
    return !unmatchedGlob && this._isntIgnored(entryPath(entry), entry.stat);
  ***REMOVED***.bind(this);

  return ***REMOVED***
    followSymlinks: follow,
    statMethod: follow ? 'stat' : 'lstat',
    path: path,
    watchPath: watchPath,
    entryPath: entryPath,
    hasGlob: hasGlob,
    globFilter: globFilter,
    filterPath: filterPath,
    filterDir: filterDir
  ***REMOVED***;
***REMOVED***;

// Directory helpers
// -----------------

// Private method: Provides directory tracking objects
//
// * directory - string, path of the directory
//
// Returns the directory's tracking object
FSWatcher.prototype._getWatchedDir = function(directory) ***REMOVED***
  var dir = sysPath.resolve(directory);
  var watcherRemove = this._remove.bind(this);
  if (!(dir in this._watched)) this._watched[dir] = ***REMOVED***
    _items: Object.create(null),
    add: function(item) ***REMOVED***
      if (item !== '.' && item !== '..') this._items[item] = true;
    ***REMOVED***,
    remove: function(item) ***REMOVED***
      delete this._items[item];
      if (!this.children().length) ***REMOVED***
        fs.readdir(dir, function(err) ***REMOVED***
          if (err) watcherRemove(sysPath.dirname(dir), sysPath.basename(dir));
        ***REMOVED***);
      ***REMOVED***
    ***REMOVED***,
    has: function(item) ***REMOVED***return item in this._items;***REMOVED***,
    children: function() ***REMOVED***return Object.keys(this._items);***REMOVED***
  ***REMOVED***;
  return this._watched[dir];
***REMOVED***;

// File helpers
// ------------

// Private method: Check for read permissions
// Based on this answer on SO: http://stackoverflow.com/a/11781404/1358405
//
// * stats - object, result of fs.stat
//
// Returns boolean
FSWatcher.prototype._hasReadPermissions = function(stats) ***REMOVED***
  return Boolean(4 & parseInt(((stats && stats.mode) & 0x1ff).toString(8)[0], 10));
***REMOVED***;

// Private method: Handles emitting unlink events for
// files and directories, and via recursion, for
// files and directories within directories that are unlinked
//
// * directory - string, directory within which the following item is located
// * item      - string, base path of item/directory
//
// Returns nothing
FSWatcher.prototype._remove = function(directory, item) ***REMOVED***
  // if what is being deleted is a directory, get that directory's paths
  // for recursive deleting and cleaning of watched object
  // if it is not a directory, nestedDirectoryChildren will be empty array
  var path = sysPath.join(directory, item);
  var fullPath = sysPath.resolve(path);
  var isDirectory = this._watched[path] || this._watched[fullPath];

  // prevent duplicate handling in case of arriving here nearly simultaneously
  // via multiple paths (such as _handleFile and _handleDir)
  if (!this._throttle('remove', path, 100)) return;

  // if the only watched file is removed, watch for its return
  var watchedDirs = Object.keys(this._watched);
  if (!isDirectory && !this.options.useFsEvents && watchedDirs.length === 1) ***REMOVED***
    this.add(directory, item, true);
  ***REMOVED***

  // This will create a new entry in the watched object in either case
  // so we got to do the directory check beforehand
  var nestedDirectoryChildren = this._getWatchedDir(path).children();

  // Recursively remove children directories / files.
  nestedDirectoryChildren.forEach(function(nestedItem) ***REMOVED***
    this._remove(path, nestedItem);
  ***REMOVED***, this);

  // Check if item was on the watched list and remove it
  var parent = this._getWatchedDir(directory);
  var wasTracked = parent.has(item);
  parent.remove(item);

  // If we wait for this file to be fully written, cancel the wait.
  var relPath = path;
  if (this.options.cwd) relPath = sysPath.relative(this.options.cwd, path);
  if (this.options.awaitWriteFinish && this._pendingWrites[relPath]) ***REMOVED***
    var event = this._pendingWrites[relPath].cancelWait();
    if (event === 'add') return;
  ***REMOVED***

  // The Entry will either be a directory that just got removed
  // or a bogus entry to a file, in either case we have to remove it
  delete this._watched[path];
  delete this._watched[fullPath];
  var eventName = isDirectory ? 'unlinkDir' : 'unlink';
  if (wasTracked && !this._isIgnored(path)) this._emit(eventName, path);

  // Avoid conflicts if we later create another file with the same name
  if (!this.options.useFsEvents) ***REMOVED***
    this._closePath(path);
  ***REMOVED***
***REMOVED***;

FSWatcher.prototype._closePath = function(path) ***REMOVED***
  if (!this._closers[path]) return;
  this._closers[path]();
  delete this._closers[path];
  this._getWatchedDir(sysPath.dirname(path)).remove(sysPath.basename(path));
***REMOVED***

// Public method: Adds paths to be watched on an existing FSWatcher instance

// * paths     - string or array of strings, file/directory paths and/or globs
// * _origAdd  - private boolean, for handling non-existent paths to be watched
// * _internal - private boolean, indicates a non-user add

// Returns an instance of FSWatcher for chaining.
FSWatcher.prototype.add = function(paths, _origAdd, _internal) ***REMOVED***
  var disableGlobbing = this.options.disableGlobbing;
  var cwd = this.options.cwd;
  this.closed = false;
  paths = flatten(arrify(paths));

  if (!paths.every(isString)) ***REMOVED***
    throw new TypeError('Non-string provided as watch path: ' + paths);
  ***REMOVED***

  if (cwd) paths = paths.map(function(path) ***REMOVED***
    var absPath;
    if (isAbsolute(path)) ***REMOVED***
      absPath = path;
    ***REMOVED*** else if (path[0] === '!') ***REMOVED***
      absPath = '!' + sysPath.join(cwd, path.substring(1));
    ***REMOVED*** else ***REMOVED***
      absPath = sysPath.join(cwd, path);
    ***REMOVED***

    // Check `path` instead of `absPath` because the cwd portion can't be a glob
    if (disableGlobbing || !isGlob(path)) ***REMOVED***
      return absPath;
    ***REMOVED*** else ***REMOVED***
      return normalizePath(absPath);
    ***REMOVED***
  ***REMOVED***);

  // set aside negated glob strings
  paths = paths.filter(function(path) ***REMOVED***
    if (path[0] === '!') ***REMOVED***
      this._ignoredPaths[path.substring(1)] = true;
    ***REMOVED*** else ***REMOVED***
      // if a path is being added that was previously ignored, stop ignoring it
      delete this._ignoredPaths[path];
      delete this._ignoredPaths[path + '/**'];

      // reset the cached userIgnored anymatch fn
      // to make ignoredPaths changes effective
      this._userIgnored = null;

      return true;
    ***REMOVED***
  ***REMOVED***, this);

  if (this.options.useFsEvents && FsEventsHandler.canUse()) ***REMOVED***
    if (!this._readyCount) this._readyCount = paths.length;
    if (this.options.persistent) this._readyCount *= 2;
    paths.forEach(this._addToFsEvents, this);
  ***REMOVED*** else ***REMOVED***
    if (!this._readyCount) this._readyCount = 0;
    this._readyCount += paths.length;
    asyncEach(paths, function(path, next) ***REMOVED***
      this._addToNodeFs(path, !_internal, 0, 0, _origAdd, function(err, res) ***REMOVED***
        if (res) this._emitReady();
        next(err, res);
      ***REMOVED***.bind(this));
    ***REMOVED***.bind(this), function(error, results) ***REMOVED***
      results.forEach(function(item) ***REMOVED***
        if (!item || this.closed) return;
        this.add(sysPath.dirname(item), sysPath.basename(_origAdd || item));
      ***REMOVED***, this);
    ***REMOVED***.bind(this));
  ***REMOVED***

  return this;
***REMOVED***;

// Public method: Close watchers or start ignoring events from specified paths.

// * paths     - string or array of strings, file/directory paths and/or globs

// Returns instance of FSWatcher for chaining.
FSWatcher.prototype.unwatch = function(paths) ***REMOVED***
  if (this.closed) return this;
  paths = flatten(arrify(paths));

  paths.forEach(function(path) ***REMOVED***
    // convert to absolute path unless relative path already matches
    if (!isAbsolute(path) && !this._closers[path]) ***REMOVED***
      if (this.options.cwd) path = sysPath.join(this.options.cwd, path);
      path = sysPath.resolve(path);
    ***REMOVED***

    this._closePath(path);

    this._ignoredPaths[path] = true;
    if (path in this._watched) ***REMOVED***
      this._ignoredPaths[path + '/**'] = true;
    ***REMOVED***

    // reset the cached userIgnored anymatch fn
    // to make ignoredPaths changes effective
    this._userIgnored = null;
  ***REMOVED***, this);

  return this;
***REMOVED***;

// Public method: Close watchers and remove all listeners from watched paths.

// Returns instance of FSWatcher for chaining.
FSWatcher.prototype.close = function() ***REMOVED***
  if (this.closed) return this;

  this.closed = true;
  Object.keys(this._closers).forEach(function(watchPath) ***REMOVED***
    this._closers[watchPath]();
    delete this._closers[watchPath];
  ***REMOVED***, this);
  this._watched = Object.create(null);

  this.removeAllListeners();
  return this;
***REMOVED***;

// Public method: Expose list of watched paths

// Returns object w/ dir paths as keys and arrays of contained paths as values.
FSWatcher.prototype.getWatched = function() ***REMOVED***
  var watchList = ***REMOVED******REMOVED***;
  Object.keys(this._watched).forEach(function(dir) ***REMOVED***
    var key = this.options.cwd ? sysPath.relative(this.options.cwd, dir) : dir;
    watchList[key || '.'] = Object.keys(this._watched[dir]._items).sort();
  ***REMOVED***.bind(this));
  return watchList;
***REMOVED***;

// Attach watch handler prototype methods
function importHandler(handler) ***REMOVED***
  Object.keys(handler.prototype).forEach(function(method) ***REMOVED***
    FSWatcher.prototype[method] = handler.prototype[method];
  ***REMOVED***);
***REMOVED***
importHandler(NodeFsHandler);
if (FsEventsHandler.canUse()) importHandler(FsEventsHandler);

// Export FSWatcher class
exports.FSWatcher = FSWatcher;

// Public function: Instantiates watcher with paths to be tracked.

// * paths     - string or array of strings, file/directory paths and/or globs
// * options   - object, chokidar options

// Returns an instance of FSWatcher for chaining.
exports.watch = function(paths, options) ***REMOVED***
  return new FSWatcher(options).add(paths);
***REMOVED***;
