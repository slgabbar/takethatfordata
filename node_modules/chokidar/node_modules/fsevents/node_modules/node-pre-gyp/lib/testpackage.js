"use strict";

module.exports = exports = testpackage;

exports.usage = 'Tests that the staged package is valid';

var fs = require('fs');
var path = require('path');
var log = require('npmlog');
var existsAsync = fs.exists || path.exists;
var versioning = require('./util/versioning.js');
var testbinary = require('./testbinary.js');
var read = require('fs').createReadStream;
var zlib = require('zlib');

function testpackage(gyp, argv, callback) ***REMOVED***
    var package_json = JSON.parse(fs.readFileSync('./package.json'));
    var opts = versioning.evaluate(package_json, gyp.opts);
    var tarball = opts.staged_tarball;
    existsAsync(tarball, function(found) ***REMOVED***
        if (!found) ***REMOVED***
            return callback(new Error("Cannot test package because " + tarball + " missing: run `node-pre-gyp package` first"));
        ***REMOVED***
        var to = opts.module_path;
        var gunzip = zlib.createGunzip();
        var extracter = require('tar').Extract(***REMOVED*** path: to, strip: 1 ***REMOVED***);
        function filter_func(entry) ***REMOVED***
            // ensure directories are +x
            // https://github.com/mapnik/node-mapnik/issues/262
            entry.props.mode |= (entry.props.mode >>> 2) & parseInt('0111',8);
            log.info('install','unpacking ' + entry.path);
        ***REMOVED***
        gunzip.on('error', callback);
        extracter.on('error', callback);
        extracter.on('entry', filter_func);
        extracter.on('end', function(err) ***REMOVED***
            if (err) return callback(err);
            testbinary(gyp,argv,function(err) ***REMOVED***
                if (err) ***REMOVED***
                    return callback(err);
                ***REMOVED*** else ***REMOVED***
                    console.log('['+package_json.name+'] Package appears valid');
                    return callback();
                ***REMOVED***
            ***REMOVED***);
        ***REMOVED***);
        read(tarball).pipe(gunzip).pipe(extracter);
    ***REMOVED***);
***REMOVED***
