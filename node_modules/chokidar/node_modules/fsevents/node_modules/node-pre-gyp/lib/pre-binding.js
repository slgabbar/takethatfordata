"use strict";

var versioning = require('../lib/util/versioning.js');
var existsSync = require('fs').existsSync || require('path').existsSync;
var path = require('path');

module.exports = exports;

exports.usage = 'Finds the require path for the node-pre-gyp installed module';

exports.validate = function(package_json) ***REMOVED***
    versioning.validate_config(package_json);
***REMOVED***;

exports.find = function(package_json_path,opts) ***REMOVED***
   if (!existsSync(package_json_path)) ***REMOVED***
        throw new Error("package.json does not exist at " + package_json_path);
   ***REMOVED***
   var package_json = require(package_json_path);
   versioning.validate_config(package_json);
   opts = opts || ***REMOVED******REMOVED***;
   if (!opts.module_root) opts.module_root = path.dirname(package_json_path);
   var meta = versioning.evaluate(package_json,opts);
   return meta.module;
***REMOVED***;
