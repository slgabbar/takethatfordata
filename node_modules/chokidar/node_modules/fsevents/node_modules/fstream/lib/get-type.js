module.exports = getType

function getType (st) ***REMOVED***
  var types = [
    'Directory',
    'File',
    'SymbolicLink',
    'Link', // special for hardlinks from tarballs
    'BlockDevice',
    'CharacterDevice',
    'FIFO',
    'Socket'
  ]
  var type

  if (st.type && types.indexOf(st.type) !== -1) ***REMOVED***
    st[st.type] = true
    return st.type
  ***REMOVED***

  for (var i = 0, l = types.length; i < l; i++) ***REMOVED***
    type = types[i]
    var is = st[type] || st['is' + type]
    if (typeof is === 'function') is = is.call(st)
    if (is) ***REMOVED***
      st[type] = true
      st.type = type
      return type
    ***REMOVED***
  ***REMOVED***

  return null
***REMOVED***
