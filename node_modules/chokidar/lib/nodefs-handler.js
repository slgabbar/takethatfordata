'use strict';

var fs = require('fs');
var sysPath = require('path');
var readdirp = require('readdirp');
var isBinaryPath = require('is-binary-path');

// fs.watch helpers

// object to hold per-process fs.watch instances
// (may be shared across chokidar FSWatcher instances)
var FsWatchInstances = Object.create(null);

// Private function: Instantiates the fs.watch interface

// * path       - string, path to be watched
// * options    - object, options to be passed to fs.watch
// * listener   - function, main event handler
// * errHandler - function, handler which emits info about errors
// * emitRaw    - function, handler which emits raw event data

// Returns new fsevents instance
function createFsWatchInstance(path, options, listener, errHandler, emitRaw) ***REMOVED***
  var handleEvent = function(rawEvent, evPath) ***REMOVED***
    listener(path);
    emitRaw(rawEvent, evPath, ***REMOVED***watchedPath: path***REMOVED***);

    // emit based on events occuring for files from a directory's watcher in
    // case the file's watcher misses it (and rely on throttling to de-dupe)
    if (evPath && path !== evPath) ***REMOVED***
      fsWatchBroadcast(
        sysPath.resolve(path, evPath), 'listeners', sysPath.join(path, evPath)
      );
    ***REMOVED***
  ***REMOVED***;
  try ***REMOVED***
    return fs.watch(path, options, handleEvent);
  ***REMOVED*** catch (error) ***REMOVED***
    errHandler(error);
  ***REMOVED***
***REMOVED***

// Private function: Helper for passing fs.watch event data to a
// collection of listeners

// * fullPath   - string, absolute path bound to the fs.watch instance
// * type       - string, listener type
// * val[1..3]  - arguments to be passed to listeners

// Returns nothing
function fsWatchBroadcast(fullPath, type, val1, val2, val3) ***REMOVED***
  if (!FsWatchInstances[fullPath]) return;
  FsWatchInstances[fullPath][type].forEach(function(listener) ***REMOVED***
    listener(val1, val2, val3);
  ***REMOVED***);
***REMOVED***

// Private function: Instantiates the fs.watch interface or binds listeners
// to an existing one covering the same file system entry

// * path       - string, path to be watched
// * fullPath   - string, absolute path
// * options    - object, options to be passed to fs.watch
// * handlers   - object, container for event listener functions

// Returns close function
function setFsWatchListener(path, fullPath, options, handlers) ***REMOVED***
  var listener = handlers.listener;
  var errHandler = handlers.errHandler;
  var rawEmitter = handlers.rawEmitter;
  var container = FsWatchInstances[fullPath];
  var watcher;
  if (!options.persistent) ***REMOVED***
    watcher = createFsWatchInstance(
      path, options, listener, errHandler, rawEmitter
    );
    return watcher.close.bind(watcher);
  ***REMOVED***
  if (!container) ***REMOVED***
    watcher = createFsWatchInstance(
      path,
      options,
      fsWatchBroadcast.bind(null, fullPath, 'listeners'),
      errHandler, // no need to use broadcast here
      fsWatchBroadcast.bind(null, fullPath, 'rawEmitters')
    );
    if (!watcher) return;
    var broadcastErr = fsWatchBroadcast.bind(null, fullPath, 'errHandlers');
    watcher.on('error', function(error) ***REMOVED***
      // Workaround for https://github.com/joyent/node/issues/4337
      if (process.platform === 'win32' && error.code === 'EPERM') ***REMOVED***
        fs.open(path, 'r', function(err, fd) ***REMOVED***
          if (fd) fs.close(fd);
          if (!err) broadcastErr(error);
        ***REMOVED***);
      ***REMOVED*** else ***REMOVED***
        broadcastErr(error);
      ***REMOVED***
    ***REMOVED***);
    container = FsWatchInstances[fullPath] = ***REMOVED***
      listeners: [listener],
      errHandlers: [errHandler],
      rawEmitters: [rawEmitter],
      watcher: watcher
    ***REMOVED***;
  ***REMOVED*** else ***REMOVED***
    container.listeners.push(listener);
    container.errHandlers.push(errHandler);
    container.rawEmitters.push(rawEmitter);
  ***REMOVED***
  var listenerIndex = container.listeners.length - 1;

  // removes this instance's listeners and closes the underlying fs.watch
  // instance if there are no more listeners left
  return function close() ***REMOVED***
    delete container.listeners[listenerIndex];
    delete container.errHandlers[listenerIndex];
    delete container.rawEmitters[listenerIndex];
    if (!Object.keys(container.listeners).length) ***REMOVED***
      container.watcher.close();
      delete FsWatchInstances[fullPath];
    ***REMOVED***
  ***REMOVED***;
***REMOVED***

// fs.watchFile helpers

// object to hold per-process fs.watchFile instances
// (may be shared across chokidar FSWatcher instances)
var FsWatchFileInstances = Object.create(null);

// Private function: Instantiates the fs.watchFile interface or binds listeners
// to an existing one covering the same file system entry

// * path       - string, path to be watched
// * fullPath   - string, absolute path
// * options    - object, options to be passed to fs.watchFile
// * handlers   - object, container for event listener functions

// Returns close function
function setFsWatchFileListener(path, fullPath, options, handlers) ***REMOVED***
  var listener = handlers.listener;
  var rawEmitter = handlers.rawEmitter;
  var container = FsWatchFileInstances[fullPath];
  var listeners = [];
  var rawEmitters = [];
  if (
    container && (
      container.options.persistent < options.persistent ||
      container.options.interval > options.interval
    )
  ) ***REMOVED***
    // "Upgrade" the watcher to persistence or a quicker interval.
    // This creates some unlikely edge case issues if the user mixes
    // settings in a very weird way, but solving for those cases
    // doesn't seem worthwhile for the added complexity.
    listeners = container.listeners;
    rawEmitters = container.rawEmitters;
    fs.unwatchFile(fullPath);
    container = false;
  ***REMOVED***
  if (!container) ***REMOVED***
    listeners.push(listener);
    rawEmitters.push(rawEmitter);
    container = FsWatchFileInstances[fullPath] = ***REMOVED***
      listeners: listeners,
      rawEmitters: rawEmitters,
      options: options,
      watcher: fs.watchFile(fullPath, options, function(curr, prev) ***REMOVED***
        container.rawEmitters.forEach(function(rawEmitter) ***REMOVED***
          rawEmitter('change', fullPath, ***REMOVED***curr: curr, prev: prev***REMOVED***);
        ***REMOVED***);
        var currmtime = curr.mtime.getTime();
        if (curr.size !== prev.size || currmtime > prev.mtime.getTime() || currmtime === 0) ***REMOVED***
          container.listeners.forEach(function(listener) ***REMOVED***
            listener(path, curr);
          ***REMOVED***);
        ***REMOVED***
      ***REMOVED***)
    ***REMOVED***;
  ***REMOVED*** else ***REMOVED***
    container.listeners.push(listener);
    container.rawEmitters.push(rawEmitter);
  ***REMOVED***
  var listenerIndex = container.listeners.length - 1;

  // removes this instance's listeners and closes the underlying fs.watchFile
  // instance if there are no more listeners left
  return function close() ***REMOVED***
    delete container.listeners[listenerIndex];
    delete container.rawEmitters[listenerIndex];
    if (!Object.keys(container.listeners).length) ***REMOVED***
      fs.unwatchFile(fullPath);
      delete FsWatchFileInstances[fullPath];
    ***REMOVED***
  ***REMOVED***;
***REMOVED***

// fake constructor for attaching nodefs-specific prototype methods that
// will be copied to FSWatcher's prototype
function NodeFsHandler() ***REMOVED******REMOVED***

// Private method: Watch file for changes with fs.watchFile or fs.watch.

// * path     - string, path to file or directory.
// * listener - function, to be executed on fs change.

// Returns close function for the watcher instance
NodeFsHandler.prototype._watchWithNodeFs =
function(path, listener) ***REMOVED***
  var directory = sysPath.dirname(path);
  var basename = sysPath.basename(path);
  var parent = this._getWatchedDir(directory);
  parent.add(basename);
  var absolutePath = sysPath.resolve(path);
  var options = ***REMOVED***persistent: this.options.persistent***REMOVED***;
  if (!listener) listener = Function.prototype; // empty function

  var closer;
  if (this.options.usePolling) ***REMOVED***
    options.interval = this.enableBinaryInterval && isBinaryPath(basename) ?
      this.options.binaryInterval : this.options.interval;
    closer = setFsWatchFileListener(path, absolutePath, options, ***REMOVED***
      listener: listener,
      rawEmitter: this.emit.bind(this, 'raw')
    ***REMOVED***);
  ***REMOVED*** else ***REMOVED***
    closer = setFsWatchListener(path, absolutePath, options, ***REMOVED***
      listener: listener,
      errHandler: this._handleError.bind(this),
      rawEmitter: this.emit.bind(this, 'raw')
    ***REMOVED***);
  ***REMOVED***
  return closer;
***REMOVED***;

// Private method: Watch a file and emit add event if warranted

// * file       - string, the file's path
// * stats      - object, result of fs.stat
// * initialAdd - boolean, was the file added at watch instantiation?
// * callback   - function, called when done processing as a newly seen file

// Returns close function for the watcher instance
NodeFsHandler.prototype._handleFile =
function(file, stats, initialAdd, callback) ***REMOVED***
  var dirname = sysPath.dirname(file);
  var basename = sysPath.basename(file);
  var parent = this._getWatchedDir(dirname);

  // if the file is already being watched, do nothing
  if (parent.has(basename)) return callback();

  // kick off the watcher
  var closer = this._watchWithNodeFs(file, function(path, newStats) ***REMOVED***
    if (!this._throttle('watch', file, 5)) return;
    if (!newStats || newStats && newStats.mtime.getTime() === 0) ***REMOVED***
      fs.stat(file, function(error, newStats) ***REMOVED***
        // Fix issues where mtime is null but file is still present
        if (error) ***REMOVED***
          this._remove(dirname, basename);
        ***REMOVED*** else ***REMOVED***
          this._emit('change', file, newStats);
        ***REMOVED***
      ***REMOVED***.bind(this));
    // add is about to be emitted if file not already tracked in parent
    ***REMOVED*** else if (parent.has(basename)) ***REMOVED***
      this._emit('change', file, newStats);
    ***REMOVED***
  ***REMOVED***.bind(this));

  // emit an add event if we're supposed to
  if (!(initialAdd && this.options.ignoreInitial)) ***REMOVED***
    if (!this._throttle('add', file, 0)) return;
    this._emit('add', file, stats);
  ***REMOVED***

  if (callback) callback();
  return closer;
***REMOVED***;

// Private method: Handle symlinks encountered while reading a dir

// * entry      - object, entry object returned by readdirp
// * directory  - string, path of the directory being read
// * path       - string, path of this item
// * item       - string, basename of this item

// Returns true if no more processing is needed for this entry.
NodeFsHandler.prototype._handleSymlink =
function(entry, directory, path, item) ***REMOVED***
  var full = entry.fullPath;
  var dir = this._getWatchedDir(directory);

  if (!this.options.followSymlinks) ***REMOVED***
    // watch symlink directly (don't follow) and detect changes
    this._readyCount++;
    fs.realpath(path, function(error, linkPath) ***REMOVED***
      if (dir.has(item)) ***REMOVED***
        if (this._symlinkPaths[full] !== linkPath) ***REMOVED***
          this._symlinkPaths[full] = linkPath;
          this._emit('change', path, entry.stat);
        ***REMOVED***
      ***REMOVED*** else ***REMOVED***
        dir.add(item);
        this._symlinkPaths[full] = linkPath;
        this._emit('add', path, entry.stat);
      ***REMOVED***
      this._emitReady();
    ***REMOVED***.bind(this));
    return true;
  ***REMOVED***

  // don't follow the same symlink more than once
  if (this._symlinkPaths[full]) return true;
  else this._symlinkPaths[full] = true;
***REMOVED***;

// Private method: Read directory to add / remove files from `@watched` list
// and re-read it on change.

// * dir        - string, fs path.
// * stats      - object, result of fs.stat
// * initialAdd - boolean, was the file added at watch instantiation?
// * depth      - int, depth relative to user-supplied path
// * target     - string, child path actually targeted for watch
// * wh         - object, common watch helpers for this path
// * callback   - function, called when dir scan is complete

// Returns close function for the watcher instance
NodeFsHandler.prototype._handleDir =
function(dir, stats, initialAdd, depth, target, wh, callback) ***REMOVED***
  var parentDir = this._getWatchedDir(sysPath.dirname(dir));
  var tracked = parentDir.has(sysPath.basename(dir));
  if (!(initialAdd && this.options.ignoreInitial) && !target && !tracked) ***REMOVED***
    if (!wh.hasGlob || wh.globFilter(dir)) this._emit('addDir', dir, stats);
  ***REMOVED***

  // ensure dir is tracked (harmless if redundant)
  parentDir.add(sysPath.basename(dir));
  this._getWatchedDir(dir);

  var read = function(directory, initialAdd, done) ***REMOVED***
    // Normalize the directory name on Windows
    directory = sysPath.join(directory, '');

    if (!wh.hasGlob) ***REMOVED***
      var throttler = this._throttle('readdir', directory, 1000);
      if (!throttler) return;
    ***REMOVED***

    var previous = this._getWatchedDir(wh.path);
    var current = [];

    readdirp(***REMOVED***
      root: directory,
      entryType: 'all',
      fileFilter: wh.filterPath,
      directoryFilter: wh.filterDir,
      depth: 0,
      lstat: true
    ***REMOVED***).on('data', function(entry) ***REMOVED***
      var item = entry.path;
      var path = sysPath.join(directory, item);
      current.push(item);

      if (entry.stat.isSymbolicLink() &&
        this._handleSymlink(entry, directory, path, item)) return;

      // Files that present in current directory snapshot
      // but absent in previous are added to watch list and
      // emit `add` event.
      if (item === target || !target && !previous.has(item)) ***REMOVED***
        this._readyCount++;

        // ensure relativeness of path is preserved in case of watcher reuse
        path = sysPath.join(dir, sysPath.relative(dir, path));

        this._addToNodeFs(path, initialAdd, wh, depth + 1);
      ***REMOVED***
    ***REMOVED***.bind(this)).on('end', function() ***REMOVED***
      if (throttler) throttler.clear();
      if (done) done();

      // Files that absent in current directory snapshot
      // but present in previous emit `remove` event
      // and are removed from @watched[directory].
      previous.children().filter(function(item) ***REMOVED***
        return item !== directory &&
          current.indexOf(item) === -1 &&
          // in case of intersecting globs;
          // a path may have been filtered out of this readdir, but
          // shouldn't be removed because it matches a different glob
          (!wh.hasGlob || wh.filterPath(***REMOVED***
            fullPath: sysPath.resolve(directory, item)
          ***REMOVED***));
      ***REMOVED***).forEach(function(item) ***REMOVED***
        this._remove(directory, item);
      ***REMOVED***, this);
    ***REMOVED***.bind(this)).on('error', this._handleError.bind(this));
  ***REMOVED***.bind(this);

  var closer;

  if (this.options.depth == null || depth <= this.options.depth) ***REMOVED***
    if (!target) read(dir, initialAdd, callback);
    closer = this._watchWithNodeFs(dir, function(dirPath, stats) ***REMOVED***
      // if current directory is removed, do nothing
      if (stats && stats.mtime.getTime() === 0) return;

      read(dirPath, false);
    ***REMOVED***);
  ***REMOVED*** else ***REMOVED***
    callback();
  ***REMOVED***
  return closer;
***REMOVED***;

// Private method: Handle added file, directory, or glob pattern.
// Delegates call to _handleFile / _handleDir after checks.

// * path       - string, path to file or directory.
// * initialAdd - boolean, was the file added at watch instantiation?
// * depth      - int, depth relative to user-supplied path
// * target     - string, child path actually targeted for watch
// * callback   - function, indicates whether the path was found or not

// Returns nothing
NodeFsHandler.prototype._addToNodeFs =
function(path, initialAdd, priorWh, depth, target, callback) ***REMOVED***
  if (!callback) callback = Function.prototype;
  var ready = this._emitReady;
  if (this._isIgnored(path) || this.closed) ***REMOVED***
    ready();
    return callback(null, false);
  ***REMOVED***

  var wh = this._getWatchHelpers(path, depth);
  if (!wh.hasGlob && priorWh) ***REMOVED***
    wh.hasGlob = priorWh.hasGlob;
    wh.globFilter = priorWh.globFilter;
    wh.filterPath = priorWh.filterPath;
    wh.filterDir = priorWh.filterDir;
  ***REMOVED***

  // evaluate what is at the path we're being asked to watch
  fs[wh.statMethod](wh.watchPath, function(error, stats) ***REMOVED***
    if (this._handleError(error)) return callback(null, path);
    if (this._isIgnored(wh.watchPath, stats)) ***REMOVED***
      ready();
      return callback(null, false);
    ***REMOVED***

    var initDir = function(dir, target) ***REMOVED***
      return this._handleDir(dir, stats, initialAdd, depth, target, wh, ready);
    ***REMOVED***.bind(this);

    var closer;
    if (stats.isDirectory()) ***REMOVED***
      closer = initDir(wh.watchPath, target);
    ***REMOVED*** else if (stats.isSymbolicLink()) ***REMOVED***
      var parent = sysPath.dirname(wh.watchPath);
      this._getWatchedDir(parent).add(wh.watchPath);
      this._emit('add', wh.watchPath, stats);
      closer = initDir(parent, path);

      // preserve this symlink's target path
      fs.realpath(path, function(error, targetPath) ***REMOVED***
        this._symlinkPaths[sysPath.resolve(path)] = targetPath;
        ready();
      ***REMOVED***.bind(this));
    ***REMOVED*** else ***REMOVED***
      closer = this._handleFile(wh.watchPath, stats, initialAdd, ready);
    ***REMOVED***

    if (closer) this._closers[path] = closer;
    callback(null, false);
  ***REMOVED***.bind(this));
***REMOVED***;

module.exports = NodeFsHandler;
