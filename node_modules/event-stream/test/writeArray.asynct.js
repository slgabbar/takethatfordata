
var es = require('../')
  , it = require('it-is').style('colour')
  , d = require('ubelt')

exports ['write an array'] = function (test) ***REMOVED***

  var readThis = d.map(3, 6, 100, d.id) //array of multiples of 3 < 100

  var writer = es.writeArray(function (err, array)***REMOVED***
    if(err) throw err //unpossible
    it(array).deepEqual(readThis)
    test.done()     
  ***REMOVED***)
  
  d.each(readThis, writer.write.bind(writer))
  writer.end()

***REMOVED***


exports ['writer is writable, but not readable'] = function (test) ***REMOVED***
  var reader = es.writeArray(function () ***REMOVED******REMOVED***)
  it(reader).has(***REMOVED***
    readable: false,
    writable: true
  ***REMOVED***)

  test.done()
***REMOVED***
require('./helper')(module)
