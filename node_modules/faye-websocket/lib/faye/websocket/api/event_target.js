var Event = require('./event');

var EventTarget = ***REMOVED***
  onopen:     null,
  onmessage:  null,
  onerror:    null,
  onclose:    null,

  addEventListener: function(eventType, listener, useCapture) ***REMOVED***
    this.on(eventType, listener);
  ***REMOVED***,

  removeEventListener: function(eventType, listener, useCapture) ***REMOVED***
    this.removeListener(eventType, listener);
  ***REMOVED***,

  dispatchEvent: function(event) ***REMOVED***
    event.target = event.currentTarget = this;
    event.eventPhase = Event.AT_TARGET;

    if (this['on' + event.type])
      this['on' + event.type](event);

    this.emit(event.type, event);
  ***REMOVED***
***REMOVED***;

module.exports = EventTarget;
