var ProxyServer = require('../spec/proxy_server');

var port   = process.argv[2],
    secure = process.argv[3] === 'tls',
    proxy  = new ProxyServer(***REMOVED***debug: true, tls: secure***REMOVED***);

proxy.listen(port);
