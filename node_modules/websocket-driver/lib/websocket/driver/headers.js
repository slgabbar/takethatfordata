'use strict';

var Headers = function() ***REMOVED***
  this.clear();
***REMOVED***;

Headers.prototype.ALLOWED_DUPLICATES = ['set-cookie', 'set-cookie2', 'warning', 'www-authenticate'];

Headers.prototype.clear = function() ***REMOVED***
  this._sent  = ***REMOVED******REMOVED***;
  this._lines = [];
***REMOVED***;

Headers.prototype.set = function(name, value) ***REMOVED***
  if (value === undefined) return;

  name = this._strip(name);
  value = this._strip(value);

  var key = name.toLowerCase();
  if (!this._sent.hasOwnProperty(key) || this.ALLOWED_DUPLICATES.indexOf(key) >= 0) ***REMOVED***
    this._sent[key] = true;
    this._lines.push(name + ': ' + value + '\r\n');
  ***REMOVED***
***REMOVED***;

Headers.prototype.toString = function() ***REMOVED***
  return this._lines.join('');
***REMOVED***;

Headers.prototype._strip = function(string) ***REMOVED***
  return string.toString().replace(/^ */, '').replace(/ *$/, '');
***REMOVED***;

module.exports = Headers;
