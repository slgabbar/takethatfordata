'use strict';
module.exports = function (url) ***REMOVED***
	if (typeof url !== 'string') ***REMOVED***
		throw new TypeError('Expected a string, got ' + typeof url);
	***REMOVED***

	url = url.trim();

	if (/^\.*\/|^(?!localhost)\w+:/.test(url)) ***REMOVED***
		return url;
	***REMOVED***

	return url.replace(/^(?!(?:\w+:)?\/\/)/, 'http://');
***REMOVED***;
