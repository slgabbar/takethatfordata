var Stream = require('stream')

// through
//
// a stream that does nothing but re-emit the input.
// useful for aggregating a series of changing but not ending streams into one stream)

exports = module.exports = through
through.through = through

//create a readable writable stream.

function through (write, end, opts) ***REMOVED***
  write = write || function (data) ***REMOVED*** this.queue(data) ***REMOVED***
  end = end || function () ***REMOVED*** this.queue(null) ***REMOVED***

  var ended = false, destroyed = false, buffer = [], _ended = false
  var stream = new Stream()
  stream.readable = stream.writable = true
  stream.paused = false

//  stream.autoPause   = !(opts && opts.autoPause   === false)
  stream.autoDestroy = !(opts && opts.autoDestroy === false)

  stream.write = function (data) ***REMOVED***
    write.call(this, data)
    return !stream.paused
  ***REMOVED***

  function drain() ***REMOVED***
    while(buffer.length && !stream.paused) ***REMOVED***
      var data = buffer.shift()
      if(null === data)
        return stream.emit('end')
      else
        stream.emit('data', data)
    ***REMOVED***
  ***REMOVED***

  stream.queue = stream.push = function (data) ***REMOVED***
//    console.error(ended)
    if(_ended) return stream
    if(data === null) _ended = true
    buffer.push(data)
    drain()
    return stream
  ***REMOVED***

  //this will be registered as the first 'end' listener
  //must call destroy next tick, to make sure we're after any
  //stream piped from here.
  //this is only a problem if end is not emitted synchronously.
  //a nicer way to do this is to make sure this is the last listener for 'end'

  stream.on('end', function () ***REMOVED***
    stream.readable = false
    if(!stream.writable && stream.autoDestroy)
      process.nextTick(function () ***REMOVED***
        stream.destroy()
      ***REMOVED***)
  ***REMOVED***)

  function _end () ***REMOVED***
    stream.writable = false
    end.call(stream)
    if(!stream.readable && stream.autoDestroy)
      stream.destroy()
  ***REMOVED***

  stream.end = function (data) ***REMOVED***
    if(ended) return
    ended = true
    if(arguments.length) stream.write(data)
    _end() // will emit or queue
    return stream
  ***REMOVED***

  stream.destroy = function () ***REMOVED***
    if(destroyed) return
    destroyed = true
    ended = true
    buffer.length = 0
    stream.writable = stream.readable = false
    stream.emit('close')
    return stream
  ***REMOVED***

  stream.pause = function () ***REMOVED***
    if(stream.paused) return
    stream.paused = true
    return stream
  ***REMOVED***

  stream.resume = function () ***REMOVED***
    if(stream.paused) ***REMOVED***
      stream.paused = false
      stream.emit('resume')
    ***REMOVED***
    drain()
    //may have become paused again,
    //as drain emits 'data'.
    if(!stream.paused)
      stream.emit('drain')
    return stream
  ***REMOVED***
  return stream
***REMOVED***

