var test = require('tape')
var through = require('../')

// must emit end before close.

test('end before close', function (assert) ***REMOVED***
  var ts = through()
  var ended = false, closed = false

  ts.on('end', function () ***REMOVED***
    assert.ok(!closed)
    ended = true
  ***REMOVED***)
  ts.on('close', function () ***REMOVED***
    assert.ok(ended)
    closed = true
  ***REMOVED***)

  ts.write(1)
  ts.write(2)
  ts.write(3)
  ts.end()
  assert.ok(ended)
  assert.ok(closed)
  assert.end()
***REMOVED***)

test('end only once', function (t) ***REMOVED***

  var ts = through()
  var ended = false, closed = false

  ts.on('end', function () ***REMOVED***
    t.equal(ended, false)
    ended = true
  ***REMOVED***)

  ts.queue(null)
  ts.queue(null)
  ts.queue(null)

  ts.resume()

  t.end()
***REMOVED***)
