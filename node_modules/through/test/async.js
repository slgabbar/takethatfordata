var from = require('from')
var through = require('../')

var tape = require('tape')

tape('simple async example', function (t) ***REMOVED***
 
  var n = 0, expected = [1,2,3,4,5], actual = []
  from(expected)
  .pipe(through(function(data) ***REMOVED***
    this.pause()
    n ++
    setTimeout(function()***REMOVED***
      console.log('pushing data', data)
      this.push(data)
      this.resume()
    ***REMOVED***.bind(this), 300)
  ***REMOVED***)).pipe(through(function(data) ***REMOVED***
    console.log('pushing data second time', data);
    this.push(data)
  ***REMOVED***)).on('data', function (d) ***REMOVED***
    actual.push(d)
  ***REMOVED***).on('end', function() ***REMOVED***
    t.deepEqual(actual, expected)
    t.end()
  ***REMOVED***)

***REMOVED***)
