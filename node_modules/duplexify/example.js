var duplexify = require('duplexify')
var http = require('http')

var request = function(opts) ***REMOVED***
  var req = http.request(opts)
  var dup = duplexify()
  dup.setWritable(req)
  req.on('response', function(res) ***REMOVED***
    dup.setReadable(res)
  ***REMOVED***)
  return dup
***REMOVED***

var req = request(***REMOVED***
  method: 'GET',
  host: 'www.google.com',
  port: 80
***REMOVED***)

req.end()
req.pipe(process.stdout)
